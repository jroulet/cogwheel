## Build 1b completes the Chang–Refsdal lens engine. VERIFIED STATE (symbol overview + directory listing, not assumption): _dd.py, _gauge.py and geometry.py are committed; geometry.py's public surface is already macro_matrix, hessian, lens_residual, delay, magnification, morse_index, image_quartic_coefficients, find_images_quartic, find_images, saddle_coefficients, image_kernel, critical_point, nearest_caustic_point, plus LensDomainError / CriticalPoint / NearestCausticPoint — but it is UNTESTED. _gauge.py (smootherstep, exact_cluster_kernel, unresolved_member_channels, exact_transition_channels, reconstructed_total) is complete and tested. MISSING: _hyp1f1.py, operator.py, channels.py and all four test modules; mpmath is still undeclared in pyproject.toml while cogwheel/tests/test_lensing_dd.py imports it at top level (clean-install collection failure), so the closeout must add a test extra. A previously-approved plan for this same brief exists at .claude/handoff/lensing/build1b_plan_approved.md; this plan REISSUES it with four deliberate corrections and is otherwise structurally identical (six WPs: WP1 geometry retrofit tests, no deps, deliberately BEFORE operator work so an untested 872-line module is verified/repaired before anything is built on it; WP2 _hyp1f1, no deps, runs parallel with WP1; WP3 operator, deps 1,2; WP4 channels/ChangRefsdalChannels, deps 1,3; WP5 fold/cusp crossing test-support builders + continuity tests, deps 1,3, parallel with WP4; WP6 merged closeout, deps all). CORRECTION 1 (the important one): the prior plan's WP1 asserts in TWO places that 'all 168 CSV rows are kind=general' and that fold/cusp rows were an exploration-agent hallucination. That claim is FALSE — I verified by regex on the fixture itself that rows 120-167 are 24 fold + 24 cusp (120 general / 24 fold / 24 cusp exactly as the brief's PRE-ANSWERED Q2 errata states). Every restatement of it is removed here, and F004's text is corrected to match; the fold/cusp rows become genuine near-caustic fixtures with assertions on residuals and delays, never positions. CORRECTION 2: the prefactor test must NOT assert the closed form against itself (brief PRE-ANSWERED Q1) — the production |C(w)|^2 = pi*w/(1-e^{-pi*w}) is gated against an independent mpmath evaluation of the DEFINITION |e^{pi w/4 + (iw/2)ln(w/2)} Gamma(1-iw/2)|^2 at >= 60 dps. CORRECTION 3: pylint is not installed (brief PRE-ANSWERED Q5) — every 'pylint clean' gate becomes a programmatic 79-column + ast.parse check; do not block on pylint. CORRECTION 4 (Simplifier, trim): the prior plan's swap of the brute-force _assign_real_images to scipy.optimize.linear_sum_assignment is DROPPED along with its equivalence test — the assignment is bounded at n<=4 by construction (at most 24 permutations), so O(n!) vs O(n^3) is a problem that does not exist here, and the swap would add a test purely to prove two methods agree. Keep the prototype's brute-force matching with deterministic tie-breaking. ASSUMPTIONS DOCUMENTED: (1) The brief's explicit global gate `branch` in {'wave','geometric'} (geometric when w*delta_min >= rho1 AND L > L_max=48) SUPERSEDES the Build-1 v3 plan's W_cut R-truncation knob — the corrective brief is authoritative and two overlapping short-circuits are one too many; ONE approximation knob, reasoning recorded as F002. The smooth switch S_j (rho0=0.5, rho1=4) remains a smoothness/topology device that injects NO error and is NOT an accuracy short-circuit — that distinction stays load-bearing. The single gate implementation lives in operator.py and channels.py consumes it. (2) The brief names F001 (cancellation law) and F002 (tautology finding); the v3 plan's F003 (reconstruction error scales with sum_a|K_hat_a| ~ sqrt|mu_a|, not |F_op|) and F004 (CSV fixture reality + n_multistart provenance) are RETAINED as short entries — dropping them is the dangerous direction, since they are exactly what stops a future maintainer 'fixing' a gate incorrectly. Per the Simplifier, FINDINGS.md is the authoritative registry for these and the module comments POINT AT the finding ID rather than duplicating the full explanation. (3) The v3 plan's numerical carve-outs stand and are non-negotiable: dd is required ONLY inside the 1F1 (L_1F1 = w*|y'|); the operator's representation table, prefactor and geometry stay float64 (L_op = w*gamma'/2 <= 6) while the brief's mandated dd accumulation applies to the running series sum only; the reconstruction gate is scale-aware (100*eps*(|F_op| + sum_a|K_hat_a| + |R|)) with frozen absolute backstops, NOT a flat 1e-12. (4) Crossing builders are TEST-SUPPORT under cogwheel/tests/, not public API — nothing in the production path calls them (YAGNI); if Build 2 needs them, promoting them then is cheap. (5) chang_refsdal_exact_partition.py is not ported (superseded, imported by nothing). (6) The 1e-12 fresh-residual gate vs find_images_quartic's residual_tolerance=3e-8 default is an empirical question (brief Q3) — WP1 MEASURES on all 168 rows, gates at 1e-12 where it holds, and documents any principled near-caustic exception as a finding rather than silently loosening. No DATA_CONTRACTS.yaml artifact is produced or consumed by this build (no on-disk data products), so no pipeline_graph.py queries are required; if any WP ends up caching a table to disk it must register the artifact plus a contracts_changelog.d fragment. PROCESS: the mandated architect_short_term memory write was ATTEMPTED and BLOCKED ('Cannot call mcp__serena__edit_memory while in plan mode'); its content is this summary. Every WP is complete ONLY when its named test module exists under cogwheel/tests/ (NEVER inside the package) and `python -m unittest cogwheel.tests.<module>` passes, with the pass count in the change report.

### WP1: test_lensing_geometry.py — retrofit tests for the committed but untested geometry.py
  What: geometry.py (872 lines) was delivered by Build 1 with NO tests and must be treated as unreviewed. Write cogwheel/tests/test_lensing_geometry.py covering the quartic CSV regression (including the fold/cusp rows), the exact y=0 analytic geometry, the Morse-index census and near-caustic behaviour on delays/residuals — and REPAIR geometry.py where the tests expose real defects. This WP gates every downstream module.
  Where: NEW: cogwheel/tests/test_lensing_geometry.py. EDIT (only if tests expose defects): cogwheel/lensing/chang_refsdal/geometry.py. READ-ONLY: .claude/spec/lensing_paper/data/quartic_geometry_validation.csv (168 rows), .claude/spec/lensing_paper/code/chang_refsdal_geometry.py, paper Appendices A and B in .claude/spec/lensing_paper/relative_binning_chang_refsdal_v5.tex.
  How: HEADLESS DISCIPLINE (program-scoped — verbatim): This build runs unattended: no human reads or answers questions mid-task. The previous run failed exactly this way — coders did excellent read-only analysis, raised good questions, ended with "let me know how to proceed", and the pipeline moved on recording zero changes. Therefore, for THIS program's builds: never end a work package waiting for direction. Resolve ambiguities and task-text errors yourself with the most defensible choice, record each under a DECISIONS line in the change report, and complete the files. A coder WP that changes no files is a FAILED WP unless it ends with 'BLOCKED: <reason>'.

Read geometry.py FIRST (get_symbols_overview, then find_symbol with include_body on find_images_quartic, _newton_polish, _axial_candidates, delay, magnification, morse_index, saddle_coefficients, image_kernel). Its public surface is already: macro_matrix, hessian, lens_residual, delay, magnification, morse_index, image_quartic_coefficients, find_images_quartic, find_images, saddle_coefficients, image_kernel, critical_point, nearest_caustic_point, plus LensDomainError / CriticalPoint / NearestCausticPoint. Do NOT redesign that surface; test it, and fix bugs surgically.

CSV FIXTURE — CORRECTED FACT, get this right: the fixture is 120 kind='general' + 24 kind='fold' + 24 kind='cusp' (rows 120-167 are the fold/cusp block; verified by regex on the file). An earlier plan document claims all 168 rows are 'general' — that claim is WRONG; ignore it. Columns: index, kind, gamma, beta, y1, y2, n_quartic, n_multistart, matched_position_error, max_quartic_residual. Build A = macro_matrix(gamma, beta, kappa=0) per row and run find_images(y, A). Assert image count == row.n_multistart for EVERY row (a frozen oracle from a multistart solver that no longer exists — independent and non-drifting, so it cannot converge toward the quartic's own bugs); assert n_quartic == n_multistart within the CSV as a fixture-integrity check; assert a FRESH gate max_a ||lens_residual(x_a, y, A)|| <= 1e-12. Do NOT assert matched_position_error (a cross-method quantity, unrecomputable without the deleted multistart solver) and do NOT try to reproduce row.max_quartic_residual (polish-detail dependent, refactor-fragile). USE the fold/cusp rows as genuine near-caustic fixtures — they are the fixture's real near-caustic coverage — with assertions on RESIDUALS and DELAYS, never on positions.

RESIDUAL GATE vs SOLVER DEFAULT — an empirical question, resolve it by measuring, not by guessing: find_images_quartic's residual_tolerance default is 3e-8 while the test gate is 1e-12. MEASURE the achieved residual on all 168 rows, gate at 1e-12 where it holds, and if any row genuinely cannot meet it, document the principled exception (near-caustic double root) as a FINDINGS-worthy note in your change report rather than silently loosening the gate for everything.

NEAR-CAUSTIC ASSERTIONS GO ON DELAYS AND RESIDUALS, NOT POSITIONS (the brief is explicit and the physics backs it): near a fold the quartic has a double root, so image POSITIONS carry only sqrt(eps) ~ 1.5e-8. Delays are quadratically insensitive to that because images are stationary points of the Fermat potential, so tau_a retains full eps accuracy. Magnifications mu_a = 1/det J ARE genuinely ill-conditioned at critical points — assert their SCALING behaviour, not a tight absolute value. A test that demands 1e-12 on near-fold POSITIONS is asserting something false and must not be written.

EXACT y=0 ANALYTIC GEOMETRY is mandatory (the CSV's degenerate/axial coverage is thin): gamma swept 0.05..0.4, kappa=0, source exactly at the origin. Expect exactly 4 images at |x|^2 = 1/(1-gamma) (shear axis) and |x|^2 = 1/(1+gamma) (transverse); tau = 1/2 + (1/2)*ln(1 -/+ gamma); Delta tau = artanh(gamma); agreement ~1e-14.

Also verify the domain guard: macro_matrix must raise LensDomainError (a named module exception, not bare ValueError) when 1 - kappa <= |gamma|, with a message naming the offending (kappa, gamma) and stating macro saddles are out of scope. Test both the raise and the message content.

TEST IDIOM: cogwheel uses stdlib unittest under cogwheel/tests/ — follow cogwheel/tests/test_lensing_dd.py exactly (helper base TestCase with a domain assertion method, classes named <Thing>TestCase, itertools.product sweeps with values interpolated into the assertion msg, self.subTest for loop isolation, module ends with `if __name__ == '__main__': main()`). Imports at top of file only. Conventions: angles in point-mass Einstein-radius units, delays dimensionless, w = 8*pi*G*M_L*(1+z_L)*f/c^3 (linear in f).

STYLE GATE: pylint is NOT installed in this environment — do not attempt to run it and do not block on it. Substitute a programmatic check: every line <= 79 columns and ast.parse succeeds on each file you create or edit. Honour the good-names list (i, j, k, ra, m1, m2, q, dt, ax) and numpy-style docstrings on any public function you touch.

If a test reveals a real geometry bug, fix it and say so LOUDLY in the change report — that is this WP's whole reason for existing.
  Who: Coder
  Verification: python -m unittest cogwheel.tests.test_lensing_geometry -v — report the pass count. Gates: (a) all 168 CSV rows, count == n_multistart AND fresh residual <= 1e-12 (with any principled exception documented, not silently loosened); (b) fixture-integrity n_quartic == n_multistart; (c) exact y=0 analytic geometry to ~1e-14; (d) Morse census (4-image: n_a = 0,0,1,1 — TWO MINIMA + TWO SADDLES, NO maximum; 2-image: n_a = 0,1) with the independent 'count is 4 iff inside the astroid' cross-check; (e) near-caustic delay/residual stability on the fold/cusp rows. python -m unittest cogwheel.tests.test_lensing_dd cogwheel.tests.test_lensing_gauge still green (71 tests). Programmatic 79-col + ast.parse check clean on every file created or edited (NOT pylint — it is not installed).
  Max turns: 90

### WP2: _hyp1f1.py — dd-accumulated complex 1F1 kernel and shared-numerator k-ladder
  What: New double-double-backed complex 1F1(1 - i*w/2 + k; 1 + k; -i*w*s/2) kernel and its k-ladder, replacing the prototype's mpmath-only point_mass_G_derivatives. This is the ONLY place in the engine where double-double arithmetic is required.
  Where: NEW: cogwheel/lensing/chang_refsdal/_hyp1f1.py, cogwheel/tests/test_lensing_hyp1f1.py. EDIT (docstring only, no code): cogwheel/lensing/chang_refsdal/_dd.py. READ-ONLY: point_mass_G_derivatives in .claude/spec/lensing_paper/code/chang_refsdal_operator.py (~lines 84-125), paper Sec. II.B.
  How: HEADLESS DISCIPLINE (program-scoped — verbatim): This build runs unattended: no human reads or answers questions mid-task. The previous run failed exactly this way — coders did excellent read-only analysis, raised good questions, ended with "let me know how to proceed", and the pipeline moved on recording zero changes. Therefore, for THIS program's builds: never end a work package waiting for direction. Resolve ambiguities and task-text errors yourself with the most defensible choice, record each under a DECISIONS line in the change report, and complete the files. A coder WP that changes no files is a FAILED WP unless it ends with 'BLOCKED: <reason>'.

CONSUME the committed cogwheel/lensing/chang_refsdal/_dd.py — read it first. It provides dd_complex_add/sub/mul/div and dd_from_float/dd_to_float in a four-flat-scalar (re_hi, re_lo, im_hi, im_lo) representation chosen for numba nopython compatibility. Do NOT rewrite it, re-derive it, or wrap it in classes. It deliberately has no sqrt/exp/log/trig — you do not need them here. Its 37 tests must keep passing unchanged.

KUMMER REPARAMETRIZATION (brief item 1, and the reason the ladder is cheap): reparametrize so that a' = iw/2 is K-INDEPENDENT. This is a STRUCTURAL win — it makes the ladder terms share a numerator — NOT a precision win. Do NOT claim Kummer buys precision: z = -i*w*s/2 is purely imaginary so |e^z| = 1 and Kummer moves no magnitude; both forms have identical max term e^{w*Y}.

WHERE DD IS MANDATORY (quantitative): the Maclaurin series in z — BOTH the terms and their summation. Compensated/Kahan summation does NOT rescue this: its bound still carries sum|term_i|, and the terms are exponentially larger than the result (max term ~ e^{w*Y} at k* = w*Y/2). Float64 (eps=2.2e-16) crosses the 1e-10 gate at L_1F1 = w*|y'| = 13, and the 2-image regime at Y >= 0.5, w = 40 exceeds that — the null-hypothesis bulk of a microlensing posterior, not a corner case. Also dd the Pochhammer/base^k ladder that builds G^(k), since it feeds the Maclaurin at the same precision.

WHERE FLOAT64 IS PROVABLY FINE: the prefactor C(w) = exp[pi*w/4 + (i*w/2)*ln(w/2)]*Gamma(1 - i*w/2). It is a COMMON multiplicative factor across all k and all n, so its relative error factors out of the sum exactly and contributes ~1e-16 flat, unamplified. Do NOT dd it. TWO MANDATORY TRAPS: (a) exp(pi*w/4) OVERFLOWS at w~900 while Gamma(1 - i*w/2) underflows like e^{-pi*w/4} — compute via complex loggamma and combine exponents ANALYTICALLY; never evaluate exp(...)*gamma(...) as written. Implement the closed-form overflow-free magnitude |C(w)|^2 = pi*w/(1 - e^{-pi*w}) using expm1 for the small-w limb (it follows from Gamma(1+ix)*Gamma(1-ix) = pi*x/sinh(pi*x)). (b) The (w/2)*ln(w/2) phase cancels against Im loggamma(1 - i*w/2) — cancel it ANALYTICALLY, not numerically.

SHARED-NUMERATOR LADDER, NO K-RECURRENCE, NO LARGE-|z| BRANCH (the brief is explicit): do NOT k-ladder the 1F1 itself via contiguous relations. Forward recurrence for 1F1(a+k; 1+k; z) is frequently UNSTABLE — the minimal solution gets swamped. The Kummer reparametrization's k-independent a' is what lets the ladder share a numerator instead.

COMPLEXITY ASSERTION — assert only what is substantiated (brief PRE-ANSWERED Q4): assert (a) the number of shared-numerator evaluations is INDEPENDENT of max_order, and (b) the total dd-multiply count is LINEAR in max_order (fit and reject a quadratic). Do NOT assert an unsubstantiated big-O claim.

PREFACTOR TEST — DO NOT WRITE A TAUTOLOGY (brief PRE-ANSWERED Q1; the previous run's coder caught this and was right): the production code computes |C(w)|^2 from the closed form pi*w/(1 - e^{-pi*w}), so asserting the closed form against itself proves nothing. Gate the production value against an INDEPENDENT mpmath evaluation of the DEFINITION |e^{pi w/4 + (iw/2)ln(w/2)} Gamma(1-iw/2)|^2 at >= 60 dps, over w in [1e-3, 500], rtol 1e-14, flat in w. Also check the limits |C|^2 -> 1 as w->0 (confirming the F->1 normalization convention) and -> pi*w as w->inf.

EXPONENT-RANGE CAVEAT: dd extends the MANTISSA, not the EXPONENT. hi is a float64, so overflow is still 1.8e308 and the low word is silently lost near denormals. |G^(k)| reaches ~1e92 at w=40, k=84 and OVERFLOWS at w ~ 700 with max_order=42. Gate the input w with a clear NAMED error above the certified ceiling rather than returning silent garbage.

numba-SHAPED, DEFER @njit: plain loops, plain arrays, no closures, no containers, homogeneous scalar returns, no object mode. Document the intended hot path in the module docstring. Add @numba.njit(cache=True) only if WP3's performance gate fails without it.

mpmath IN TESTS ONLY — never import it from cogwheel/lensing/.

ALSO FIX _dd.py's DOCSTRING ONLY (its WHY section is mis-scoped — it implies the whole pipeline needs dd): state that L_1F1 = w*|y'| and L_op = w*gamma'/2 are INDEPENDENT channels at SEPARATE code sites; that dd is required only for the former; that dd's ceiling (w*Y <= 60 at 1e-6) exists precisely to close the gap to the geometric branch's onset (w*Y >= 50), which float64 (w*Y <= 22) cannot bridge; add the mantissa-not-exponent caveat. NO code changes to _dd.py.

STYLE GATE: pylint is NOT installed — substitute a programmatic 79-column + ast.parse check on every file you create or edit; do not block on pylint.
  Who: Coder
  Verification: python -m unittest cogwheel.tests.test_lensing_hyp1f1 cogwheel.tests.test_lensing_dd -v — report pass counts; the 37 pre-existing dd tests MUST pass unchanged. Gates: production |C(w)|^2 vs an INDEPENDENT mpmath evaluation of the definition at >= 60 dps, rtol 1e-14, flat over w in [1e-3, 500] (NOT the closed form against itself); kernel and k-ladder vs mpmath oracle (60-70 dps) over the certified (w, s) domain incl. w*Y up to the dd ceiling, rel err <= 1e-10, with a cancellation-law fit sitting below the eps*e^{w*Y} contour; shared-numerator evaluation count independent of max_order AND total dd-multiply count linear (quadratic rejected); named error above the w ceiling. grep -r mpmath cogwheel/lensing/ returns nothing. Programmatic 79-col + ast.parse check clean.
  Max turns: 105

### WP3: operator.py — contour-free amplification F_op, mass-sheet rescaling, branch gate, diagnostics
  What: Port chang_refsdal_operator.py to cogwheel/lensing/chang_refsdal/operator.py: point-mass 1F1 seed (via WP2) + D_beta^n source-derivative operator series + EXACT mass-sheet kappa rescaling + the explicit wave/geometric branch gate + convergence/cancellation diagnostics. Absorb the prototype's misplaced _operator_total wrapper as a relative-time-origin option.
  Where: NEW: cogwheel/lensing/chang_refsdal/operator.py, cogwheel/tests/test_lensing_operator.py. READ-ONLY: .claude/spec/lensing_paper/code/chang_refsdal_operator.py (~232 lines), _operator_total + _validate_w in .claude/spec/lensing_paper/code/chang_refsdal_topology_stable.py, .claude/spec/lensing_paper/code/test_external_convergence.py, paper Sec. II.B and Appendix C.
  How: HEADLESS DISCIPLINE (program-scoped — verbatim): This build runs unattended: no human reads or answers questions mid-task. The previous run failed exactly this way — coders did excellent read-only analysis, raised good questions, ended with "let me know how to proceed", and the pipeline moved on recording zero changes. Therefore, for THIS program's builds: never end a work package waiting for direction. Resolve ambiguities and task-text errors yourself with the most defensible choice, record each under a DECISIONS line in the change report, and complete the files. A coder WP that changes no files is a FAILED WP unless it ends with 'BLOCKED: <reason>'.

ROTATE beta INTO THE SHEAR EIGENFRAME (brief item 2): this leaves ONE real representation table instead of a beta-parameterised family. Consequence: do NOT lru_cache on a float — cache the real table keyed by integer order only. Use a DENSE GRADED ARRAY representation for the D_beta^n monomials in (z, zbar, radial-derivative order).

SERIES ACCUMULATION IN dd, float64 ELSEWHERE — and justify it in the module docstring, because it is counterintuitive: term_n = coeff_n * monomials * G^(k), so the accumulation error is eps*sum|term_n| = eps*e^{L_op}*|result| where L_op = w*gamma'/2 <= 6 over the certified domain (w <= 40, gamma' <= 0.3). The L_1F1 cancellation is resolved INSIDE the 1F1 and never re-enters. The brief mandates dd accumulation here for headroom: use _dd's dd_complex_add for the running sum, but do NOT dd the representation table, the prefactor, or the geometry — that would cost 10-20x for nothing. Document the two-knob error model: rel_err ~ eps_1F1*e^{w|y'|} + eps_accum*e^{w*gamma'/2}.

REPLACE point_mass_G_derivatives' mpmath body with WP2's _hyp1f1 kernel and DELETE the dps plumbing entirely (the prototype threads dps=60 -> 70 -> 70/80 with drifting defaults through amplification_grid; it is mpmath-only and must not survive). mpmath must never appear in cogwheel/lensing/.

ADAPTIVE ORDER: n_max ~ zeta + 5*sqrt(zeta) + 10. Keep OperatorDiagnostics as a frozen dataclass exposing order_used, converged, estimated_relative_tail, and the MEASURED cancellation (max_partial_term/|total|) — not just the predicted L_est = w*(|y'| + gamma'/2). RUNTIME REFUSAL: raise a named error when the MEASURED max_partial_term/|total| exceeds ~1e13. Measured, not predicted — that is what makes the precision claim falsifiable and prevents silent domain overrun.

EXPLICIT GLOBAL BRANCH GATE (brief item 3 places this in channels.py; the DECISION for this plan is that operator.py OWNS the single implementation and channels.py consumes it, so there is exactly ONE gate): `branch` in {'wave','geometric'}, geometric when (w*delta_min >= rho1) AND (L > L_max = 48). This SUPERSEDES the Build-1 v3 plan's W_cut R-truncation knob — do NOT implement both. Keep the distinction that the smooth switch S_j (rho0=0.5, rho1=4) is a smoothness/topology device injecting NO error, whereas the branch gate IS an approximation — document that contrast in the module docstring.

MASS-SHEET RESCALING is purely algebraic and float64-exact — WRITE IT ONCE, in this module, and have every other module call it: F_{kappa,gamma}(w, y; beta) = (1/lam)*exp[i*w*(ln(lam)/2 - kappa*|y|^2/(2*lam))]*F_{0,gamma/lam}(w, y/sqrt(lam)), lam = 1 - kappa, same w. The exponential is a pure phase.

HOIST _operator_total (~25 lines) FROM the prototype's topology_stable INTO this module — it is just 'amplification total at a relative time origin' (phase shift by tmin + diagnostics repackaging). Expose it as an optional time-origin argument on the grid evaluator, not a private cross-module import. This is what makes WP4 and WP5 independent. Bring _validate_w's checks along as input validation, and enforce 1 - kappa > |gamma| via geometry's LensDomainError.

Document the certified domain with its derivation: float64 1F1 reaches w*Y <= 22 at 1e-6; the geometric branch reaches 1e-6 at w*Y >= 50; dd reaches w*Y <= 60 and closes the gap. Conventions: w dimensionless (= 8*pi*G*M_L*(1+z_L)*f/c^3, linear in f), Einstein-radius units. numba-shaped hot loops, @njit deferred.

STYLE GATE: pylint is NOT installed — substitute a programmatic 79-column + ast.parse check on every file you create or edit; do not block on pylint.
  Who: Coder
  Dependencies: WP1, WP2
  Verification: python -m unittest cogwheel.tests.test_lensing_operator -v — report the pass count. Gates: F_op vs mpmath oracle rtol <= 1e-10 over the paper's 4 configs plus stress cases (2-image incl. Y ~ 0.5-1.25 where float64 alone provably fails, 4-image, near-fold, near-cusp) for L <= 48; GEOMETRIC-OPTICS SLOPE TEST (fitted exponent in [0.9,1.1] without C1/C2, [2.8,3.2] with — self-oracling, no mpmath); mass-sheet identity in NON-VACUOUS form (Delta tau_ac and |K_a/K_c| exactly kappa-invariant, not just F to 2e-12); gamma->0 point-mass reduction to 1e-12 going as O(gamma^2); cancellation-exponent gate (measured_rel_err <= 20*eps*exp(L_est)) and the runtime refusal above ~1e13 measured cancellation; single F_op at w=20 <= 10 ms. grep -r mpmath cogwheel/lensing/ returns nothing. Programmatic 79-col + ast.parse check clean.
  Max turns: 115

### WP4: channels.py — ChangRefsdalChannels entry point, label continuation, exact projection
  What: The production entry point Build 2 consumes. Port the prototype's GlobalChannelTracker into cogwheel/lensing/chang_refsdal/channels.py as ChangRefsdalChannels: topology-stable 4-label decomposition, path-based label continuation with a deterministic reset convention, virtual labels at the nearest critical point, the smooth switch S_j, and cluster residual projection exposing (tau_a, K_a(w)) plus the exact total.
  Where: NEW: cogwheel/lensing/chang_refsdal/channels.py, cogwheel/tests/test_lensing_channels.py. READ-ONLY: .claude/spec/lensing_paper/code/chang_refsdal_global_tracking.py (~121 lines), paper Secs. V-VI and Appendix D. CONSUMES (already committed and tested): cogwheel/lensing/chang_refsdal/_gauge.py, geometry.py, and WP3's operator.py.
  How: HEADLESS DISCIPLINE (program-scoped — verbatim): This build runs unattended: no human reads or answers questions mid-task. The previous run failed exactly this way — coders did excellent read-only analysis, raised good questions, ended with "let me know how to proceed", and the pipeline moved on recording zero changes. Therefore, for THIS program's builds: never end a work package waiting for direction. Resolve ambiguities and task-text errors yourself with the most defensible choice, record each under a DECISIONS line in the change report, and complete the files. A coder WP that changes no files is a FAILED WP unless it ends with 'BLOCKED: <reason>'.

REUSE _gauge.py — it is committed and tested (34 tests); do NOT reimplement any of its five primitives (smootherstep, exact_cluster_kernel, unresolved_member_channels, exact_transition_channels, reconstructed_total). After WP3 hoists _operator_total, this module needs only geometry (WP1) + operator (WP3) + _gauge. VERIFY that import independence explicitly (assert channels.py does not import the WP5 crossings test-support module).

THE PROTOTYPE'S global_tracking.py IS THE WORST-WRITTEN MODULE IN THE SET — densely compressed, multi-statement lines, semicolons. RESTRUCTURE it; do not transliterate. Preserve the ALGORITHM exactly: assignment-problem label continuation on lens-plane markers, virtual labels at the nearest critical point (geometry.nearest_caustic_point / critical_point are already committed — use them), and _exact_four_channel_projection.

ASSIGNMENT SOLVER — KEEP THE BRUTE FORCE (this is a deliberate reversal of an earlier plan draft, per the Simplifier; do not relitigate): the problem is bounded at n <= 4 images by construction, so itertools.permutations(range(4), n) is at most 24 evaluations — well inside the 100 ms/10-node performance gate. Do NOT swap in scipy.optimize.linear_sum_assignment: it would not reduce code (deterministic tie-breaking is needed either way) and it would add an equivalence test purely to prove two methods agree on a problem size where the asymptotics are irrelevant. What DOES matter: break equal-cost ties DETERMINISTICALLY and test that.

BRANCH GATE: consume operator.py's `branch` in {'wave','geometric'} (WP3 owns the single implementation — do NOT re-implement the (w*delta_min >= rho1) AND (L > L_max=48) condition here). The switch S_j (rho_start=0.5, rho_end=4.0 as documented defaults) is a separate, error-free smoothness device — keep those responsibilities distinct in the code and the docstring.

RESET CONVENTION FOR FAR PROPOSALS: continuation state must support BOTH 'evaluate at a nearby parameter point' (path from the previous point) AND a DETERMINISTIC reset for far points (paper Appendix D) — keep reset() and evaluate_path(). The tracker holds MUTABLE state (self._markers) across calls: document that LOUDLY and make the reset convention deterministic and testable. This is inference-critical: if labels depend on evaluation history, the likelihood is not a function of theta, the posterior is not defined, and detailed balance breaks SILENTLY.

DOCUMENT AND TEST label-permutation invariance: sum_a e^{iw tau_a} K_a is invariant under relabeling; only ratio SMOOTHNESS needs consistent labels.

API FOR BUILD 2: one class constructed from lens params, exposing vectorized delays() -> tau_a (dimensionless), kernels(w) -> K_a array, amplification(w) for brute-force reference, and convergence/series diagnostics. KEEP ONE dataclass here (GlobalPartition-equivalent: w, y, gamma, beta, kappa, theta_critical, x_critical, y_critical, caustic_distance, delays, kernels, slot_positions, real_mask, exact_total, operator_orders, operator_converged, plus reconstructed / reconstruction_error properties). Do NOT unify it with WP5's scenario dataclasses — per-w online tracking and batch scenario construction are different jobs.

RECONSTRUCTION GATE — do NOT use a flat 1e-12 (unachievable near folds). The identity K_a = K_hat_a + alpha_a*e^{-i*w*tau_a}*R with sum_a alpha_a = 1 is algebraic and exact, so the only error is roundoff relative to the LARGEST INTERMEDIATE, max_a|K_hat_a| ~ sqrt|mu_a|, which diverges at critical points. Gate on 100*eps*(|F_op| + sum_a|K_hat_a| + |R|) PLUS a frozen 3e-13 absolute backstop at the prototype's demonstrated configs. The looser global-tracker tolerance vs the cluster-local builders (WP5's 2e-14) is a REAL algorithmic property, not sloppiness — add a short code comment that POINTS AT FINDINGS F003 for the full explanation rather than duplicating it (FINDINGS.md is the authoritative registry; WP6 writes F003).

All float64 here. cogwheel unittest idiom per cogwheel/tests/test_lensing_dd.py.

STYLE GATE: pylint is NOT installed — substitute a programmatic 79-column + ast.parse check on every file you create or edit; do not block on pylint.
  Who: Coder
  Dependencies: WP1, WP3
  Verification: python -m unittest cogwheel.tests.test_lensing_channels -v — report the pass count. Gates: scale-aware reconstruction bound + frozen 3e-13 backstop on the prototype's elliptical path (17 points, shape (3,4), all converged) and on a kappa != 0 path; label continuity under small parameter steps (max|delta delays| < 0.03, max|delta slot_positions| < 0.15); assignment/reset equivalence — total agrees to 1e-13 and per-label K_a agree up to a permutation; label-permutation invariance of the total <= 1e-15 over all 4! permutations; deterministic tie-breaking in the brute-force assignment, incl. n<4 cases; K_a(w) over a 10-node w-grid <= 100 ms. Assert channels.py does not import the crossings test-support module. Programmatic 79-col + ast.parse check clean.
  Max turns: 110

### WP5: Fold/cusp crossing scenario builders (test-support) and label-continuity tests
  What: Port the prototype's topology_stable fold/cusp crossing-scenario builders — fixture generators whose sole job is constructing controlled fold and cusp crossings — and the label-continuity tests they support (the brief's 'label continuity across fold+cusp paths').
  Where: NEW: cogwheel/tests/test_lensing_crossings.py plus, if a separate support module is warranted, a test-support module under cogwheel/tests/ (NOT under cogwheel/lensing/). READ-ONLY: .claude/spec/lensing_paper/code/chang_refsdal_topology_stable.py (~431 lines, MINUS _operator_total and _validate_w which WP3 absorbs), .claude/spec/lensing_paper/code/test_topology_stable.py.
  How: HEADLESS DISCIPLINE (program-scoped — verbatim): This build runs unattended: no human reads or answers questions mid-task. The previous run failed exactly this way — coders did excellent read-only analysis, raised good questions, ended with "let me know how to proceed", and the pipeline moved on recording zero changes. Therefore, for THIS program's builds: never end a work package waiting for direction. Resolve ambiguities and task-text errors yourself with the most defensible choice, record each under a DECISIONS line in the change report, and complete the files. A coder WP that changes no files is a FAILED WP unless it ends with 'BLOCKED: <reason>'.

SCOPE DECISION (already made — implement it, do not relitigate): build_fold_crossing_partition and build_cusp_crossing_partition take (theta_c, eta_s, eta_h) offsets to construct a controlled crossing. NOTHING in the production path calls them; only tests do. They are TEST-SUPPORT code under cogwheel/tests/, NOT public library API — no crossings.py under cogwheel/lensing/, no public numpy-doc API surface. YAGNI: if Build 2 turns out to need them, promoting them then is cheap; shipping public API nobody calls is not.

INDEPENDENCE: this module depends only on WP1 geometry + WP3 operator + the committed _gauge.py primitives — NOT on WP4's channels.py. Assert that with a test.

KEEP the prototype's dataclass consolidation: TopologyStableGeometry (with the `kind` discriminator) + TopologyStablePartition — TWO classes, not four. Fold (2 persistent + 2 cluster images) and cusp (1 persistent + 3 cluster images) have genuinely DIFFERENT image-counting logic; they are not copy-paste twins. Do NOT collapse them into one function with a flag parameter (that is exactly the flag-parameter anti-pattern), and do NOT re-split into four classes.

Do NOT port chang_refsdal_exact_partition.py — superseded, imported by nothing (not even its own tests), and its interpolation functions are Build 2's job.

PORT the prototype's assertions from test_topology_stable.py — they encode the ground truth: fold exactness both sides (shapes (9,2)/(9,2)), cusp exactness both sides (shapes (9,1)/(9,3)), fold continuity (relative kernel jump < 2e-4), cusp continuity (< 1e-4), plus the seeded (rng 20260714) random quartic residual test. Keep the frozen 2e-14 backstop for these CLUSTER-LOCAL builders — unlike WP4's global tracker, 2e-14 IS achievable here by construction; add a short comment noting the contrast and pointing at FINDINGS F003 rather than restating it.

The prototype's test file has imports at lines 68 and 95, mid-file — it reads like three files concatenated. Do NOT reproduce that: imports at top, cogwheel unittest idiom (unittest.TestCase, <Thing>TestCase names, itertools.product sweeps with values interpolated into the assertion msg, self.subTest, `if __name__ == '__main__': main()`). Follow cogwheel/tests/test_lensing_dd.py.

STYLE GATE: pylint is NOT installed — substitute a programmatic 79-column + ast.parse check on every file you create or edit; do not block on pylint.
  Who: Coder
  Dependencies: WP1, WP3
  Verification: python -m unittest cogwheel.tests.test_lensing_crossings -v — report the pass count. Gates: fold and cusp exactness on BOTH sides at the frozen 2e-14 backstop with correct channel-count shapes (fold (9,2)/(9,2); cusp (9,1)/(9,3)) and all converged; fold continuity < 2e-4; cusp continuity < 1e-4; channel count stays fixed at 4 through the crossing; seeded random quartic residual test. Assert no import of channels.py. Programmatic 79-col + ast.parse check clean.
  Max turns: 90

### WP6: Closeout — exports, mpmath test extra, SPEC row rewrite, fragments, FINDINGS, docs
  What: Wire up the subpackage public API, fix the undeclared-mpmath packaging gap, and land the spec/docs/findings closeout the build owes. The commit gate enforces that new modules appear in SPEC.md.
  Where: EDIT: cogwheel/lensing/__init__.py, cogwheel/lensing/chang_refsdal/__init__.py, pyproject.toml, .claude/spec/FINDINGS.md, docs/source/overview.rst. NEW: .claude/spec/spec_changelog.d/2026-07-16_lensing-engine-complete.md. KEEP (do not delete): .claude/spec/todo.d/2026-07-16_lensing-program.md.
  How: HEADLESS DISCIPLINE (program-scoped — verbatim): This build runs unattended: no human reads or answers questions mid-task. The previous run failed exactly this way — coders did excellent read-only analysis, raised good questions, ended with "let me know how to proceed", and the pipeline moved on recording zero changes. Therefore, for THIS program's builds: never end a work package waiting for direction. Resolve ambiguities and task-text errors yourself with the most defensible choice, record each under a DECISIONS line in the change report, and complete the files. A coder WP that changes no files is a FAILED WP unless it ends with 'BLOCKED: <reason>'.

EXPORTS: follow the existing cogwheel idiom exactly — cogwheel/likelihood/__init__.py uses a one-line docstring then explicit `from .module import Name1, Name2`; neither it nor gw_prior defines __all__. cogwheel/__init__.py is VERSION-ONLY and imports no submodules — do NOT add a lensing import there. Re-export the Build 2 surface from chang_refsdal/__init__.py (currently just a one-line docstring): ChangRefsdalChannels plus the few geometry/operator entry points Build 2 needs. Keep _dd, _hyp1f1 and _gauge private (leading underscore, not re-exported).

PACKAGING GAP (real, pre-existing, verified: grep for mpmath in pyproject.toml returns NOTHING, yet cogwheel/tests/test_lensing_dd.py imports it at top level, so the suite fails to collect on a clean install — and this build adds three more mpmath-using test modules). Add mpmath to a TEST extra under [project.optional-dependencies] (which currently has only dev/full/docs), NOT to runtime dependencies — mpmath is oracle-only and must never be importable from a production path. numba and scipy are already declared.

SPEC.md — REWRITE, do not append. The Layers/modules table's lensing row currently reads 'Microlensing engine (IN PROGRESS — foundation only) ... Pending: fast complex-1F1 kernel, contour-free operator F_op, topology-stable ChangRefsdalChannels, geometry tests.' Replace it with the COMPLETED description: all modules listed (_dd.py, _gauge.py, geometry.py, _hyp1f1.py, operator.py, channels.py), and the limitations RECORDED rather than dropped — positive-parity macro images only (1-kappa > |gamma|, macro saddles out of scope), the certified w*Y domain and its ceiling, and that Builds 2-3 (LensedWaveformGenerator + likelihood; sampled lens coordinates) remain. Do this via a .claude/spec/spec_changelog.d fragment with frontmatter `bump: patch` (completing an existing layer, not adding one) and a body starting with a `### Title` heading — match the format of .claude/spec/contracts_changelog.d/2026-07-16_consumer-graph-drift-layer.md. Then run `python scripts/render_fragments.py` and verify `python scripts/render_fragments.py --check` exits 0. render_fragments.py writes derived versions back into SPEC.md — do not fight it, and do not hand-edit generated files.

DO NOT write a completed.d fragment for the program and DO NOT delete .claude/spec/todo.d/2026-07-16_lensing-program.md — the three-build program has Builds 2-3 pending. Update the todo fragment only to reflect that build (1) is done.

FINDINGS.md is currently empty ('no findings recorded yet') and is the AUTHORITATIVE registry for these facts — module comments point at the IDs, they do not duplicate the explanations. Use the documented ID/date/writeup format. F001 (cancellation law): L = w*(|y'| + gamma'/2) = w*dtau/2 splits into TWO INDEPENDENT channels at TWO SEPARATE code sites, L_1F1 = w*|y'| inside the 1F1 Maclaurin and L_op = w*gamma'/2 in the shear-series accumulation; dd is required only for the former; include the empirical validation (the prototype casts its mpmath G^(k) to complex128 and still measures 4.7e-16 over w in [5,40], where a both-channels-float64 pipeline would have given 6.6e-13); add the mantissa-not-exponent caveat (|G^(k)| ~ 1e92 at w=40, overflow at w ~ 700). F002 (tautology finding): TWO parts — (a) the switch S_j is NOT an accuracy short-circuit (the operator is called at EVERY w and S_j injects no error), which is why the explicit wave/geometric branch gate exists as the ONE real approximation, superseding the earlier W_cut R-truncation proposal (one knob, not two), with the |R| ~ (w*dtau)^-3 scaling and the physical argument that a fractional waveform error eps costs dlnL ~ SNR^2*eps^2/2 so 1e-6 is free at SNR 100 while the paper's 1e-12/1e-15 numbers verify an ALGEBRAIC IDENTITY not a physics requirement; and (b) the prefactor-test tautology: |C(w)|^2 must never be asserted against the closed form the production code itself uses — the gate is an independent >= 60 dps mpmath evaluation of the definition. F003: reconstruction error scales with sum_a|K_hat_a| ~ sqrt|mu_a|, not |F_op|, so a flat relative gate is unachievable near folds — hence the scale-aware gate, and why cluster-local (2e-14) beats global (3e-13). F004 — CORRECTED FACT, write it right: quartic_geometry_validation.csv is 120 kind='general' + 24 kind='fold' + 24 kind='cusp' (an earlier plan document's claim that all 168 rows are 'general' is FALSE and is superseded here); n_multistart is a frozen oracle from a deleted multistart solver — assert counts against it, never matched_position_error; record the measured fresh-residual behaviour vs find_images_quartic's residual_tolerance=3e-8 default that WP1 reports.

docs/source/overview.rst: ONE short paragraph on the lens engine — what it computes and its domain limits. Do not write a tutorial.

NO DATA_CONTRACTS.yaml CHANGE EXPECTED: this build creates no on-disk data products. If WP2/WP3 ended up caching any table to disk, register it in DATA_CONTRACTS.yaml plus a contracts_changelog.d fragment; otherwise leave both untouched.

STYLE GATE: pylint is NOT installed — substitute a programmatic 79-column + ast.parse check on every file you create or edit; do not block on pylint.
  Who: Foreman-Lite
  Dependencies: WP1, WP2, WP3, WP4, WP5
  Verification: python -c 'from cogwheel.lensing.chang_refsdal import ChangRefsdalChannels' succeeds. python -m unittest discover cogwheel/tests -v — FULL suite green, no regressions; report the total pass count. python scripts/render_fragments.py --check exits 0. git diff confirms SPEC.md/TODO.md changed only via render_fragments.py, and that the lensing SPEC row no longer says 'IN PROGRESS — foundation only'. grep -r mpmath cogwheel/lensing/ returns nothing; grep mpmath pyproject.toml shows it under a test extra only. Programmatic 79-col + ast.parse check clean on the subpackage.
  Max turns: 60

### Domain Test Descriptions
  - QUARTIC CSV REGRESSION (WP1). Setup: load all 168 rows of .claude/spec/lensing_paper/data/quartic_geometry_validation.csv (index, kind, gamma, beta, y1, y2, n_quartic, n_multistart, matched_position_error, max_quartic_residual) — the fixture is 120 kind='general' + 24 kind='fold' + 24 kind='cusp' (rows 120-167; VERIFIED by regex, contrary to an earlier plan document's 'all general' claim); build A = macro_matrix(gamma, beta, kappa=0) per row. Operation: run find_images(y, A). Expected: image count == row.n_multistart for every row (a frozen oracle from a multistart solver that no longer exists — independent and non-drifting, so it cannot converge toward the quartic's own bugs); n_quartic == n_multistart within the CSV as a fixture-integrity check; a FRESH gate max_a ||lens_residual(x_a, y, A)|| <= 1e-12, MEASURED on all 168 rows (find_images_quartic's residual_tolerance default is 3e-8, so whether the tighter gate holds is empirical — document any principled near-caustic exception as a finding rather than loosening the gate globally). Do NOT assert matched_position_error (cross-method, unrecomputable without the deleted solver) and do NOT reproduce row.max_quartic_residual (polish-detail dependent). Diagnostic: scatter of residual vs |y| colored by image count with the astroid caustic overlaid, fold/cusp rows marked.
  - NEAR-CAUSTIC STABILITY ON DELAYS AND RESIDUALS, NOT POSITIONS (WP1). Setup: the CSV's 24 fold and 24 cusp rows (real near-caustic fixtures) plus a controlled approach path to a fold. Operation: track lens_residual, tau_a and mu_a as the double root forms. Expected: residual meets the fresh gate and tau_a retains ~eps accuracy even though positions degrade to sqrt(eps) ~ 1.5e-8 (delays are quadratically insensitive because images are stationary points of the Fermat potential); mu_a = 1/det J is ill-conditioned by construction — assert its SCALING, not a tight absolute value. A test demanding 1e-12 on near-fold positions asserts something false and must not be written. Diagnostic: log-log position error, delay error and |mu| vs distance to the caustic — position error should go as d^-1/2 while delay error stays flat.
  - EXACT y=0 GEOMETRY (WP1). Setup: gamma swept 0.05..0.4, kappa=0, source exactly at the origin. Operation: find_images, delay, hessian, Morse indices. Expected: exactly 4 images at |x|^2 = 1/(1-gamma) (shear axis) and |x|^2 = 1/(1+gamma) (transverse); tau = 1/2 + (1/2)*ln(1 -/+ gamma); Delta tau = artanh(gamma); agreement ~1e-14. This is the axial/degenerate-branch coverage and exercises quartic, delays and the 4-image branch at once. Diagnostic: |numeric tau - analytic tau| vs gamma on a log axis — a slope change flags the axial branch cutting in at the wrong threshold.
  - MORSE-INDEX CENSUS (WP1). Setup: sample sources inside and outside the astroid caustic across a gamma sweep. Operation: classify each image by the signs of its Hessian eigenvalues. Expected: 4-image case has exactly TWO MINIMA and TWO SADDLES and NO maximum (n_a = 0,0,1,1); 2-image case has n_a = 0,1. CORRECTED 2026-07-16 — the earlier '0,1,1,2' (one min / two saddles / one max) was WRONG and must not be reinstated: a point mass has -ln|x| -> +inf at the origin, so tau has no local maximum, and n_max = 0 in every regime. MEASURED against the committed geometry.py at y=0 and for general y inside the astroid over gamma in {0.05..0.4}: census is [0,0,1,1] everywhere (2-image: [0,1]). NOTE the invariant n_min - n_saddle + n_max = 0 holds for BOTH 0,0,1,1 and 0,1,1,2, so it CANNOT discriminate — MEASURE the census, never assert it from task text; independently, image count is 4 iff y is inside the astroid and 2 outside (free, needs no CSV, catches a wrong caustic). Diagnostic: caustic-plane map colored by (n_min, n_saddle, n_max) — misclassification shows as speckle along the caustic.
  - DOMAIN GUARD (WP1). Setup: (kappa, gamma) pairs violating 1 - kappa > |gamma|, including the boundary. Operation: call macro_matrix. Expected: raises LensDomainError (the named module exception, not bare ValueError) with a message naming the offending (kappa, gamma) and stating that macro saddles / Type II images are out of scope; valid pairs do not raise. Diagnostic: table of (kappa, gamma) vs raised/not, with the 1-kappa=|gamma| line marked.
  - PREFACTOR IDENTITY — NON-TAUTOLOGICAL (WP2). Setup: w over [1e-3, 500]; oracle = mpmath at >= 60 dps evaluating the DEFINITION |e^{pi w/4 + (iw/2) ln(w/2)} Gamma(1 - iw/2)|^2 (test-time only). Operation: compare the production |C(w)|^2, which is computed from the overflow-free closed form pi*w/(1 - e^{-pi*w}) with expm1 on the small-w limb. Expected: rtol 1e-14, FLAT in w. Do NOT assert the closed form against itself — that proves nothing about the production path. Also check the limits |C|^2 -> 1 as w->0 (confirming the F->1 normalization convention) and -> pi*w as w->inf, and that the naive exp(pi*w/4)*Gamma(...) evaluation would overflow where the production path does not. Diagnostic: ratio of production to mpmath vs w on a log axis — a constant offset is a convention error; drift at large w is the loggamma cancellation.
  - 1F1 K-LADDER vs mpmath ORACLE + CANCELLATION-LAW FIT (WP2). Setup: grid over (w, s) spanning the certified domain including w*Y up to the dd ceiling; mpmath at 60-70 dps as oracle (test-time only). Operation: evaluate the dd-backed kernel and the shared-numerator k-ladder for k = 0..max_order; fit the measured error against the predicted eps*e^{w*Y} law. Expected: relative error <= 1e-10 (comfortably better inside the float64-safe region); the fit sits BELOW the model contour everywhere (the model is deliberately conservative by ~1 order). Diagnostic: heatmap of log10(rel err) over (w, Y) with the eps*e^{w*Y} contour overlaid — a knee at L ~ 13 would prove dd is not actually engaged in the Maclaurin.
  - LADDER COMPLEXITY — SUBSTANTIATED CLAIMS ONLY (WP2). Setup: instrument the kernel to count (a) shared-numerator evaluations and (b) dd multiplies; sweep max_order. Operation: evaluate the ladder at each max_order. Expected: (a) the shared-numerator evaluation count is INDEPENDENT of max_order (this is what the k-independent a' = iw/2 Kummer reparametrization buys); (b) the total dd-multiply count is LINEAR in max_order — fit and reject a quadratic. Do not assert an unsubstantiated big-O claim. Diagnostic: counts vs max_order with linear and quadratic fits overlaid.
  - W-CEILING REFUSAL (WP2). Setup: w beyond the certified ceiling (dd extends the mantissa, not the exponent: |G^(k)| ~ 1e92 at w=40, k=84, and overflows at w ~ 700 with max_order=42). Operation: call the kernel. Expected: a clear NAMED error naming the offending w and the certified ceiling — never a silent inf/nan. Diagnostic: |G^(k)| vs w on a log axis with the float64 overflow line and the refusal threshold marked.
  - F_op vs mpmath ORACLE (WP3). Setup: the paper's 4 configs plus stress cases spanning 2-image (incl. Y ~ 0.5-1.25 where float64 alone provably fails), 4-image, near-fold and near-cusp; mpmath oracle. Operation: evaluate F_op. Expected: relative error <= 1e-10 for L <= 48. Diagnostic: log10(rel err) vs L_est = w*(|y'| + gamma'/2) — points must fall below the eps_working*e^{L_est} line.
  - GEOMETRIC-OPTICS SLOPE TEST (WP3) — the highest-value test in the build, and self-oracling (no mpmath). Setup: fixed lens config well away from caustics; w over a decade. Operation: form R(w) = F_op(w) - sum_a sqrt|mu_a| * exp(-i*pi*n_a/2) * exp(i*w*tau_a), first WITHOUT and then WITH the C1, C2 corrections; fit the power-law slope of |R| vs w. Expected: fitted exponent in [0.9, 1.1] without C1/C2 and in [2.8, 3.2] with them. Simultaneously validates operator normalization, geometry, signed magnifications, Morse indices AND the C1/C2 polynomials — a wrong Morse phase or a factor of 2 in C1 changes the SLOPE, which no single-w tolerance test catches. Diagnostic: log-log |R| vs w with the fitted line and the w^-1 / w^-3 references.
  - MASS-SHEET IDENTITY, NON-VACUOUS FORM (WP3). Setup: the prototype's config (w=13.0, y=[.17,-.08], gamma=.12, beta=.31, kappa=.27) plus a broader kappa sweep. Operation: (a) compare F_{kappa,gamma}(w, y; beta) against (1/lam)*exp[i*w*(ln(lam)/2 - kappa*|y|^2/(2*lam))]*F_{0,gamma/lam}(w, y/sqrt(lam)), lam = 1-kappa; (b) STRONGER — check that the OBSERVABLES Delta tau_ac and the flux ratios |K_a/K_c| are EXACTLY kappa-invariant (kappa maps only into apparent distance plus a constant time shift). Expected: (a) ~2e-12 tightening toward 1e-13; (b) exact invariance. Form (b) is what Build 2's sampled coordinates actually rest on — test it, not just F. Diagnostic: Delta tau_ac and |K_a/K_c| vs kappa — must be flat lines; any slope is a broken degeneracy that would corrupt Build 3's sampling.
  - gamma -> 0 POINT-MASS REDUCTION (WP3). Setup: gamma = 1e-8, kappa = 0. Operation: compare F_op against the analytic point-mass C(w)*1F1(1 - i*w/2; 1; -i*w*s/2). Expected: agreement to 1e-12, with the deviation going as O(gamma^2) on a log-log sweep. L_op -> 0 here, so this cleanly isolates the operator-series normalization and sign from the 1F1 precision question. Diagnostic: |F_op - F_PM| vs gamma log-log — an O(gamma) or O(1) floor is a normalization or sign bug.
  - CANCELLATION-EXPONENT GATE AND RUNTIME REFUSAL (WP3). Setup: scan (w, Y, gamma') including points beyond the certified ceiling. Operation: for each point compute the measured relative error vs oracle AND the reported diagnostics (predicted L_est and the MEASURED max_partial_term/|total|). Expected: measured_rel_err <= 20 * eps_working * exp(L_est); and where the MEASURED cancellation exceeds ~1e13 the code RAISES a named error rather than silently returning garbage. Measured, not predicted — that is what makes the precision claim falsifiable. Diagnostic: measured error vs predicted bound, scatter with the y=x line.
  - BRANCH-GATE BEHAVIOUR — ONE GATE, ONE OWNER (WP3/WP4). Setup: configs straddling the gate condition (geometric when w*delta_min >= rho1 AND L > L_max=48). Operation: query the reported `branch` from operator.py and from ChangRefsdalChannels for the same params. Expected: the two agree exactly (channels consumes operator's gate and does not reimplement it); the wave branch is selected wherever either clause fails; the transition is documented as an approximation while the smooth switch S_j (rho0=0.5, rho1=4) injects no error. Diagnostic: (w*delta_min, L) plane colored by branch with the two clause boundaries drawn.
  - EXACT RECONSTRUCTION, SCALE-AWARE (WP4). Setup: the prototype's closed elliptical path (17 points) crossing fold and cusp regions, w grid, plus a kappa != 0 path. Operation: evaluate ChangRefsdalChannels; form sum_a exp(i*w*tau_a)*K_a and compare to F_op. Expected: |sum_a exp(i*w*tau_a)*K_a - F_op| <= 100*eps*(|F_op| + sum_a|K_hat_a| + |R|), PLUS a frozen 3e-13 absolute backstop at the prototype's demonstrated configs, all channels converged, shape (3,4). Do NOT impose a flat 1e-12: the identity is algebraic and exact, so the only error is roundoff relative to the largest INTERMEDIATE (max_a|K_hat_a| ~ sqrt|mu_a|), which diverges at critical points. Diagnostic: reconstruction error and the scale-aware bound plotted along the path with caustic crossings marked — the error should track the bound's excursions at the crossings, not exceed it.
  - ASSIGNMENT / RESET EQUIVALENCE — PATH-INDEPENDENCE OF LABELS (WP4). Setup: one far parameter point reachable two ways. Operation: evaluate it (a) via the deterministic reset convention and (b) by continuation along a path from the fiducial point. Expected: the TOTAL agrees to 1e-13 (it must — the total is label-invariant) and the per-label K_a agree UP TO A PERMUTATION. Inference-critical: if labels depend on evaluation history then the likelihood is not a function of theta, the posterior is not defined, detailed balance breaks SILENTLY and the sampler returns a plausible-looking wrong answer — the failure mode most likely to survive undetected into Build 3. Diagnostic: per-label K_a from both routes on the complex plane with the matching permutation annotated.
  - LABEL-PERMUTATION INVARIANCE OF THE TOTAL (WP4). Setup: any valid lens config, w grid. Operation: shuffle the (tau_a, K_a) label ordering and re-form sum_a exp(i*w*tau_a)*K_a. Expected: invariant to <= 1e-15 over all 4! permutations. Cheap, and it converts 'document the invariance' into an enforced property. Diagnostic: max deviation over all permutations vs w.
  - DETERMINISTIC ASSIGNMENT TIE-BREAKING (WP4). Setup: marker-assignment subproblems arising along the prototype's test path, including 2-image (n<4) cases, and a synthetic equal-cost tie. Operation: run the brute-force itertools.permutations(range(4), n) matching repeatedly, including on the tie. Expected: the same assignment every time — ties broken by a documented deterministic rule, never by dict/iteration order. Note: the assignment is bounded at n<=4 (<= 24 permutations), so no Hungarian-algorithm swap is warranted and no solver-equivalence test is needed. Diagnostic: assignment cost and chosen permutation along the path, plus repeated runs on the tie case.
  - LABEL CONTINUITY ACROSS FOLD AND CUSP (WP5). Setup: the prototype's configs — w = [5,10,20,40], gamma=0.2, theta_c=4.0, eta_s in {-1e-5, 0, +1e-5} for the fold; theta_c=pi, eta_h in {-1e-5, 0, +1e-5} for the cusp. Operation: build the crossing partition on each side of and exactly at the crossing; measure the relative kernel jump between adjacent eta values. Expected: fold jump < 2e-4, cusp jump < 1e-4; channel count stays fixed at 4 through the crossing (fold shapes (9,2)/(9,2); cusp (9,1)/(9,3)); exactness holds at the frozen 2e-14 cluster-local backstop on BOTH sides. Diagnostic: Re/Im K_a vs eta through zero, one line per label — a discontinuity at eta=0 is a label swap; smooth-but-crossing lines are fine (the total is permutation invariant).
  - PERFORMANCE SANITY / NO-mpmath GUARD (WP3, WP4). Setup: warm process, typical lens params. Operation: time a single F_op evaluation at w=20 and a kernels(w) evaluation over a 10-node w-grid. Expected: F_op <= 10 ms; 10-node K_a grid <= 100 ms. Bounds are deliberately generous — they exist to catch accidental mpmath in the hot path (the prototype's known weakness, ~1e6x too slow), not to police micro-optimization. Pair with a static guard asserting grep -r mpmath cogwheel/lensing/ finds nothing. Diagnostic: per-call timing histogram — an mpmath regression shows as a multi-second outlier, not a slow tail.

Domain tests: yes
New public API: yes
Spec update: yes
Files affected: 17