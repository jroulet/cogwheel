---
section: Backlog
---

- **THE DIFFRACTIVE BOTTOM MUST SERVE ANALYTICALLY — engine fall-through
  below the far-field w-floor is UNACCEPTABLE** `[→ spec]` — owner
  directive 2026-08-17, on being shown the far-exterior serving ladder:
  the below-`farfield_w_floor` band currently REFUSES at the serve site
  (the F070/8dfb8ca fix — correct to refuse rather than clamp a
  divergent kernel-sum envelope) and falls through to the exact engine.
  The physics is the OPPOSITE of hard there: as w → 0 the amplification
  tends smoothly to the unlensed limit F → 1, and the long-wavelength
  (single-scattering / Born-series-in-the-potential) expansion gives the
  leading corrections in powers of w with coefficients that are pure
  geometry (moments of the Fermat delay over the image/potential
  structure). DESIGN: a low-w analytic rung, BOTH parities, admitted by
  its own truncation certificate (the leading omitted term ≤ bar — the
  c3-certificate pattern at the opposite end of the band), serving
  w below a per-draw analytic ceiling w_low(draw) with NO measured
  constants. ONE RUNG, THREE CLOSURES: (1) the astroid far-exterior
  diffractive sliver (this directive's trigger); (2) the deltoid
  below-w_split table need (the c3-served saddle exteriors' engine-
  hosted bands, w_split medians 3.3–6.2, max 21.5 — if w_low covers
  w_split the deltoid far-field needs ZERO charts and the coordinate
  redesign descopes, pending the lobe-gauge probe); (3) every c3/Born
  band-split's below-split engine hosting shrinks to
  [w_low, w_split]. Mirrors: serve_route_census route taxonomy gains
  the rung (per the mirror-fidelity law), tiling census w-band floors
  rise to w_low. Sequence: BEFORE the demand-sized tiling design (it
  changes every region's w-band floor); candidate pairing with the 2b
  arm-extension build (both are analytic-rung work). Cost note: this
  rung deletes engine demand at the band bottom where every prior draw
  has nodes (20 Hz floor), so its demand-map impact may exceed its
  band width.
