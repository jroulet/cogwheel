# Completed

Archive of finished work items. This file is **generated** from fragments in
`.claude/spec/completed.d/` — do not edit it directly. When a TODO item is done,
delete its `todo.d/` fragment and create `.claude/spec/completed.d/<date>_<slug>.md`
(frontmatter: `date`, `section`), then run `python scripts/render_fragments.py`.

---

_(nothing completed yet)_
