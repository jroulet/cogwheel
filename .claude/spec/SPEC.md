---
spec_version: 0.23.0
last_updated: 2026-07-28
---

# cogwheel — Project Specification

## Mission

cogwheel (PyPI/conda package `cogwheel-pe`, import name `cogwheel`) is a scientific
Python library for **Bayesian parameter estimation of gravitational-wave sources**
from compact binary coalescences (black-hole / neutron-star mergers). Given
conditioned detector strain data, it infers a posterior over source parameters
(component masses, spins, tidal deformabilities, sky location, distance,
orientation, coalescence time/phase).

Three signature contributions:
1. **Sampled vs standard coordinates** — a custom sampling coordinate system that
   separates reparameterized "sampled" parameters from physical "standard"
   parameters to reduce correlations.
2. **Folding** — an algorithm that reduces posterior multimodality by sampling
   folded dimensions over half their range and summing reflected images.
3. **Relative binning (heterodyning) + marginalization** — a fast likelihood that
   interpolates the ratio of a trial waveform to a reference over coarse frequency
   bins (generalized to higher modes), plus analytic/semi-analytic marginalization
   over distance and over all extrinsic parameters (the "coherent score").

## Architecture

cogwheel is a layered scientific package. `cogwheel/__init__.py` sets version
metadata only; functionality lives in submodules.

### Data flow

```
EventData (strain + ASD)  ->  WaveformGenerator  ->  Likelihood
                                                        |
                  Prior (sampled<->standard) ----------> Posterior (folding) -> Sampler -> samples.feather -> postprocessing
```

### Layers / modules

| Layer | Purpose | Key modules |
|-------|---------|-------------|
| Data acquisition & conditioning | Download public GW strain (GWOSC/GWpy), condition it, store ASDs and event metadata, build `EventData` (incl. Gaussian-noise and injected-signal events). | `cogwheel/data.py`, `cogwheel/data/events_metadata.csv`, `cogwheel/data/example_asds/` |
| Waveform generation | Frequency-domain waveforms via LALSimulation; project onto detectors; register approximants. | `cogwheel/waveform.py`, `cogwheel/waveform_models/__init__.py`, `cogwheel/waveform_models/xode.py` |
| Likelihood | CBC likelihood, fast relative-binning likelihood (with higher modes), marginalized variants. | `cogwheel/likelihood/likelihood.py`, `relative_binning.py`, `marginalized_distance.py`, `marginalized_distance_phase.py`, `marginalized_extrinsic.py`, `marginalized_extrinsic_qas.py`, `reference_waveform_finder.py` |
| Extrinsic marginalization (coherent score) | Marginalize over extrinsic params from matched-filter timeseries; sky dictionary + lookup tables; numba-accelerated. | `cogwheel/likelihood/marginalization/base.py`, `coherent_score_hm.py`, `coherent_score_qas.py`, `skydict.py`, `lookup_table.py` |
| Priors & coordinates | Abstract `Prior` base with sampled<->standard transforms; composable subpriors; concrete GW priors; PN-inspired QMC proposals. | `cogwheel/prior.py`, `cogwheel/prior_ratio.py`, `cogwheel/pn_coordinates.py`, `cogwheel/gw_prior/combined.py`, `mass.py`, `spin.py`, `extrinsic.py`, `tides.py`, `pn.py`, `twosquircle.py`, `miscellaneous.py` |
| Posterior & sampling | Combine prior + likelihood into `Posterior` (with folding); find reference solution; sample via dynesty/nautilus/zeus/PyMultiNest. | `cogwheel/posterior.py`, `cogwheel/sampling.py`, `cogwheel/postprocessing.py` |
| Utilities & physics | JSON (de)serialization mixin, caching, detector geometry/response, sky-loc angles, cosmology. | `cogwheel/utils.py`, `cogwheel/gw_utils.py`, `cogwheel/skyloc_angles.py`, `cogwheel/cosmology.py` |
| Plotting | Corner plots with GW-specific LaTeX labels. | `cogwheel/plotting.py`, `cogwheel/gw_plotting.py` |
| Validation | End-to-end injection-recovery pipeline (PP-plots / coverage). | `cogwheel/validation/generate_injections.py`, `inference.py`, `analyze.py`, `injection_prior.py`, `example/config.py` |
| Microlensing engine (Chang–Refsdal wave optics) | Complete wave-optics amplification engine for microlensed-PE: double-double arithmetic substrate (`_dd.py`), exact gauge/cluster-split channel algebra (`_gauge.py`), image geometry (`geometry.py` — quartic solver, delays, magnifications, stationary-phase kernels), dd-accumulated complex-1F1 kernel (`_hyp1f1.py`), contour-free operator `F_op` (`operator.py`), and topology-stable `ChangRefsdalChannels` (`channels.py`, the public entry point). Limitations: positive-parity macro images only (`1-kappa > \|gamma\|`; macro saddles out of scope); certified frequency ceiling `w <= 500`; double-double product ceiling `w*sqrt(s) <= 60`; geometric branch taken only above `w*delta_min >= 4.0` and `L > 48`; wave-branch contraction oracle-certified at 1e-10 only to `L ~ 25-30`, and above it the overflow-safe contraction raises a named `CancellationError` (never a silent `nan` or finite-but-wrong number) so the band `L in [~30, 48]` is certified-or-refused rather than silently wrong — the accuracy extension remains open (FINDINGS F005, NARROWED). Batched fast path (Build 3c): `operator.F_op_grid(w_array, ...)` evaluates the whole wave-branch node grid in one call via the per-order weight-vector contraction — the w-independent monomial/table weights are scatter-added once per evaluation and each node reduces to a length-`dim` dot product, replacing the per-node 85x85 bilinear form at byte-unchanged refusal thresholds; scalar `F_op` delegates to it (single contraction path, single certification), and `channels._exact_total` calls it once per wave-branch node subset. Certified by `cogwheel/tests/test_lensing_batched_operator.py`: 70-dps-mpmath-oracle accuracy at 1e-10 across the F005 band, single-vs-batch refusal-decision identity with zero flips, and F010-style py_func-chain self-falsification. NEGATIVE-PARITY (macro-saddle) branch (Build 6): the geometry layer accepts saddles (`0 < 1-kappa < \|gamma\|` — two 3-cusp deltoid caustic lobes via the +- branch of the astroid formula; census {1,1}/{0,1,1,1}, index sum -2), `lam <= 0` (Type III) and the det-A=0 parity boundary are named `LensDomainError` refusals; the saddle wave branch is the NEW exact 1D Schwinger-parameter dd quadrature (`_schwinger.py`, `f_schwinger`, paired N-vs-2N certification, `SchwingerCertificationError`, ceiling `w <= 60`, single y-independent cancellation channel `L_S = pi*w/4`); the geometric branch serves resolved saddles unmodified. HOMOGENIZATION (Build 8d): the Schwinger quadrature is THE exact wave evaluator on BOTH parities — the positive-parity arm of `F_op`/`F_op_grid` routes every sheared host (`gamma' > 0`, any `w`) through the SAME reduce/rotate/reconstruct as the saddle arm (`lam = 1 - kappa`); the legacy dd/1F1 operator-series contraction is DEMOTED to the shear-free `gamma' == 0` point-lens exit (measure-zero in the prior; its `CancellationError`/F005 refusal contract unchanged there) and to test-only oracle duty (`operator.legacy_operator_oracle`). Positive-parity `gamma' > 0` refusals are `SchwingerCertificationError`, including UNCONDITIONALLY every `w > 60`: the two retired-vs-serving evaluators' ceilings live in DIFFERENT variables (legacy: product `w*sqrt(s) <= 60`; Schwinger: frequency alone — F019), so the `w > 60` non-geometric corner (~25% of prior draws carry such nodes, measured upper bound, `scripts/census_homogenization_corners.py`) refuses by name until the Build-8e uniform-asymptotics build serves it — accepted interim state, sampling parked. Positive-parity wave values changed at the ~1e-14 level (witnessed contract flips, physics tolerances unchanged); the exact path is re-priced (~90 ms/node, crown lnlike ~751 ms — the surrogate is the production speed layer) and brute-force accuracy tests are gated behind `COGWHEEL_BRUTE_ACCURACY` (driver post-build tier, `.claude/sdk/post_build_sweeps.sh`). UNIFORM-ASYMPTOTIC SERVING (Build 8e): the serving ladder per node is surrogate (in box) -> geometric (resolved, `select_branch`/`L_MAX` UNCHANGED) -> uniform arms (certified) -> Schwinger exact (`w <= 60`) -> the existing named refusal — the uniform arms fire ONLY at the previously-refusing sites (previously-geometric and Schwinger-certified nodes byte-identical, tested via the sys.modules HEAD-side-by-side idiom). `chang_refsdal/_airy_fold.py`: the fold (two-coalescing-image) uniform Airy evaluator — `F = 2*sqrt(pi)*exp(i*w*tau_bar + i*sigma)*[p*w^{1/6}*Ai(-xi) - i*q*w^{-1/6}*Ai'(-xi)]`, `xi = (3*w*Delta_tau/4)^{2/3}`, amplitudes from the FINITE fold-frame curvatures (hard-axis eigenvalue + soft-axis cubic; never the divergent per-image `sqrt|mu|`), prefactor/phase pinned by the closed-form `xi -> inf` match to the exact geometric two-image sum, finite on-caustic (`Ai(0)`), self-certified via the `O(xi^{-3/2})` leading-error estimate against the F016 envelope bar, returns None (fall-through) when uncertifiable — NO new exception class. `chang_refsdal/_pearcey_cusp.py`: the cusp (three-coalescing-image) uniform Pearcey evaluator — certified Pearcey primitive by Connor-Curtis rotated-contour quadrature (steepest-descent valleys, paired N-vs-2N Gauss-Legendre at the `_schwinger` 3e-10 tolerance, P certified BEFORE any prefactor), control arguments `x ~ w^{1/2}*delta_par`, `y ~ w^{3/4}*delta_perp` (semicubical 2/3 law), large-argument limit pinned to the geometric image sum, same None-fall-through contract. The 8c TubeChart cusp exclusion windows shrink to the complement of the arms' certified coverage (runtime only; chart npz schema and DATA_CONTRACTS untouched). The corner census reports fold/cusp argument CDFs and category fractions (geometric-now / geometric-relaxed-MEASURED-ONLY / uniform-resolvable / hard-core) with Wilson intervals. Above `w = 60` the arms are anchored by continuity from the certified band plus a sparse offline mpmath high-dps anchor (driver post-build sweep step; uniform error DECREASES with `w`). SERVING MICRO-LEVERS (Build 8f, all value-preserving): the geometry_partition residual and the likelihood moment contraction are profile-first optimized (committed benchmarks scripts/profile_geometry_partition.py + profile_likelihood_contraction.py; <= 1e-10 relative preservation vs HEAD, branch/mask decisions byte-identical, witnessed byte-pin flips at ~1e-13); the per-node Schwinger loops run through an njit prange PURE-MAP driver (bit-exact per node vs serial, refusal decisions scheduling-independent, any-node-refuses -> whole-grid-refuses, the named exception raised only in the Python wrapper; driver .py_func exposed for F010); the Pearcey primitive is served from a REGISTERED table artifact (pearcey_table: build-time-derived box + demodulated Re/Im bicubic splines, content-hash-verified load, live-quadrature fallback on box exit or ANY anomaly — never serves where wrong; regeneration scripts/train_pearcey_table.py); the geometric-served path gains runtime image-count-match and Morse parity-sum guards (named LensDomainError), with L_MAX = 48 UNCHANGED — it is the wave/geometric HANDOFF exponent (raising it pushes geometric-served nodes onto the degrading wave path; the earlier relax-toward-60 framing was wrong) — enforced by a double-sided bracket test (L_geo <= 48 <= L_wave + margin, mpmath oracle). NUMBA THREAD-LAYER INVARIANT (8f gate incident): numba's parallel layer initializes ONCE per process at the first prange/parallel-ufunc call with the env value AT THAT MOMENT — nothing may pin NUMBA_NUM_THREADS at import scope outside COGWHEEL_STRICT_TIMING, or later parallel callers hard-fail on the mismatch. Certified by the 8e test phase: sign/convention falsifiers (Ai(-xi) oscillation-vs-decay handoff ON the caustic; asymmetric-fixture p/q-swap catch; at-the-fold finiteness), Pearcey primitive certification incl. honest-certificate tracking and the `P(0,0)` closed form, scaling-swap exponent fits, both-directions fall-through with the F010 mutation idiom, serving-ladder determinism, and byte-identity of all previously-certified paths. Certified by `cogwheel/tests/test_lensing_saddle_geometry.py` and `test_lensing_schwinger.py` (independent AST-guarded mpmath oracle 9.1e-14..1.6e-11 across the band; deep-band F009-S magnitude + Morse-phase pins; FINDINGS F011-F013). INTERIM: the channel/waveform layer refuses saddles by name until the Build-7 saddle channel layer. ENGINE HARDENING (Build 7a): (1) a runtime index-theorem census guard at the end of `find_images_quartic` (the single image producer) converts the F012 near-axial dead zone into a named `LensDomainError` on both parities — invariant `sum (-1)^{n_a} = sign(det A) - 1` for NON-degenerate censuses, with a single Morse-theory-correct degenerate pass-through: a discrepancy is legitimate ONLY when the census carries a near-critical witness image (fold-merged pair, count 3, or on-cusp triple merge, count 2); any discrepancy over regular-only images — the F012 pair drop or any single-image loss — still refuses; (2) a cross-parity strong-shear fallback in `F_op`/`F_op_grid` routes positive-parity `CancellationError` refusals at `w <= 60` AND `gamma' > 0` through the Schwinger evaluator (same reduce/rotate/reconstruct as the saddle arm with `lam = 1-kappa`; oracle-certified at 1e-10 uniformly; the all-certified hot path and every certified output stay byte-identical; above `w = 60`, and for shear-free `gamma' = 0` refusals, the named refusal stands) — the practical strong-shear refusal onset for the wave branch now sits at the Schwinger ceiling; (3) an exactly-singular projected Fermat Hessian in the stationary-phase kernel raises a named `LensDomainError` instead of crashing with a raw `LinAlgError` (crash-class-scoped guard; near-singular metrics still flow to the F008/SACR-C switch by design — F015); (4) `LensedPosterior`'s refusal net covers the full named vocabulary (`LensDomainError`, `CancellationError`, `SchwingerCertificationError`, `LensedBinningError`), built at raise time so the F010 mutation falsification can still red it. SADDLE INTEGRATION (Build 7b): the interim refusals are LIFTED end to end — `ChangRefsdalChannels.evaluate` serves saddle hosts by delegating every saddle node to the batched operator call (whose saddle arm owns per-node geometric-vs-Schwinger routing; the positive-parity channel branch decision is byte-identical), `LensedWaveformGenerator` constructs on saddles (`macro_matrix` remains the domain gate: Type III `1-kappa <= 0` and exact `det A = 0` refuse by name), the sampled reduced-shear range is `gamma in (0, 1.6)` spanning BOTH parities (parity deterministic in gamma; the `gamma = 1` boundary is a measure-zero named refusal; the `u1/u2` quadrant fold stays valid on the deltoid — Fermat-potential reflection symmetry is parity-blind), and the envelope LOO stop is gamma'-keyed (`4e-3` below `gamma' = 0.5`, `1e-3` above — crown byte-identical; justified by the saddle envelope gate, NOT the rescued nat-gap, which is RB-binning-limited, F016). Certified by `cogwheel/tests/test_lensing_saddle_channels.py` (identity residual 1.8e-16; independent mpmath end-to-end oracle 5e-15; switch bound measured 1.32/1.52 vs ceilings 2/4; lobe-jump continuity; F009-S channel-layer macro plateau 1.4e-7) and `test_lensing_saddle_likelihood.py` (saddle RB-vs-brute; rescued-node gates; refusal-precedes-coherent-score spy; 1e4 both-parity prior round-trips; deltoid reflection 1e-14). SERVING-PATH LEVERS (Build 8b-levers, value-preserving): (1) `geometry.nearest_caustic_point` replaces the dense-scan + Brent search with an analytic Newton iteration on the stationarity condition of the squared distance (njit `_squared_distance_derivatives`/`_newton_caustic_cell`; ~32-node seeding, two best cells, per-lobe/branch wedge clamping, bounded-Brent fallback at cusps and discriminant clamps) — the caustic-point DISTANCE is certified value-preserving (worst 9.3e-12 rel over 5677 both-parity configs; HEAD pins at places=14; zero basin misses); the arc parameter theta is GAUGE and re-certified at 1e-10 (1e-8 at shallow minima, where Newton is MORE accurate than the old Brent — F017), gated in the arc-length currency theta_gap*caustic_speed vs an independent dense oracle (cusp-safe); timing 1.23 -> 0.095 ms (positive) / 4.54 -> 0.99 ms (saddle, residual = cusp-Brent fallbacks). (2) `operator._fused_contraction` merges the per-order weight-vector build and the grid contraction into one njit pass (dispatch-only refactor; `half_sum` stays an argument and `_SERIES_TOLERANCE` a patchable module global so the F010 falsifications keep a reachable red path, re-homed in `test_lensing_fast_path.py::OperatorFusionFalsificationTestCase`) — certified 0-BIT different across the certified sweep with exact refusal parity (`OperatorFusionByteIdentityTestCase`). | `cogwheel/lensing/chang_refsdal/channels.py` (public), `operator.py`, `_schwinger.py`, `_hyp1f1.py`, `geometry.py`, `_gauge.py`, `_dd.py` |
| Microlensed waveform & likelihood (Chang–Refsdal PE) | Apply the engine's amplification to CBC waveforms and evaluate a fast relative-binning likelihood for microlensed events. `LensedWaveformGenerator` composes an ordinary `WaveformGenerator` and multiplies every harmonic mode by the shared factor `F(w(f))`, with `w = 8*pi*G*M_L*(1+z_L)*f/c^3` dimensionless and linear in `f`; it exposes the per-image `(tau_a, K_a)` decomposition the likelihood consumes, not only the collapsed total. `LensedRelativeBinningLikelihood` (subclass of `BaseLinearFree`) heterodynes against an *unlensed* reference `par_dic_0` and reconstructs the lensed `(d\|h)`/`(h\|h)` from delay-continuous frequency-moment summaries contracted mode-then-image (additive `M^2 + n_img^2` cost, no FFTs on the hot path); image-delay phases stay analytic, smooth kernels are interpolated, and a lens-aware bin guard raises `LensedBinningError` if `pi*Delta_f_bin*delta_t_max` breaches tolerance. Positive-parity macro images only: `1-kappa > \|gamma\|` is enforced by raising `geometry.LensDomainError` at the API boundary (constructor and every strain/likelihood path), never a warning or `nan`; the wave branch is certified-or-named-refusal everywhere (WP1, F005 NARROWED). Fast path (Builds 3-3f, SACR-C): the channel construction is the switched-analytic + single-envelope decomposition — persistent resolved images carried by the ANALYTIC saddle kernel `geometry.image_kernel` under their own carriers with smootherstep weights `S_a(w) = smootherstep(w*\|tau_a - tau_c\|, 0.5, 4)` keyed on the CRITICALITY separation (`tau_c` = the parked critical-carrier delay from `nearest_caustic_point`; supersedes the F008 full-cluster switch keying — see the F008 addendum), plus ONE beat-free interpolated transition envelope `E(w) = e^{-i w tau_c}(F - sum_a S_a H_a e^{i w tau_a})` whose demodulated phase is bounded at 4 rad by construction. The envelope is engine-evaluated (`F_op_grid`) on a leave-one-out-adaptive coarse grid (`_LOO_SEED_NODES = 8`, stop `4e-3`, ceiling `_LOO_MAX_NODES = 48`, node count config-independent) and candidates are rebuilt by closed-form dense reconstruction (`reconstruct_from_envelope`); the dd/1F1 ladder and operator contraction are numba-njit (`fastmath=False`, refusal logic and thresholds untouched in Python), and the frequency-independent nearest-caustic search is njit-accelerated (value-preserving to rel `1e-10`, branch-invariant). Ratio layer (Build 3g): per-proposal cost is cut by heterodyning the candidate envelope against a memoized FIDUCIAL envelope — a pure function of the candidate (all five lens-geometry params snapped to a fixed lattice, `m_lens`/`z_lens` shared exactly so the `w` grid never regrids) — interpolating only the ultra-smooth ratio `rho = e^{i w dtau_c} E_cand/E_fid` (~8 LOO nodes, config-independent) with the critical-delay difference pulled out analytically; image-count and envelope-health guards (and any fiducial-side refusal) fall back to the certified direct path, while candidate-side refusals propagate unswallowed on ratio, direct, and brute paths alike. Certified by `cogwheel/tests/test_lensing_ratio_layer.py` (lattice-point identity, perturbed ratio-vs-direct, ratio-vs-brute at inherited tolerances, bit-identical cache determinism, refusal symmetry, deep-band macro limit; measured warm single-thread lnlike ~9.8 ms, ~143x over brute force). Certified by `cogwheel/tests/test_lensing_fast_path.py`: numba-vs-mpmath preservation, a null-safe production-grid interpolation gate (`max\|dF\|/max\|F\| < 1e-3`, worst regime two-image `4.2e-4`), RB-vs-brute agreement on every lens regime, and single-thread timing guards (warm lnlike ~0.3 s/eval, ~50x over brute force; the few-ms target is deferred to a 2D surrogate-table decision). | `cogwheel/lensing/waveform.py` (`LensedWaveformGenerator`), `cogwheel/lensing/likelihood.py` (`LensedRelativeBinningLikelihood`) |
| Microlensed sampling layer (priors, folding, posterior) | Sampled lens coordinates per the locked reduced parametrization: kappa/beta/z_lens ELIMINATED (`FixedLensGeometryPrior` — kappa is exactly mass-sheet degenerate and never sampled; z_lens folds into the sampled REDSHIFTED lens mass since only `w ∝ M_L(1+z_L) f` enters), `ln m_lens` log-uniform with certified-domain provenance, reduced shear `gamma` uniform on (0, 1.6) — a single continuous BOTH-PARITY range since Build 7b (parity deterministic in gamma; the `gamma = 1` det-A=0 boundary is a measure-zero named refusal), and the source position as a unit box `(u1, u2)` in the shear frame scaled by a mass-conditioned factor keeping `w*sqrt(s) <= 58` by construction; quadrant folding via `folded_reflected_params = ['u1','u2']` (valid on BOTH the astroid and the saddle deltoid — the fold is a reflection symmetry of the Fermat potential, parity-blind); NO phase-fold (the constant-lens-phase ~ orbital-phase degeneracy is 22-only and must not be assumed for XPHM). `LensedIASPrior` (registered, `default_likelihood_class = LensedRelativeBinningLikelihood`) composes these with the IAS CBC subpriors; `LensedPosterior` maps the full named-refusal vocabulary (`LensDomainError`, `CancellationError`, `SchwingerCertificationError`, `LensedBinningError`) to `lnL = -inf` at the POSTERIOR BOUNDARY ONLY (the engine/likelihood named-refusal contract is untouched); the in-memory fiducial cache is dropped on pickle so sampler workers rebuild deterministically. Certified by `cogwheel/tests/test_lensing_prior.py` (round-trips 1e-12, Jacobians, 1e4-draw both-parity domain safety, reflection/fold consistency, mass-sheet lnL invariance, refusal-net mutation check, XPHM no-phase-fold). SURROGATE SPEED LAYER (Build 8a, off by default): `cogwheel/lensing/surrogate.py` (`LensAmplificationSurrogate`) — an offline-trained tensor cubic-spline emulator of the SACR-C envelope over (log w, gamma, y1_eig, y2_eig), beta eliminated exactly by eigenframe rotation, real/imag interpolated separately; refusal-conservative domain gate (certified-box containment + exclusion balls around refused training points; the surrogate NEVER answers where the engine refuses); npz/pickle serialization with provenance. Consumed via `LensedRelativeBinningLikelihood(amplification_surrogate=...)` — a single intercept serving `channels.geometry_partition` + surrogate envelope + `reconstruct_from_envelope`; DEFAULT `None` keeps every path byte-identical (crown max|diff| = 0 pinned). Enabling-by-default requires the operator-deferred production gates: full-box trained artifact (registered in DATA_CONTRACTS + LOADERS on shipping), production-scale nat tiers, caustic-margin census, PP validation. Certified by `cogwheel/tests/test_lensing_surrogate.py` (crown byte-identity, refusal preservation + F010 mutation, beta-elimination 1e-12, F002 AST guard, budget-calibrated reconstruction gates with documented production-target provenance). GLOBAL MULTI-CHART ARTIFACT (Build 8c): the surrogate is a flat chart collection — near-caustic TUBE charts in caustic-adapted coordinates `(gamma, u = sqrt(eta), theta, log w)` (eta/theta from `nearest_caustic_point`; the caustic is the fixed plane eta = 0; u = sqrt(eta) absorbs the envelope's own fold branch; theta bounded, non-periodic, per fold arc, query-unwrapped into the chart frame by `_theta_into_frame` since `nearest_caustic_point` reports [0, 2pi) while wedge-frame deltoid arcs span negative theta) plus mass-stratified exterior FAR-FIELD charts per image-count region (Build 8g tiling — see TRAINING) plus, for the macro-saddle (`gamma > 1`), per-lobe interior `LobeInteriorChart`s (Build S2-2 admission + the lobe-frame serve wiring): the saddle caustic is two disjoint 3-cusp deltoid lobes sitting OFF the origin on the shear axis, so neither encloses the origin and the origin-centred caustic-fixed coordinate does not apply. Each lobe instead carries its OWN frame — source-plane deltoid centroid, the other lobe's centroid, the inter-lobe corridor half-width, and the directional boundary `(boundary_theta, boundary_r)` — and is charted in lobe-local `(rho_lobe, theta_local)` where `rho_lobe = |y - centroid| / r_deltoid(theta_local)` so `rho_lobe = 1` tracks the deltoid boundary in EVERY direction (a scalar reach overshoots the near-cusp directions of an elongated lobe and leaves its interior untileable). `r_deltoid` has ONE authoritative definition, `surrogate._lobe_boundary_radius`, shared by the coordinate maps and by `_SaddleLobeAdmission._r_deltoid`. Lobe charts store the interior SACR-C `tau_c`-demodulated label and carry `image_count = 4` derived from the engine's `real_mask.sum()` with a constancy assert (the origin-centred `rho < 1` region heuristic is structurally wrong off-origin). Serve-side `_lobe_serves` gates gamma box, log-w band, then the INTER-LOBE CORRIDOR (`|y - centroid| + corridor_half <= |y - other_centroid|`), then lobe-local box containment, exclusion balls, image-count and the eta floor; a source inside the corridor fails for BOTH lobes and falls through to the exact-engine ladder as a named refusal, so no admitted tile straddles the lobe-equidistance line and no source is ever served from the wrong lobe. Lobe artifacts carry their own axis-schema tag (`_LOBE_AXIS_SCHEMA`) and hard-refuse at load under any other tag. Certified by `cogwheel/tests/test_lensing_surrogate_lobe.py` (coordinate round-trip <= 1e-12 including the `+-pi` theta_local seam and near-cusp directions, single-source `r_deltoid` parity under helper mutation, corridor named-refusal and lobe exclusivity, served-vs-fresh-engine oracle with node-exactness, `image_count == 4` constancy, persistence round-trip with axis-schema refusal, and a history-free positive-parity golden-value regression); deterministic guard-stack `select_chart` keyed ONLY on certified (gamma, eta, image-count) — theta solely for cusp-window exclusion (F017) — with a |gamma-1| < 1e-3 guard band and tube-over-far-field priority; cusp neighborhoods are EXCLUDED (2/3-power singularity; served exact until the cusp fast-serving build) with (theta_cusp, delta_theta) windows and eta_floor recorded per chart; single-npz serialization with a JSON provenance scalar (incl. dropped_gamma_slivers); package-data default load path (`cogwheel/data/`) + explicit override; an 8a single-box npz loads as a one-chart special case. TRAINING (`cogwheel/lensing/surrogate_training.py` + `scripts/train_lens_surrogate.py`): prior box read from the prior classes; cusp locations derived from caustic-speed minima with a topology cross-check (4 astroid / 6 deltoid, loud `CausticTopologyError`); fold arcs probed at multiple interior thetas (near-axial F012 refusals cannot silently drop an arc); gamma bands bisected into TOPOLOGY-STABLE sub-bands (`band_caustic_structure` / `stable_gamma_bands`: edge+center detection, intersected theta bounds, merged conservative cusp windows; metamorphosis slivers below `min_gamma_band` are DROPPED refusal-conservatively and recorded); a runtime FOOT-OF-NORMAL guard skips a tube chart whose `eta_max` exceeds half the band's minimum caustic curvature radius (`_min_curvature_radius`, worst gamma in band) and records it loudly, since far-field charts plus the serving ladder cover the skipped region rather than the chart being trained wrongly. FAR-FIELD TILING (Build 8g): the far-field stage partitions each parity's REACHABLE lens-mass range (capped where `w(f_hi, m)` reaches the parity's engine ceiling — astroid `_POSITIVE_W_CEILING`, saddle `_SADDLE_W_CEILING`) into log mass strata of fixed ratio `R = sqrt(f_hi/f_lo)` (`_mass_strata`), each stratum's whole `[w(f_lo, m_lo), w(f_hi, m_hi)]` band bracketed by one chart w-range (`_stratum_w_range`, whole-band containment is the serving contract); mass above the reachable top is recorded loudly as `beyond_w_cap`, never silently dropped. Each stratum's shear-frame y-support box is tiled into a uniform Cartesian grid of square tiles (`_farfield_tiles`); tile admission is a PER-THETA_C-COLUMN test (`_InteriorAdmission.admits_exterior`, Build 8h-b4) that probes each tile's inner rho edge across its theta_c span against the exact nearest-caustic distance and admits only where every band gamma clears eta_max. This REPLACED the original single scalar exclusion disk of radius `caustic_reach + eta_max`: the astroid caustic is a directional SPIKE, not a disk (at gamma=0.9 the directional caustic radius runs ~0.9-1.5 over most angles but 5.69 at the cusp), so the scalar rule's disk radius blew up with the cusp spike rather than tracking the true boundary — above gamma~0.85 the exclusion disk exceeded the entire prior source box (`\|y\| <= 4.24`) and ZERO exterior tiles were built even though ~98% of that region is genuinely exterior (driver-measured coverage of the true `eta >= eta_max` region under the old rule: 0.944 at gamma 0.30-0.40, 0.632 at 0.50-0.70, 0.271 at 0.70-0.80, 0.000 at 0.80-0.90). `TrainingConfig.max_farfield_regions` defaults to `None` (uncapped — the tiling itself bounds the count); an int caps the total admitted tiles with a loud truncation record. CAUSTIC-FIXED RADIAL COORDINATE (`_to_caustic_fixed`/`_from_caustic_fixed`, used by chart classification and the exterior admission test above): on the astroid interior, the astroid exterior arm, and the saddle interior arm the coordinate is directional-MULTIPLICATIVE (`rho = \|y\| / _caustic_reach(gamma, theta_c)`, unchanged); on the SADDLE (parity -1, macro-saddle) EXTERIOR arm only, it is instead a scalar ADDITIVE offset (`rho = 1 + \|y\| - _caustic_reach`, giving `drho/d\|y\| = 1`, Build 8h-b4) — a directional r_caustic is ill-posed there because the saddle's two disjoint deltoid lobes do not enclose the origin, so rays that miss both lobes have no enclosing directional radius to normalize by; it therefore stays SCALAR by design, not directional. `_box_region_labels` additionally catches the named refusal errors around the box-centre coordinate map (Build 8h-b4) and returns `(None, None)` — a chart whose box centre sits exactly on the `gamma = 1` parity wall records unknown image_count/parity instead of crashing chart construction. REGISTRATION GATE (Build 8g): each chart's held-out max-normalized envelope eps (`_heldout_eps`) is checked against a per-kind bar (`TrainingConfig.tube_eps_max = 5e-2`, `farfield_eps_max = 3e-3`); a chart above its bar, or with NaN eps (zero held-out points served), is recorded GATED in the training report and excluded from the packed artifact — its window falls through to the serving ladder rather than being silently registered; the gate decision round-trips through per-chart provenance on resume (`heldout_eps` persisted alongside the saved chart and read back rather than recomputed; charts from a pre-8g trainer carry a `legacy_no_eps` marker and pass through un-gated so a mixed-version resume never silently drops a previously-registered chart). Per-chart-file resumability; JSON training report with measured artifact size. CENSUS (`cogwheel/lensing/surrogate_census.py` + `scripts/census_lens_surrogate.py`): served fraction with a 6-way MECE fall-through breakdown (gamma-guard / dropped-sliver / born / cusp-window / refusal-ball / out-of-box; near-cusp neighboring-arc projections categorize as out-of-box BY DESIGN), per-chart held-out envelope eps in the max-normalized currency (F016) against a fresh-engine oracle (F002), lnL tiers on certified axes only (crown <= 0.05 nats — Professor-ratified override of the aspirational 0.01, dlnL ~ eps*SNR^2; strong-shear/saddle <= 0.1; rescued <= RB_ATOL = 1.5) with a band-edge sub-tier, and the measured RB-BINNING-FLOOR line (exact-RB at delta vs delta/4 — the artifact is delta-independent; owner-approved 2026-07-20). REGISTERED data product `lens_amplification_surrogate` (DATA_CONTRACTS + data_registry package_data root + LOADERS enrollment; consumer graph regenerated). Tube-vs-raw advantage claim refined by measurement — F018: the >= 2-3x gain is against the EXTRAPOLATING far-field alternative (the production comparison); a fair same-band raw chart ties at fixture scale in the max-normalized currency. Certified by `cogwheel/tests/test_lensing_surrogate.py` (multi-chart: selection determinism/no-overlap incl. negative-theta unwrap, serialization round-trip with provenance, 8a back-compat) and `cogwheel/tests/test_lensing_surrogate_census.py` (27 tests: hand-computed MECE breakdown, F002-guarded eps currency incl. trough robustness, three lnL tiers measured 0.0148/0.0008/0.0163 nats, both design falsifiables at the F018 bars, F010 bound-mutation flips). Full-box training + full census remain the deferred post-8e driver campaign (train once on the final engine + chart set). Marginalized path (Build 5, the production sampler configuration): `LensedMarginalizedExtrinsicLikelihood` folds the total amplification into the reused `MarginalizedExtrinsicLikelihood` machinery — data side as the image-summed, exactly-time-shifted, kernel-weighted contraction against the UNCHANGED fiducial weights (`e^{i w tau_a}` is an exact timeseries shift since `w` is linear in `f`), norm side as `\|F(w_b)\|^2`-scaled bin weights (valid under the lens-aware bin guard) — and hands sky/time/psi/phase/amplitude to the unmodified `CoherentScoreHM` (the higher-mode path; the 22-only lens-phase degeneracy is never assumed). Sampled space: 12 intrinsic + 7 standard lens params, extrinsics drawn from conditionals in postprocessing. Engine refusals propagate BEFORE the marginalization integral (never averaged inside the QMC; verified by call-count). The posterior's `d_luminosity` column is the PHYSICAL luminosity distance on both routes — `LensedIASPrior`'s sampled `d_hat` transforms to standard `d_luminosity` via `UniformLuminosityVolumePrior` (physical by construction of the transform and its Jacobian), and `LensedMarginalizedExtrinsicIASPrior`'s coherent-score-drawn `d_luminosity` blob column is likewise physical since both `(d\|h)` and `(h\|h)` already carry `F` (single authoritative statement in `cogwheel/lensing/waveform.py`; F(w->0) = sqrt(mu_macro), F009); no post-analysis `sqrt(mu_macro)` rescaling is required or correct on either route. Registered pair: `LensedMarginalizedExtrinsicIASPrior` (intrinsic CBC + lens subpriors). Certified by `cogwheel/tests/test_lensing_marginalized_likelihood.py`; the absolute-normalization oracle (marginalized lnL vs importance-sampled plain likelihood) is a driver-run gate. | `cogwheel/lensing/prior.py` (`LensedIASPrior`), `cogwheel/lensing/posterior.py` (`LensedPosterior`), `cogwheel/lensing/marginalized_likelihood.py` (`LensedMarginalizedExtrinsicLikelihood`), `cogwheel/lensing/surrogate.py` (`LensAmplificationSurrogate`) |

### Key abstractions

- **`EventData`** (`data.py`) — strain + ASD for an event.
- **`WaveformGenerator`** (`waveform.py`) — wraps LALSimulation approximants
  (IMRPhenomXPHM / XAS / XODE). Note: uses IMRPhenomXP, *not* Pv2, due to
  phase-convention differences (LIGO-T1500602).
- **`Likelihood`** classes (`likelihood/`) — `CBCLikelihood`,
  `RelativeBinningLikelihood`, `Marginalized*`.
- **`Prior`** classes (`prior.py`, `gw_prior/`) — composable subpriors with
  sampled<->standard transforms, registered in a `prior_registry`.
- **`Posterior`** (`posterior.py`) — pairs a prior + likelihood, supports folding.
- **`Sampler`** subclasses (`sampling.py`) — wrap dynesty/nautilus/zeus/PyMultiNest;
  write `samples.feather` to run directories.
- Most stateful objects subclass `utils.JSONMixin` for JSON (de)serialization.

### External interfaces & dependencies

numpy, scipy, pandas (>=2.0), numba, lalsuite (lal, lalsimulation), gwpy, gwosc,
astropy, matplotlib, pyarrow; samplers dynesty, nautilus-sampler, zeus-mcmc,
pymultinest (optional). Docs: Sphinx + numpydoc + furo (`docs/source/`, Read the
Docs). Packaging: setuptools + setuptools_scm, GPL-3.0-or-later, Python >=3.9.

### Conventions

- Units: frequencies Hz, times GPS seconds, masses solar masses, distances Mpc,
  angles radians.
- Tests live in `cogwheel/tests/` (stdlib `unittest`), not a top-level `tests/`.
- `largedata/` (top-level) holds large run outputs, injection HDF5, and detector
  `.gwf` frame files used by tutorials — not part of the installed package.
- Numerically hot paths (relative binning, coherent-score marginalization) use
  numba and lookup tables and must remain numerically accurate.
- **Born rung (carrier machinery landed; serve slot still unwired).**
  `chang_refsdal/_born.py` provides the analytic carrier for the far annulus
  (`3.0 < |y| <= 4.2426`, positive parity, exact exterior fence `gamma < 3/4`,
  F025/F026). `_born_factors` returns the DERIVED, closed-form `b1`/`a0`
  (F023; a pure point mass gives `b1 = -1`, `a0 = 0`) feeding two consumers:
  `born_lead_carrier`, the lead-only serve object
  `sqrt(mu_macro)*exp(1j*w*phi_geo)` with NO `a0`/`b1` correction, and
  `born_amplification`/`born_envelope`, the resolved-image macro-limit
  diagnostic that DOES carry them. `a0`/`b1` serve nowhere (F025): `a0`
  violates the exact `F(w->0) = sqrt(mu_macro)` limit (F009) and the
  resolved-image correction inflates the demodulated residual's azimuthal
  node count 2.5x-11x over the lead-only carrier. `channels.
  born_carrier_from_partition` assembles the band-split carrier at
  `w * Delta_tau = RHO_END` (`Delta_tau` read from the partition's real-image
  delays, never recomputed, never `phi_geo`, never `w*r0_sq` — F024): below
  the split, the lead-only carrier; above, the two-real-image
  geometric-optics sum plus `farfield_ghost_term` where admitted. It is
  STILL NOT wired into the serve path: the served object is `F_carrier`
  minus a driver-trained residual chart (`F_exact - F_carrier`), and that
  chart is a TRAIN_TIER artifact that does not yet exist. Draws in the
  annulus therefore still fall through to the exact engine via
  `likelihood._surrogate_coefficients`, which is certifiable there
  (`w * |y| <= 60` never binds inside the prior). Wiring requires the
  trained residual chart, not a coefficient derivation — that blocker is
  discharged.
- **Lensing delay frame.** Every channel kernel in `chang_refsdal` is carried in
  the partition's *min-subtracted* frame: `ChangRefsdalPartition` subtracts
  `t_min = min(absolute real-image Fermat delays)` from every delay, so
  `partition.delays` always contains an exact `0`. Any term added to or
  subtracted from a sum of channel kernels MUST be carried in that same frame.
  The `geometry` primitives (`delay`, `ghost_kernel`, `_ghost_delay`) return
  RAW absolute delays and are deliberately frame-agnostic — `t_min` is a gauge
  choice belonging to the *set* of real images, not to any single saddle — so
  the min-subtraction is applied at the composition layer in `channels`, where
  `_frame_delays(source, matrix)` is the single authoritative construction of the
  frame origin (returning `(images, absolute_delays, t_min)` together so callers
  need not re-solve the image quartic; `_frame_t_min` is a thin accessor over it).
  Mixing the two frames is silent: it leaves magnitudes and decay
  rates correct while corrupting only the phase, so it is invisible to any
  amplitude-based check and must be pinned by frame assertions
  (`test_lensing_chang_refsdal_ghost_frame.py`).

## Constraints

- Numerical accuracy is paramount: relative-binning and marginalized likelihoods
  must agree with exact/brute-force references within tolerance.
- Phase/spin conventions must be respected across waveform and coordinate code.
- numba-accelerated code must stay numba-compatible.
