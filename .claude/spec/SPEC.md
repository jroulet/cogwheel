---
spec_version: 0.36.3
last_updated: 2026-08-10
---

# cogwheel — Project Specification

## Mission

cogwheel (PyPI/conda package `cogwheel-pe`, import name `cogwheel`) is a scientific
Python library for **Bayesian parameter estimation of gravitational-wave sources**
from compact binary coalescences (black-hole / neutron-star mergers). Given
conditioned detector strain data, it infers a posterior over source parameters
(component masses, spins, tidal deformabilities, sky location, distance,
orientation, coalescence time/phase).

Three signature contributions:
1. **Sampled vs standard coordinates** — a custom sampling coordinate system that
   separates reparameterized "sampled" parameters from physical "standard"
   parameters to reduce correlations.
2. **Folding** — an algorithm that reduces posterior multimodality by sampling
   folded dimensions over half their range and summing reflected images.
3. **Relative binning (heterodyning) + marginalization** — a fast likelihood that
   interpolates the ratio of a trial waveform to a reference over coarse frequency
   bins (generalized to higher modes), plus analytic/semi-analytic marginalization
   over distance and over all extrinsic parameters (the "coherent score").

## Architecture

cogwheel is a layered scientific package. `cogwheel/__init__.py` sets version
metadata only; functionality lives in submodules.

### Data flow

```
EventData (strain + ASD)  ->  WaveformGenerator  ->  Likelihood
                                                        |
                  Prior (sampled<->standard) ----------> Posterior (folding) -> Sampler -> samples.feather -> postprocessing
```

### Layers / modules

| Layer | Purpose | Key modules |
|-------|---------|-------------|
| Data acquisition & conditioning | Download public GW strain (GWOSC/GWpy), condition it, store ASDs and event metadata, build `EventData` (incl. Gaussian-noise and injected-signal events). | `cogwheel/data.py`, `cogwheel/data/events_metadata.csv`, `cogwheel/data/example_asds/` |
| Waveform generation | Frequency-domain waveforms via LALSimulation; project onto detectors; register approximants. | `cogwheel/waveform.py`, `cogwheel/waveform_models/__init__.py`, `cogwheel/waveform_models/xode.py` |
| Likelihood | CBC likelihood, fast relative-binning likelihood (with higher modes), marginalized variants. | `cogwheel/likelihood/likelihood.py`, `relative_binning.py`, `marginalized_distance.py`, `marginalized_distance_phase.py`, `marginalized_extrinsic.py`, `marginalized_extrinsic_qas.py`, `reference_waveform_finder.py` |
| Extrinsic marginalization (coherent score) | Marginalize over extrinsic params from matched-filter timeseries; sky dictionary + lookup tables; numba-accelerated. | `cogwheel/likelihood/marginalization/base.py`, `coherent_score_hm.py`, `coherent_score_qas.py`, `skydict.py`, `lookup_table.py` |
| Priors & coordinates | Abstract `Prior` base with sampled<->standard transforms; composable subpriors; concrete GW priors; PN-inspired QMC proposals. | `cogwheel/prior.py`, `cogwheel/prior_ratio.py`, `cogwheel/pn_coordinates.py`, `cogwheel/gw_prior/combined.py`, `mass.py`, `spin.py`, `extrinsic.py`, `tides.py`, `pn.py`, `twosquircle.py`, `miscellaneous.py` |
| Posterior & sampling | Combine prior + likelihood into `Posterior` (with folding); find reference solution; sample via dynesty/nautilus/zeus/PyMultiNest. | `cogwheel/posterior.py`, `cogwheel/sampling.py`, `cogwheel/postprocessing.py` |
| Utilities & physics | JSON (de)serialization mixin, caching, detector geometry/response, sky-loc angles, cosmology. | `cogwheel/utils.py`, `cogwheel/gw_utils.py`, `cogwheel/skyloc_angles.py`, `cogwheel/cosmology.py` |
| Plotting | Corner plots with GW-specific LaTeX labels. | `cogwheel/plotting.py`, `cogwheel/gw_plotting.py` |
| Validation | End-to-end injection-recovery pipeline (PP-plots / coverage). | `cogwheel/validation/generate_injections.py`, `inference.py`, `analyze.py`, `injection_prior.py`, `example/config.py` |
| Microlensing engine (Chang–Refsdal wave optics) | Wave-optics amplification engine for microlensed PE: double-double arithmetic substrate (`_dd.py`), exact gauge/cluster-split channel algebra (`_gauge.py`), image geometry (`geometry.py` — quartic solver, delays, magnifications, stationary-phase kernels, `nearest_caustic_point`, analytic closed-form caustic derivatives `caustic_derivatives`/`caustic_third_derivative`/`caustic_speed`/`caustic_curvature_radius`/`fold_opening_direction`), dd-accumulated complex-1F1 kernel (`_hyp1f1.py`, certified `0 < w <= W_MAX_CERTIFIED = 500` with product ceiling `w*sqrt(s) <= DD_PRODUCT_CEILING = 60`), the operator entry points `F_op` / `F_op_grid` (`operator.py`), the exact wave evaluator (`_schwinger.py`), the uniform-asymptotic arms (`_airy_fold.py`, `_pearcey_cusp.py` + its `_pearcey_table.py` spline artifact), the Born exterior rung carrier (`_born.py`), and topology-stable `ChangRefsdalChannels` (`channels.py`, the public entry point). PARITY DOMAIN — BOTH parities serve, end to end. Positive parity is `1-kappa > \|gamma\|`; the macro saddle is `0 < 1-kappa < \|gamma\|`, its caustic two disjoint 3-cusp deltoid lobes (the +- branch of the astroid formula; census {1,1}/{0,1,1,1}, index sum -2). Type III (`lam = 1-kappa <= 0`) and the exact `det A = 0` parity boundary refuse by name (`LensDomainError`); the sampled reduced shear spans `gamma in (0, 1.6)` across both parities with `gamma = 1` a measure-zero named refusal, and the `u1/u2` quadrant fold stays valid on astroid and deltoid alike (the fold is a Fermat-potential reflection symmetry, parity-blind). `ChangRefsdalChannels.evaluate` delegates every saddle node to the batched operator call and `LensedWaveformGenerator` constructs on saddles, with `macro_matrix` as the domain gate. SERVING LADDER, per node: surrogate (in box) -> geometric -> uniform arm -> Schwinger exact -> named refusal. `select_branch` is the ONE geometric-vs-wave predicate, called by both operator grids (`_positive_parity_grid`, `_saddle_grid`) and by `channels._exact_total`: geometric only when resolved (`w*delta_min >= RHO_END = 4.0`), strongly cancelling (`L > L_MAX = 48`) AND far from the caustic (`eta >= ETA_MIN_GEOMETRIC = 0.3`). The `eta` leg is live on BOTH parities, each measured on its own terms: positive-parity worst-case p90 1.17 -> 7.65e-5 (F031), saddle p90 8.95e-1 -> 4.54e-3 with worst case 484x over 15% of resolved draws (F034). It cannot be substituted by `L`: below the floor the error is FLAT in `L`, because just outside a fold the annihilated image pair are undamped complex saddles that a real-image sum omits (F029) — and the runtime `_certify_geometric_census` does NOT catch that tail, since it checks image count and Morse parity, not distance to the caustic. The saddle passes an infinite cancellation exponent (no saddle analogue) and measures `eta` once per grid via `nearest_caustic_point`. `L_MAX = 48` is the wave/geometric HANDOFF exponent, not an accuracy dial — raising it pushes geometric-served nodes onto the degrading wave path — pinned by a double-sided bracket test (`L_geo <= 48 <= L_wave + margin`, mpmath oracle) and re-derived in F031 independently of the retired operator series. EXACT WAVE EVALUATOR: the 1D Schwinger-parameter dd quadrature (`_schwinger.f_schwinger`, paired N-vs-2N certification) is THE exact evaluator on both parities — a sheared host of either parity reduces / rotates / reconstructs identically (`lam = 1 - kappa`, single y-independent cancellation channel `L_S = pi*w/4`), oracle-certified to the 1e-10 bar (F005) for its dd-quadrature path up to `w <= W_CEILING_SCHWINGER = 60`; for `60 < w <= W_CEILING_SCHWINGER_QD = 150`, a sequential mpmath arbitrary-precision path (`_f_schwinger_mpmath`, `dps = 30 + ceil(w)`, paired N/2N certification) serves instead; above `W_CEILING_SCHWINGER_QD = 150` it refuses by name (`SchwingerCertificationError`) rather than returning a silent `nan` or a finite-but-wrong number. THREE DISTINCT CEILINGS (F019 — extended): `W_CEILING_SCHWINGER = 60` bounds the dd-quadrature FREQUENCY domain alone; `DD_PRODUCT_CEILING = 60` bounds the 1F1 kernel's PRODUCT `w*sqrt(s)`; `W_CEILING_SCHWINGER_QD = 150` is the hard mpmath upper bound — all three are separate variables and none implies the others. The shear-free `gamma' == 0` point lens (measure-zero in the prior) is a closed form: the shear operator is the identity there, so the amplification is the point-mass kernel `point_mass_g_derivatives` times the mass-sheet phase, and its only refusal is the kernel's own `_hyp1f1.HypergeometricDomainError`. Per-node Schwinger loops run through an njit prange PURE-MAP driver (bit-exact per node vs serial, refusal decisions scheduling-independent, any-node-refuses -> whole-grid-refuses, the named exception raised only in the Python wrapper; `.py_func` exposed for F010). UNIFORM ARMS — NEITHER the arms NOR the geometric branch is CERTIFIED; the honest description of both is "best available serve under the authoritative gate". `_airy_fold.py` is the fold (two-coalescing-image) uniform Airy evaluator — `F = 2*sqrt(pi)*exp(i*w*tau_bar + i*sigma)*[p*w^{1/6}*Ai(-xi) - i*q*w^{-1/6}*Ai'(-xi)]`, `xi = (3*w*Delta_tau/4)^{2/3}`, amplitudes from the FINITE fold-frame curvatures (hard-axis eigenvalue + soft-axis cubic; never the divergent per-image `sqrt\|mu\|`), prefactor/phase pinned by the closed-form `xi -> inf` match to the exact geometric two-image sum, finite on-caustic (`Ai(0)`), self-certified via the `O(xi^{-3/2})` leading-error estimate against the F016 envelope bar. It serves ONLY within `eta < _ETA_MAX_FOLD = 0.3` of the caustic (the complement of `ETA_MIN_GEOMETRIC`, pinned equal by test) and returns None — fall-through, no new exception class — outside that fence or when otherwise uncertifiable. The fence is load-bearing and PERMANENT: `q = 0` is a symmetric-fold assumption that cannot represent a two-image sum with unequal magnifications, the `xi` self-certificate cannot see distance from the caustic (it reads 1.2e-2..4.7e-2 where the true error is O(1)), and the unfenced arm measured 60%-267% wrong on well-resolved above-ceiling configs (F028), independently confirmed at 63%-64% against GLoW (F032). No amplitude refinement removes the residual, which is the cubic normal form's own O(eta) truncation rather than `q = 0` — `p` fails identically (F033). `_pearcey_cusp.py` is the cusp (three-coalescing-image) uniform Pearcey evaluator — certified Pearcey primitive by Connor-Curtis rotated-contour quadrature (steepest-descent valleys, paired N-vs-2N Gauss-Legendre at the `_schwinger` 3e-10 tolerance, P certified BEFORE any prefactor), control arguments `x ~ w^{1/2}*delta_par`, `y ~ w^{3/4}*delta_perp` (semicubical 2/3 law), large-argument limit pinned to the geometric image sum, same None-fall-through contract; served from the REGISTERED `pearcey_table` artifact (build-time-derived box + demodulated Re/Im bicubic splines, content-hash-verified load, live-quadrature fallback on box exit or ANY anomaly — never serves where wrong; regeneration `scripts/train_pearcey_table.py`). `cusp_amplification` opens with a high-w ppGO fast rung: when the control radius clears the ppGO gate (the uniform-form leading-error bar, tightened by the `_PPGO_BAR_DIVISOR` divisor against the `_R_PPGO_ERROR_CONST` coefficient, and `w >= _W_PPGO_FLOOR`) it serves `_airy_fold.fold_ppgo_correction` directly — the geometric-image-sum limit of the Pearcey function — a ~10^3x faster path that returns before any table or quadrature lookup; the None-fall-through contract is unchanged (rung refusal falls to the uniform Pearcey path). Above `w = 60` the arms are anchored by continuity from the certified band plus a sparse offline mpmath high-dps anchor (driver post-build sweep; uniform error DECREASES with `w`). BORN EXTERIOR RUNG (`_born.py`, carrier + wiring + trained residual chart shipped): the last ladder rung for the exterior-to-caustic region (`rho > 1`, both parities), keyed on `w*Delta_tau` (a mid-`w` RESOLVED-IMAGE expansion, not a low-frequency or `\|y\|`-only one). `born_lead_carrier` is the serve object — the lead-only analytic carrier `sqrt(mu_macro)*exp(1j*w*phi_geo)` expanded about `mu_macro = 1/((1-kappa)**2 - gamma**2)`, NOT about 1, with small parameter `1/\|y'\|**2`; the resolved-image `a0`/`b1` corrections stay OUT of the serve carrier because they spline far worse and violate F009 below the band split (F025), surviving only in the `born_amplification` diagnostic. `born_gate` is the measured validity boundary as a named refusal (`BornDomainError`) and `born_envelope` demodulates via the same `switched_analytic_channels` projection the trained far-field charts use. The fact-4 slot in `likelihood._surrogate_coefficients` is wired: when a `BornResidualChart` is attached, it reconstructs `F_carrier + R`; when `None` (default), exterior draws fall through to the exact engine, certifiable throughout (`w*\|y\| <= 60`). A trained artifact `cogwheel/data/born_residual_chart.npz` (≈ 8 KB) is **shipped as package data** and is attached at construction time to complete the zero-quadrature exterior serve path for `rho > 2`. (A `BornResidualChart.load` classmethod is not yet implemented; construct directly from the shipped `.npz` arrays.). LOW-W FLAT EXTRAPOLATION: all chart types (tube, exterior-polar, lobe, wedge) now serve draws with `w < chart.w_min` by clamping `log_w_query` to `log_w_grid[0]` — the envelope is smooth and nearly constant below the first Airy fringe (`O(w_min^2)` correction to the geometric limit); `_log_w_band_serveable` uses a one-sided high-end check (low end is open), `_evaluate_chart` clips before the spline. Certified by `cogwheel/tests/test_lensing_low_w_extrapolation.py`. FOLD-PPGO INTERIOR HANDOFF: interior draws (positive parity, `rho <= 1.0`) above the `InteriorWedgeChart` DD-product w-ceiling — where no chart covers the point — are now served by `_surrogate_coefficients` when the merging fold pair is well-resolved (`xi_min = (3*w_min*Delta_tau/4)^{2/3} >= _XI_FOLD_THRESHOLD = 4.0`) AND the per-pair uniform error estimate (`_uniform_error_estimate`) is below `CERTIFICATION_BAR`; reconstruction mirrors the Born rung path (`reconstruct_farfield` with `FARFIELD_KERNEL_SUM`). Draws failing either gate fall through to the exact engine as before. `surrogate_census.characterize_sample` mirrors the same gate and records category `ppgo_fold` for qualifying draws. Certified by `cogwheel/tests/test_lensing_fold_ppgo_handoff.py` (fold-ppGO accuracy vs exact engine at high xi, xi gate refusal for near-caustic draws, envelope reconstruction round-trip, self-falsification witnesses, c_A fine gate refusal, default path unaffected, census `ppgo_fold` category recording). NAMING HAZARD: `far-field` / `farfield_*` elsewhere in the package mean "trained chart OUTSIDE the caustic in the far-field GAUGE", never this weak-deflection far field. RUNTIME GUARDS: an index-theorem census guard closes `find_images_quartic` (the single image producer) on both parities — invariant `sum (-1)^{n_a} = sign(det A) - 1` for non-degenerate censuses, with one Morse-theory-correct degenerate pass-through (a discrepancy is legitimate ONLY when the census carries a near-critical witness image: fold-merged pair, count 3, or on-cusp triple merge, count 2); any discrepancy over regular-only images still refuses (F012). The geometric-served path additionally carries image-count-match and Morse parity-sum guards. An exactly-singular projected Fermat Hessian in the stationary-phase kernel raises a named `LensDomainError` instead of a raw `LinAlgError`; near-singular metrics still flow to the F008/SACR-C switch by design (F015). `LensedPosterior`'s refusal net covers the full named vocabulary (`LensDomainError`, `SchwingerCertificationError`, `LensedBinningError`), built at raise time so the F010 mutation falsification can still red it. NUMBA THREAD-LAYER INVARIANT: numba's parallel layer initializes ONCE per process at the first prange/parallel-ufunc call with the env value AT THAT MOMENT — nothing may pin `NUMBA_NUM_THREADS` at import scope outside `COGWHEEL_STRICT_TIMING`, or later parallel callers hard-fail on the mismatch. `nearest_caustic_point` solves the stationarity condition of the squared distance by analytic Newton (njit `_squared_distance_derivatives` / `_newton_caustic_cell`; ~32-node seeding, two best cells, per-lobe/branch wedge clamping, bounded-Brent fallback at cusps and discriminant clamps): the DISTANCE is value-preserving to 9.3e-12 worst over 5677 both-parity configs, while the arc parameter theta is GAUGE, certified at 1e-10 (1e-8 at shallow minima, where Newton beats the superseded Brent — F017) in the arc-length currency `theta_gap*caustic_speed`; 0.095 ms positive / 0.99 ms saddle. COST AND TIERS: the exact path runs ~90 ms/node (crown lnlike ~751 ms — the surrogate is the production speed layer); brute-force accuracy tests are gated behind `COGWHEEL_BRUTE_ACCURACY` and run as a driver post-build tier (`.claude/sdk/post_build_sweeps.sh`). CERTIFIED BY `cogwheel/tests/test_lensing_batched_operator.py` (70-dps-mpmath-oracle accuracy at 1e-10, single-vs-batch refusal-decision identity with zero flips, F010-style py_func-chain self-falsification, served-band values vs the oracle above the ceiling), `test_lensing_operator.py` (`select_branch` one-home predicate: the routing each operator grid ACTUALLY performed, recovered from what it served, equals the gate's own label — and the `eta` leg is shown live, so a mirror that drops it cannot pass), `test_lensing_saddle_geometry.py` and `test_lensing_schwinger.py` (independent AST-guarded mpmath oracle 9.1e-14..1.6e-11 across the band; deep-band F009-S magnitude + Morse-phase pins; F011-F013), `test_lensing_saddle_channels.py` (identity residual 1.8e-16; independent mpmath end-to-end oracle 5e-15; switch bound measured 1.32/1.52 vs ceilings 2/4; lobe-jump continuity; F009-S channel-layer macro plateau 1.4e-7), `test_lensing_saddle_likelihood.py` (saddle RB-vs-brute; rescued-node gates; refusal-precedes-coherent-score spy; 1e4 both-parity prior round-trips; deltoid reflection 1e-14), and the uniform-arm suite (sign/convention falsifiers incl. the `Ai(-xi)` oscillation-vs-decay handoff ON the caustic, asymmetric-fixture p/q-swap catch, at-the-fold finiteness; Pearcey primitive certification incl. honest-certificate tracking and the `P(0,0)` closed form; scaling-swap exponent fits; both-directions fall-through with the F010 mutation idiom; serving-ladder determinism). Certified by `cogwheel/tests/test_lensing_caustic_cusps.py` (analytic caustic-derivative certification of `caustic_derivatives`/`caustic_speed`/`caustic_curvature_radius`/`fold_opening_direction` against independent oracles, plus the analytic-vs-sampled cusp-root cross-check). The envelope LOO stop is gamma'-keyed (`4e-3` below `gamma' = 0.5`, `1e-3` above), justified by the saddle envelope gate and NOT by the rescued nat-gap, which is RB-binning-limited (F016). | `cogwheel/lensing/chang_refsdal/channels.py` (public), `operator.py`, `_schwinger.py`, `_airy_fold.py`, `_pearcey_cusp.py`, `_pearcey_table.py`, `_born.py`, `_hyp1f1.py`, `geometry.py`, `_gauge.py`, `_dd.py` |
| Microlensed waveform & likelihood (Chang–Refsdal PE) | Apply the engine's amplification to CBC waveforms and evaluate a fast relative-binning likelihood for microlensed events. `LensedWaveformGenerator` composes an ordinary `WaveformGenerator` and multiplies every harmonic mode by the shared factor `F(w(f))`, with `w = 8*pi*G*M_L*(1+z_L)*f/c^3` dimensionless and linear in `f`; it exposes the per-image `(tau_a, K_a)` decomposition the likelihood consumes, not only the collapsed total. `LensedRelativeBinningLikelihood` (subclass of `BaseLinearFree`) heterodynes against an *unlensed* reference `par_dic_0` and reconstructs the lensed `(d\|h)`/`(h\|h)` from delay-continuous frequency-moment summaries contracted mode-then-image (additive `M^2 + n_img^2` cost, no FFTs on the hot path); image-delay phases stay analytic, smooth kernels are interpolated, and a lens-aware bin guard raises `LensedBinningError` if `pi*Delta_f_bin*delta_t_max` breaches tolerance. Positive-parity macro images only: `1-kappa > \|gamma\|` is enforced by raising `geometry.LensDomainError` at the API boundary (constructor and every strain/likelihood path), never a warning or `nan`; the wave branch is certified-or-named-refusal everywhere (WP1, F005 NARROWED). Fast path (Builds 3-3f, SACR-C): the channel construction is the switched-analytic + single-envelope decomposition — persistent resolved images carried by the ANALYTIC saddle kernel `geometry.image_kernel` under their own carriers with smootherstep weights `S_a(w) = smootherstep(w*\|tau_a - tau_c\|, 0.5, 4)` keyed on the CRITICALITY separation (`tau_c` = the parked critical-carrier delay from `nearest_caustic_point`; supersedes the F008 full-cluster switch keying — see the F008 addendum), plus ONE beat-free interpolated transition envelope `E(w) = e^{-i w tau_c}(F - sum_a S_a H_a e^{i w tau_a})` whose demodulated phase is bounded at 4 rad by construction. The envelope is engine-evaluated (`F_op_grid`) on a leave-one-out-adaptive coarse grid (`_LOO_SEED_NODES = 8`, stop `4e-3`, ceiling `_LOO_MAX_NODES = 48`, node count config-independent) and candidates are rebuilt by closed-form dense reconstruction (`reconstruct_from_envelope`); the dd/1F1 ladder and operator contraction are numba-njit (`fastmath=False`, refusal logic and thresholds untouched in Python), and the frequency-independent nearest-caustic search is njit-accelerated (value-preserving to rel `1e-10`, branch-invariant). Ratio layer (Build 3g): per-proposal cost is cut by heterodyning the candidate envelope against a memoized FIDUCIAL envelope — a pure function of the candidate (all five lens-geometry params snapped to a fixed lattice, `m_lens`/`z_lens` shared exactly so the `w` grid never regrids) — interpolating only the ultra-smooth ratio `rho = e^{i w dtau_c} E_cand/E_fid` (~8 LOO nodes, config-independent) with the critical-delay difference pulled out analytically; image-count and envelope-health guards (and any fiducial-side refusal) fall back to the certified direct path, while candidate-side refusals propagate unswallowed on ratio, direct, and brute paths alike. Certified by `cogwheel/tests/test_lensing_ratio_layer.py` (lattice-point identity, perturbed ratio-vs-direct, ratio-vs-brute at inherited tolerances, bit-identical cache determinism, refusal symmetry, deep-band macro limit; measured warm single-thread lnlike ~9.8 ms, ~143x over brute force). Certified by `cogwheel/tests/test_lensing_fast_path.py`: numba-vs-mpmath preservation, a null-safe production-grid interpolation gate (`max\|dF\|/max\|F\| < 1e-3`, worst regime two-image `4.2e-4`), RB-vs-brute agreement on every lens regime, and single-thread timing guards (warm lnlike ~0.3 s/eval, ~50x over brute force; the few-ms target is deferred to a 2D surrogate-table decision). ABOVE-CEILING ppGO INTERCEPT (`_ppgo_above_ceiling`, Build exterior_followup WP4): `LensedRelativeBinningLikelihood._amplification_coefficients` opens with a ppGO rung for draws where `w_max > W_CEILING_SCHWINGER_QD` (=150), where the Schwinger QD evaluator hard-refuses. Gate: ≥ 2 real images AND narrowest image pair resolved (`w_lo * min_delta_tau >= RHO_END = 4.0`). On pass, computes the fold-corrected ppGO carrier via `fold_ppgo_correction`, demodulates the residual envelope, and reconstructs kernels via `reconstruct_farfield` (same path as Born rung and fold-ppGO interior handoff). On gate miss falls through to the exact engine unchanged, which then raises `SchwingerCertificationError` as before. Certified by `cogwheel/tests/test_lensing_ppgo_above_ceiling.py` (boundary continuity: exterior ppGO matches engine at w=55,60 within 3e-5; fold-correction error bounded at w=150 < 0.30 and at all w < 0.50; finite at w=151,500; gate border structural tests; self-falsification witnesses; unresolved fall-through raises `SchwingerCertificationError`; single-image telescoping identity ~1e-15). | `cogwheel/lensing/waveform.py` (`LensedWaveformGenerator`), `cogwheel/lensing/likelihood.py` (`LensedRelativeBinningLikelihood`), `cogwheel/lensing/ppgo_map.py` (`CertifiedPpgoMap`, `caustic_rho`), `cogwheel/lensing/born_residual_chart.py` (`BornResidualChart`) |
| Microlensed sampling layer (priors, folding, posterior) | Sampled lens coordinates per the locked reduced parametrization: kappa/beta/z_lens ELIMINATED (`FixedLensGeometryPrior` — kappa is exactly mass-sheet degenerate and never sampled; z_lens folds into the sampled REDSHIFTED lens mass since only `w ∝ M_L(1+z_L) f` enters), `ln m_lens` log-uniform with certified-domain provenance, reduced shear `gamma` uniform on (0, 1.6) — a single continuous BOTH-PARITY range, parity deterministic in gamma, with the `gamma = 1` det-A=0 boundary a measure-zero named refusal — and the source position as a unit box `(u1, u2)` in the shear frame scaled by a mass-conditioned factor keeping `w*sqrt(s) <= 58` by construction; quadrant folding via `folded_reflected_params = ['u1','u2']` (valid on BOTH the astroid and the saddle deltoid — the fold is a reflection symmetry of the Fermat potential, parity-blind); NO phase-fold (the constant-lens-phase ~ orbital-phase degeneracy is 22-only and must not be assumed for XPHM). `LensedIASPrior` (registered, `default_likelihood_class = LensedRelativeBinningLikelihood`) composes these with the IAS CBC subpriors; `LensedPosterior` maps the full named-refusal vocabulary (`LensDomainError`, `SchwingerCertificationError`, `LensedBinningError`) to `lnL = -inf` at the POSTERIOR BOUNDARY ONLY (the engine/likelihood named-refusal contract is untouched); the in-memory fiducial cache is dropped on pickle so sampler workers rebuild deterministically. Certified by `cogwheel/tests/test_lensing_prior.py` (round-trips 1e-12, Jacobians, 1e4-draw both-parity domain safety, reflection/fold consistency, mass-sheet lnL invariance, refusal-net mutation check, XPHM no-phase-fold). SURROGATE SPEED LAYER (off by default): `cogwheel/lensing/surrogate.py` (`LensAmplificationSurrogate`) — an offline-trained tensor cubic-spline emulator of the SACR-C envelope over (log w, gamma, y1_eig, y2_eig), beta eliminated exactly by eigenframe rotation, real/imag interpolated separately; refusal-conservative domain gate (certified-box containment + exclusion balls around refused training points; the surrogate NEVER answers where the engine refuses); npz/pickle serialization with provenance. Consumed via `LensedRelativeBinningLikelihood(amplification_surrogate=...)` — a single intercept serving `channels.geometry_partition` + surrogate envelope + `reconstruct_from_envelope`; DEFAULT `None` keeps every path byte-identical (crown max\|diff\| = 0 pinned). Enabling by default requires the operator-deferred production gates: full-box trained artifact (registered in DATA_CONTRACTS + LOADERS on shipping), production-scale nat tiers, caustic-margin census, PP validation. Certified by `cogwheel/tests/test_lensing_surrogate.py` (crown byte-identity, refusal preservation + F010 mutation, beta-elimination 1e-12, F002 AST guard, budget-calibrated reconstruction gates with documented production-target provenance). GLOBAL MULTI-CHART ARTIFACT: the surrogate is a flat chart collection — near-caustic TUBE charts in caustic-adapted coordinates `(gamma, u = sqrt(eta), s, log w)` (eta/theta from `nearest_caustic_point`; the caustic is the fixed plane eta = 0; u = sqrt(eta) absorbs the envelope's own fold branch; the arc axis is ARC LENGTH `s = integral caustic_speed dtheta` along the fold arc, NOT raw theta — the chart interpolates in `s`, and a query theta is converted through the per-chart `theta_to_s` map stored with the chart, one `np.interp` and no quadrature on the serve path; splining in the raw angle made held-out eps depend on where the arc bounds happened to fall, swinging +-23% under a +-0.01 rad bound shift, F042. theta bounded, non-periodic, per fold arc, query-unwrapped into the chart frame by `_theta_into_frame` since `nearest_caustic_point` reports [0, 2pi) while wedge-frame deltoid arcs span negative theta; membership tests and cusp-window exclusion stay in theta), plus mass-stratified `ExteriorPolarChart` exterior charts per image-count region, interpolating in caustic-fixed polar `(rho, theta_c)` (4th spline axis cusp-adapted `u = d**(2/3)` on positive parity, raw `theta_c` on the macro-saddle), plus per-lobe interior `LobeInteriorChart`s for the macro saddle (`gamma > 1`). The saddle caustic is two disjoint 3-cusp deltoid lobes sitting OFF the origin on the shear axis, so neither encloses the origin and the origin-centred caustic-fixed coordinate does not apply. Each lobe instead carries its OWN frame — source-plane deltoid centroid, the other lobe's centroid, the inter-lobe corridor half-width, and the directional boundary `(boundary_theta, boundary_r)` — and is charted in lobe-local `(rho_lobe, u)` where `u = d**(2/3)` with `d` the angular distance to the NEAREST DELTOID CUSP VERTEX — the exact, gamma-universal caustic-reach cusp scaling (`r_deltoid ~ const - c*d**(2/3)`), the macro-saddle counterpart of the wedge's cusp-adapted axis, so `rho_lobe` no longer carries the `|dtheta|**(1/3)` divergence of the raw `theta_local` axis at a cusp vertex — with `rho_lobe = \|y - centroid\| / r_deltoid(theta_local)`, so `rho_lobe = 1` tracks the deltoid boundary in EVERY direction (a scalar reach overshoots the near-cusp directions of an elongated lobe and leaves its interior untileable). `r_deltoid` has ONE authoritative definition, `surrogate._lobe_boundary_radius`, shared by the coordinate maps and by `_SaddleLobeAdmission._r_deltoid`. Lobe charts store the interior SACR-C `tau_c`-demodulated label and carry `image_count = 4` derived from the engine's `real_mask.sum()` with a constancy assert (an origin-centred `rho < 1` heuristic is structurally wrong off-origin). Serve-side `_lobe_serves` gates gamma box, log-w band, then the INTER-LOBE CORRIDOR (`\|y - centroid\| + corridor_half <= \|y - other_centroid\|`), then lobe-local box containment, exclusion balls, image-count and the eta floor; a source inside the corridor fails for BOTH lobes and falls through to the exact-engine ladder as a named refusal, so no admitted tile straddles the lobe-equidistance line and no source is ever served from the wrong lobe. Lobe artifacts carry a SINGLE axis-schema tag: `_LOBE_AXIS_SCHEMA_NEW = 'lobe_caustic_relative_v1'`, and the cusp-adapted `theta_to_u` map is REQUIRED: `from_lobe_engine` builds it via `_lobe_cusp_axis_map` (a uniform-in-`u` `(2, 2001)` array `[theta_fine, u_fine]`, same node count as `_FARFIELD_ARC_MAP_SIZE` and the wedge's map), and the loader reads it unconditionally, so an absent map hard-refuses (KeyError). The two OLD lobe tags — `_LOBE_AXIS_SCHEMA_V1` (raw `theta_local` spline, backward-compatible) and the sqrt-edge `_LOBE_AXIS_SCHEMA` (`s = sqrt(span) - sqrt(theta_max - theta_local)`, `theta_to_s` array) — are DROPPED from the known set, so a stale V1 or sqrt-edge artifact hard-refuses at load rather than being served on the wrong angular coordinate. Gated lobe tiles are subdivided at the U-MIDPOINT (mapped back to `theta_local` through the nearest cusp's `_lobe_cusp_axis_map`), NOT the raw theta midpoint, and the `_LOBE_CUSP_EXCLUSION_DISTANCE` carve-out is RETIRED — the cusp-adapted coordinate now handles near-cusp tiles, so the `eta_max` nearest-caustic-distance test alone excludes them. Certified by `cogwheel/tests/test_lensing_surrogate_lobe.py` (coordinate round-trip <= 1e-12 including the `+-pi` theta_local seam and near-cusp directions, single-source `r_deltoid` parity under helper mutation, corridor named-refusal and lobe exclusivity, served-vs-fresh-engine oracle with node-exactness, `image_count == 4` constancy, persistence round-trip with axis-schema refusal, a history-free positive-parity golden-value regression; cusp-adapted `u` round-trips (`theta_to_u` vs a closed-form oracle, `u = 0` endpoint exactness, strict monotonicity, `(2, 2001)` shape) and bound-shift stability for the u-coordinate path; single-schema persistence covering the `lobe_caustic_relative_v1` tag, absent/unknown axis-schema hard-refusal, and byte-identical served values after reload). ASTROID INTERIOR CHARTS (`InteriorWedgeChart`, positive parity, `gamma < 1`): `InteriorWedgeChart` tiles the astroid interior in caustic-normalised wedge-polar coordinates `(r, theta_wedge)` where `r = \|y_eig\| / r_caustic(gamma, theta_wedge)` (`r = 0` at the centre, `r = 1` at the caustic boundary) and `theta_wedge = atan2(\|y2_eig\|, \|y1_eig\|) in [0, pi/2]`, exploiting D2 (4-fold) symmetry — one wedge is trained and all four quadrants are served by folding at query time. The DD product `w * \|y\| = w * r * r_caustic(gamma, theta_wedge)` is exactly known at each grid point, eliminating the DD cap bottleneck for high-w draws at small `\|y\|`. Artifacts carry `axis_schema = 'wedge_caustic_relative_v3'` (`_WEDGE_AXIS_SCHEMA`); v3 is the ONLY known tag. Both v1 (the genuinely arc-length angular axis) and v2 (the same cusp-adapted `u` axis as v3, serialized under arc-length field names) are DROPPED from the known set, so absent or unknown tags — including a stale v1 or v2 artifact — hard-refuse at load rather than mis-serving at the wrong angular coordinate or under the wrong field names. Serve-side `_wedge_serves` gates gamma box, log-w band, folds the source into the canonical first quadrant, checks `(r, theta_wedge)` box containment, exclusion balls, image-count, and the eta floor. CUSP-ADAPTED ANGULAR AXIS: the wedge angular spline axis is `u = d**(2/3)`, `d` the angular distance to the NEAR astroid cusp (`theta_wedge = 0` or `pi/2`) — the exact, gamma-universal cusp scaling of the caustic reach (`r_caustic ~ const - c * d**(2/3)`), which the retired arc-length axis made WORSE (`caustic_speed` vanishes linearly at a cusp, so arc length `s ~ theta**2` and the envelope behaved as `f(s**(1/3))`). The wedge splits at the caustic WAIST `theta_waist = argmin_theta r_caustic(gamma, theta)` (`_wedge_theta_waist`), NOT at `pi/4`: the waist migrates up to ~30% away from `pi/4` as shear stretches the astroid, so each training tile lies entirely on one side of the waist and carries a matching near-cusp `axis_origin` (`'low'` for `theta_wedge = 0`, `'high'` for `pi/2`), single-sourced from the tiler and verified against the engine's own midpoint-vs-waist classification on mismatch (`ValueError`). `from_wedge_engine` always builds a `theta_to_u` map via `_wedge_cusp_axis_map` — a `(2, 2001)` array `[theta_fine, u_fine]` built UNIFORM IN `u` (same node density as `_FARFIELD_ARC_MAP_SIZE` and the tube's `_TUBE_ARC_MAP_SIZE`) — and derives `u_grid` as the images of the training `theta_wedge_grid` nodes through that map (`np.interp`); the spline's 4th axis is `u_grid` rather than raw `theta_wedge`, concentrating knots near the tile's near cusp rather than arc-length-uniformly. `theta_to_u` is REQUIRED under v3: it is read unconditionally at load and an absent map hard-refuses (the v2 tolerance for pre-56a223a artifacts is retired with the v2 tag). The stored `theta_to_u` converts a query `theta_wedge` to `u` at serve time (one `np.interp`, no quadrature in the hot path). The FIELD NAMES split by coordinate: the tube and far-field maps genuinely hold arc length and keep `theta_to_s` / `_validate_theta_to_s`, while the wedge AND lobe-interior maps carry the cusp-adapted `theta_to_u` / `_validate_theta_to_u`; both validators now delegate to a shared `_validate_axis_map` core that checks monotonicity and start-at-zero ONLY, with deliberately NO length-scale bound, since `u` is `rad**(2/3)` and must not inherit a bound sized for radians. `_wedge_cusp_axis_map` HARD-RAISES (`ValueError`) for a `theta` bound outside `[0, pi/2]` — exactly the D2-folded fundamental domain — rather than clamping: a bound outside it can only come from a caller that failed to fold, and a clamp would silently serve the reflected tile's basin and mask the fold bug (it previously returned a silently COMPLEX array from the negative base of `(pi/2 - theta)**(2/3)`, surfacing frames later as an unrelated-looking cast error inside `np.interp`). DD-PRODUCT W-CEILING: `from_wedge_engine` caps `w_max` at `_DD_PRODUCT_MARGIN / (r_grid[-1] * reach_max)` before building the log-w grid — where `reach_max` is the max caustic reach over theta nodes in the tile's `theta_wedge_range` — so every training node satisfies `w * r * r_caustic < _DD_PRODUCT_MARGIN = 58` by construction; the cap is applied BEFORE the density calculation, so node spacing is unchanged by the cap. `_WedgeCausticMap` (`_validate_wedge_caustic_map`) is the precomputed caustic-radius lookup table; `from_wedge_engine` is the minimal training entry point. Certified by `cogwheel/tests/test_lensing_interior_wedge_chart.py` and `cogwheel/tests/test_lensing_wedge_dd_arclength.py` (DD-product w-ceiling formula pinned against `_DD_PRODUCT_MARGIN / (r_max * reach_max)`, cap enforced below requested w_max, theta_to_u cusp-adapted map construction and node-exact serve accuracy). Chart selection is a deterministic guard stack, `select_chart`, keyed ONLY on certified (gamma, eta, image-count) — theta solely for cusp-window exclusion (F017) — with a `\|gamma-1\| < 1e-3` guard band and tube-over-far-field priority; cusp neighborhoods are EXCLUDED from tube charts (2/3-power singularity); draws within `_CUSP_ARM_COVERAGE = 0.07 rad` of the cusp vertex are now served by the Pearcey arm; the residual exclusion window beyond the arm's certified reach falls through to the exact engine; (theta_cusp, delta_theta) windows and eta_floor recorded per chart; single-npz serialization with a JSON provenance scalar (incl. dropped_gamma_slivers); package-data default load path (`cogwheel/data/`) + explicit override; a single-box npz loads as a one-chart special case. TRAINING (`cogwheel/lensing/surrogate_training.py` + `scripts/train_lens_surrogate.py`): prior box read from the prior classes; cusp locations derived from caustic-speed minima with a topology cross-check (4 astroid / 6 deltoid, loud `CausticTopologyError`); fold arcs probed at multiple interior thetas (near-axial F012 refusals cannot silently drop an arc); gamma bands bisected into TOPOLOGY-STABLE sub-bands (`band_caustic_structure` / `stable_gamma_bands`: edge+center detection, intersected theta bounds, merged conservative cusp windows; with `min_gamma_band = 1e-6` (the default), bisection continues to near-float resolution — dropped-sliver prior mass ~1e-6 fraction, negligible); the foot-of-normal constraint is satisfied by construction (C6): `eta_max = f_max * R_c` per arc at training time, with `f_max < 0.5` asserted, so no chart is ever skipped for curvature — the old skip-and-record branch is replaced by an assertion. Certified by `cogwheel/tests/test_lensing_caustic_cusps.py` (analytic cusp-root, closed-form caustic-inradius, foot-of-normal curvature value, and fold-orientation-guard certification for the estimators `surrogate_training.py` retired in favor of the analytic geometry cascade; shared with the geometry-engine row above). Certified by `cogwheel/tests/test_lensing_min_gamma_band.py` (`stable_gamma_bands` and `band_caustic_structure` threshold-discriminant invariants at explicit non-zero widths — the production default `min_gamma_band = 1e-6` means bisection continues to near-float resolution with negligible dropped prior mass (~1e-6 fraction), across both parities). FAR-FIELD TILING: the far-field stage partitions each parity's REACHABLE lens-mass range (capped where `w(f_hi, m)` reaches the parity's engine ceiling — astroid `_POSITIVE_W_CEILING`, saddle `_SADDLE_W_CEILING`) into log mass strata of fixed ratio `R = sqrt(f_hi/f_lo)` (`_mass_strata`), each stratum's whole `[w(f_lo, m_lo), w(f_hi, m_hi)]` band bracketed by one chart w-range (`_stratum_w_range`; whole-band containment is the serving contract); mass above the reachable top is recorded loudly as `beyond_w_cap`, never silently dropped. Each stratum's shear-frame y-support box is tiled into a uniform Cartesian grid of square tiles (`_farfield_tiles`); tile admission is a PER-THETA_C-COLUMN test (`_InteriorAdmission.admits_exterior`) probing each tile's inner rho edge across its theta_c span against the exact nearest-caustic distance, admitting only where every band gamma clears eta_max. The per-column test is required because the astroid caustic is a directional SPIKE, not a disk: at gamma = 0.9 the directional caustic radius runs ~0.9-1.5 over most angles but 5.69 at the cusp, so any single scalar exclusion radius is set by the spike and discards the ~98% of the annulus that is genuinely exterior. `TrainingConfig.max_farfield_regions` defaults to `None` (uncapped — the tiling itself bounds the count); an int caps the total admitted tiles with a loud truncation record. CUSP-EXCLUSION FILTER (`_exclude_near_cusp`, `_CUSP_EXCLUSION_DISTANCE = 0.35`): tiles whose nearest corner sits within 0.35 source-plane units of any cusp vertex are excluded from exterior training on BOTH parities — astroid cusps via `_cusp_source_angles` (positive parity) AND deltoid/saddle cusps via `_deltoid_cusp_source_angles` (D₂-folded source-plane angles, both lobes) — checked at band edges (gamma_lo/mid/hi) so a tile is dropped if ANY band-edge shear places a corner within the exclusion distance. The 0.35 distance is calibrated from the measured FARFIELD_KERNEL_SUM envelope turn-on (the old 0.2 admitted tiles that still failed the 1e-3 bar with eps ~ 0.076). Tiles within the exclusion window are served by the Pearcey arm / exact engine as designed. Certified by `cogwheel/tests/test_lensing_exterior_admission.py` (exclusion boundary: tiles just beyond the cut are admitted, tiles within are excluded; both-parity cusp angles covered; band-edge gamma check; delta_tau-based cusp source angles match geometry oracle). CAUSTIC-FIXED RADIAL COORDINATE (`_to_caustic_fixed`/`_from_caustic_fixed`, used by chart classification and the exterior admission test): on the astroid interior, the astroid exterior arm, and the saddle interior arm the coordinate is directional-MULTIPLICATIVE (`rho = \|y\| / _caustic_reach(gamma, theta_c)`); on the SADDLE (parity -1) EXTERIOR arm only, it is a scalar ADDITIVE offset (`rho = 1 + \|y\| - _caustic_reach`, giving `drho/d\|y\| = 1`) — a directional r_caustic is ill-posed there because the two disjoint deltoid lobes do not enclose the origin, so rays that miss both lobes have no enclosing directional radius to normalize by; it is therefore SCALAR by design, not by omission. `_box_region_labels` catches the named refusal errors around the box-centre coordinate map and returns `(None, None)`, so a chart whose box centre sits exactly on the `gamma = 1` parity wall records unknown image_count/parity instead of crashing chart construction. REGISTRATION GATE: each chart's held-out max-normalized envelope eps (`_heldout_eps`) is checked against a per-kind bar (`TrainingConfig.tube_eps_max = 5e-2`, `farfield_eps_max = 3e-3`); a chart above its bar, or with NaN eps (zero held-out points served), is recorded GATED in the training report and excluded from the packed artifact — its window is subdivided recursively where the chart kind has a subdivider (far-field, wedge, lobe; bounded by `MAX_SUBDIVISION_DEPTH`), and only a window whose subdivided children all fail falls through to the serving ladder rather than being silently registered; the gate decision round-trips through per-chart provenance on resume (`heldout_eps` persisted alongside the saved chart and read back rather than recomputed; charts from a trainer predating the gate carry a `legacy_no_eps` marker and pass through un-gated so a mixed-version resume never silently drops a previously-registered chart). Per-chart-file resumability; JSON training report with measured artifact size. CENSUS (`cogwheel/lensing/surrogate_census.py` + `scripts/census_lens_surrogate.py`): served fraction with a 7-way fall-through breakdown (gamma-guard / dropped-sliver / born / cusp-window / refusal-ball / out-of-box / ppgo_fold; near-cusp neighboring-arc projections categorize as out-of-box BY DESIGN; ppgo_fold is served, not a fall-through, but tracked by `characterize_sample` as a distinct category), per-chart held-out envelope eps in the max-normalized currency (F016) against a fresh-engine oracle (F002), lnL tiers on certified axes only (crown <= 0.05 nats — Professor-ratified override of the aspirational 0.01, dlnL ~ eps*SNR^2; strong-shear/saddle <= 0.1; rescued <= RB_ATOL = 1.5) with a band-edge sub-tier, and the measured RB-BINNING-FLOOR line (exact-RB at delta vs delta/4 — the artifact is delta-independent; owner-approved 2026-07-20). REGISTERED data product `lens_amplification_surrogate` (DATA_CONTRACTS + data_registry package_data root + LOADERS enrollment; consumer graph regenerated). The tube-vs-raw advantage is measured, not assumed: the >= 2-3x gain is against the EXTRAPOLATING far-field alternative (the production comparison); a fair same-band raw chart ties at fixture scale in the max-normalized currency (F018). Certified by `cogwheel/tests/test_lensing_surrogate.py` (multi-chart: selection determinism/no-overlap incl. negative-theta unwrap, serialization round-trip with provenance, single-box back-compat) and `cogwheel/tests/test_lensing_surrogate_census.py` (27 tests: hand-computed MECE breakdown, F002-guarded eps currency incl. trough robustness, three lnL tiers measured 0.0148/0.0008/0.0163 nats, both design falsifiables at the F018 bars, F010 bound-mutation flips). Full-box training + full census remain a deferred driver campaign (train once on the final engine + chart set). MARGINALIZED PATH (the production sampler configuration): `LensedMarginalizedExtrinsicLikelihood` folds the total amplification into the reused `MarginalizedExtrinsicLikelihood` machinery — data side as the image-summed, exactly-time-shifted, kernel-weighted contraction against the UNCHANGED fiducial weights (`e^{i w tau_a}` is an exact timeseries shift since `w` is linear in `f`), norm side as `\|F(w_b)\|^2`-scaled bin weights (valid under the lens-aware bin guard) — and hands sky/time/psi/phase/amplitude to the unmodified `CoherentScoreHM` (the higher-mode path; the 22-only lens-phase degeneracy is never assumed). Sampled space: 12 intrinsic + 7 standard lens params, extrinsics drawn from conditionals in postprocessing. Engine refusals propagate BEFORE the marginalization integral (never averaged inside the QMC; verified by call-count). The posterior's `d_luminosity` column is the PHYSICAL luminosity distance on both routes — `LensedIASPrior`'s sampled `d_hat` transforms to standard `d_luminosity` via `UniformLuminosityVolumePrior` (physical by construction of the transform and its Jacobian), and `LensedMarginalizedExtrinsicIASPrior`'s coherent-score-drawn `d_luminosity` blob column is likewise physical since both `(d\|h)` and `(h\|h)` already carry `F` (single authoritative statement in `cogwheel/lensing/waveform.py`; `F(w->0) = sqrt(mu_macro)`, F009); no post-analysis `sqrt(mu_macro)` rescaling is required or correct on either route. Registered pair: `LensedMarginalizedExtrinsicIASPrior` (intrinsic CBC + lens subpriors). Certified by `cogwheel/tests/test_lensing_marginalized_likelihood.py`; the absolute-normalization oracle (marginalized lnL vs importance-sampled plain likelihood) is a driver-run gate. | `cogwheel/lensing/prior.py` (`LensedIASPrior`), `cogwheel/lensing/posterior.py` (`LensedPosterior`), `cogwheel/lensing/marginalized_likelihood.py` (`LensedMarginalizedExtrinsicLikelihood`), `cogwheel/lensing/surrogate.py` (`LensAmplificationSurrogate`) |

### Key abstractions

Far-field surrogate coordinate contract: exterior charts — positive-parity
astroid and macro-saddle alike — are `ExteriorPolarChart` records,
interpolating in caustic-fixed polar `(rho, theta_c)` coordinates (axis-schema
tag `'exterior_polar_carrier_demod_v2'` (`_EXTERIOR_POLAR_AXIS_SCHEMA_CARRIER`) — the ONLY known tag; the retired
`'exterior_polar_rho_theta_c'` and `'exterior_polar_rho_u_v1'` schemas are dropped from the known set and
hard-refuse at load). Each `ExteriorPolarChart` additionally carries a `carrier_rate` field (float, default 0.0): the residual carrier-phase rate `k_chart` estimated from per-node unwrapped-phase slope; when nonzero, the stored envelope is demodulated by `exp(-1j*k_chart*w)` before spline fitting and re-modulated at serve, absorbing the ~1000x dynamic range of `|E(w)| ~ w^(-0.60)`. Positive-parity (`parity == 1`) exterior charts carry an
optional cusp-adapted `theta_to_u` map — the spline's 4th axis is then
`u = d**(2/3)`, `d` the angular distance to the NEAR caustic cusp (`0` or
`pi/2` in the D2-folded quadrant), the same gamma-universal cusp-reach scaling
the wedge and lobe charts use, absorbing the `d**(-1/3)` near-cusp divergence
in `dE/dtheta_c`; macro-saddle (`parity == -1`) exterior charts interpolate on
raw `theta_c` (no map). No arc-length map is needed. Unknown schema tags
hard-refuse at load. Macro-saddle exterior charts interpolate in the same polar
coordinates, with an additive scalar-reach `rho` (the two deltoid lobes do not
enclose the origin, so `rho = 1 + |y| - _caustic_reach` with `drho/d|y| = 1`).

- **`EventData`** (`data.py`) — strain + ASD for an event.
- **`WaveformGenerator`** (`waveform.py`) — wraps LALSimulation approximants
  (IMRPhenomXPHM / XAS / XODE). Note: uses IMRPhenomXP, *not* Pv2, due to
  phase-convention differences (LIGO-T1500602).
- **`Likelihood`** classes (`likelihood/`) — `CBCLikelihood`,
  `RelativeBinningLikelihood`, `Marginalized*`.
- **`Prior`** classes (`prior.py`, `gw_prior/`) — composable subpriors with
  sampled<->standard transforms, registered in a `prior_registry`.
- **`Posterior`** (`posterior.py`) — pairs a prior + likelihood, supports folding.
- **`Sampler`** subclasses (`sampling.py`) — wrap dynesty/nautilus/zeus/PyMultiNest;
  write `samples.feather` to run directories.
- Most stateful objects subclass `utils.JSONMixin` for JSON (de)serialization.

### External interfaces & dependencies

numpy, scipy, pandas (>=2.0), numba, lalsuite (lal, lalsimulation), gwpy, gwosc,
astropy, matplotlib, pyarrow; samplers dynesty, nautilus-sampler, zeus-mcmc,
pymultinest (optional). Docs: Sphinx + numpydoc + furo (`docs/source/`, Read the
Docs). Packaging: setuptools + setuptools_scm, GPL-3.0-or-later, Python >=3.9.

### Conventions

- Units: frequencies Hz, times GPS seconds, masses solar masses, distances Mpc,
  angles radians.
- Tests live in `cogwheel/tests/` (stdlib `unittest`), not a top-level `tests/`.
- `cogwheel/tests/conftest.py` bounds every FAST-TIER test at 900 s wall clock,
  so a test that stops terminating fails loudly and names itself instead of
  pinning a worker. The ceiling lifts entirely when any slow tier is requested
  (`COGWHEEL_BRUTE_ACCURACY`, `COGWHEEL_TRAIN_TIER`, `COGWHEEL_STRICT_TIMING`,
  `COGWHEEL_RUN_TIMING_SMOKE`), an explicit `--timeout` overrides it, and it is
  a no-op without `pytest-timeout` — the plugin is never a hard dependency.
  Motivated by F061: above `w = 60` one `f_schwinger` call costs ~85-120 s
  rather than ~0.2 s, and four such tests stranded a build by exhausting the
  tree gate's whole 3600 s ceiling without naming themselves.
- `largedata/` (top-level) holds large run outputs, injection HDF5, and detector
  `.gwf` frame files used by tutorials — not part of the installed package.
- Numerically hot paths (relative binning, coherent-score marginalization) use
  numba and lookup tables and must remain numerically accurate.
- **Born rung (carrier + wiring + trained residual chart shipped).** `chang_refsdal/_born.py` provides the analytic carrier for the
  exterior-to-caustic region (`rho > 1`, i.e. outside the caustic in
  caustic-relative coordinates), serving BOTH macro parities: positive parity
  (`det A = (1-kappa)**2 - gamma**2 > 0`) and macro saddle (`det A < 0`).
  The prior-box annulus `3.0 < |y| <= 4.2426` and the parity-specific outer
  fences (`gamma < 3/4` for positive parity, `saddle_caustic_max_y` for the
  saddle) were retired in C8 (F036); `rho > 1` is the sole exterior criterion
  on both parities. `_born_factors`
  returns the DERIVED, closed-form `b1`/`a0` (F023; a pure point mass gives
  `b1 = -1`, `a0 = 0`) and the parity-agnostic magnitude
  `sqrt(abs(det_a))`, feeding two consumers: `born_lead_carrier`, the
  lead-only serve object — `sqrt(mu_macro)*exp(1j*w*phi_geo)` at positive
  parity, with the exact Morse phase `-1j` applied on the macro saddle
  (F024/F009-S) — carrying NO `a0`/`b1` correction on either parity; and
  `born_amplification`/`born_envelope`, the positive-parity-only
  resolved-image macro-limit diagnostic that DOES carry them (the a0/b1
  correction is not derived on the saddle). `a0`/`b1` serve nowhere (F025):
  `a0` violates the exact `F(w->0) = sqrt(mu_macro)` limit (F009) and the
  resolved-image correction inflates the demodulated residual's azimuthal
  node count 2.5x-11x over the lead-only carrier. `born_gate` refuses via
  two guards: guard B, a two-sided parity-wall margin
  (`abs(gamma_p - 1) > DELTA_GAMMA_P` on `gamma_p = abs(gamma)/(1-kappa)`,
  catching degeneration from EITHER side of `det A = 0`); and guard A, the
  band split. `channels.
  born_carrier_from_partition` assembles the band-split carrier at
  `w * Delta_tau = RHO_END` (`Delta_tau` read from the partition's real-image
  delays, never recomputed, never `phi_geo`, never `w*r0_sq` — F024): below
  the split, the parity-agnostic lead-only carrier (`born_lead_carrier`
  applies the Morse phase internally); above the split, positive parity
  serves the two-real-image geometric-optics sum plus `farfield_ghost_term`
  where admitted (separation gate `min_a |x_a - x_c| >= _GHOST_SEPARATION_MIN = 0.7` and decay gate `Im(tau_c) >= _GHOST_DECAY_IM_THRESHOLD = 0.4` — a fixed constant guarding near-axis configs where the ghost is pure oscillation rather than an exponentially-decaying correction, F027), while the macro saddle serves the pure two-real-image
  geometric-optics sum with the complex ghost explicitly REFUSED (`det A <
  0` is not a regime `farfield_ghost_term`'s sqrt branch is derived for).
  The census (`surrogate_census.classify_fallthrough`) attributes both
  arms to the `'born'` category by checking `rho > 1` (exterior-to-caustic),
  independent of `born_gate` (no `w` available at census time). The
  fact-4 slot in `likelihood._surrogate_coefficients` is now wired: when
  a `BornResidualChart` (frozen 3-D interpolation dataclass in
  `cogwheel/lensing/born_residual_chart.py`) is attached to the likelihood
  object, the slot reconstructs `F_carrier + R(w; gamma, rho)` and returns
  the surrogate coefficient array. When the chart is `None` (default),
  exterior draws fall through to the exact engine — correct, just not
  zero-quadrature. A trained artifact `cogwheel/data/born_residual_chart.npz`
  (≈ 8 KB, produced by `scripts/train_born_residual.py`) is **shipped as
  package data** — a 3-D tensor-product cubic spline of
  `R(w; gamma, rho) = F_exact_demod(w) − F_carrier_demod(w)` over a
  7 gamma × 5 rho × 10 w sparse grid, all in the min-relative delay frame.
  Attaching a `BornResidualChart` instance (constructed from the shipped `.npz`) completes
  the zero-quadrature exterior serve path for `rho > 2`.
- **Lensing delay frame.** Every channel kernel in `chang_refsdal` is carried in
  the partition's *min-subtracted* frame: `ChangRefsdalPartition` subtracts
  `t_min = min(absolute real-image Fermat delays)` from every delay, so
  `partition.delays` always contains an exact `0`. Any term added to or
  subtracted from a sum of channel kernels MUST be carried in that same frame.
  The `geometry` primitives (`delay`, `ghost_kernel`, `_ghost_delay`) return
  RAW absolute delays and are deliberately frame-agnostic — `t_min` is a gauge
  choice belonging to the *set* of real images, not to any single saddle — so
  the min-subtraction is applied at the composition layer in `channels`, where
  `_frame_delays(source, matrix)` is the single authoritative construction of the
  frame origin (returning `(images, absolute_delays, t_min)` together so callers
  need not re-solve the image quartic; `_frame_t_min` is a thin accessor over it).
  Mixing the two frames is silent: it leaves magnitudes and decay
  rates correct while corrupting only the phase, so it is invisible to any
  amplitude-based check and must be pinned by frame assertions
  (`test_lensing_chang_refsdal_ghost_frame.py`).

## Constraints

- Numerical accuracy is paramount: relative-binning and marginalized likelihoods
  must agree with exact/brute-force references within tolerance.
- Phase/spin conventions must be respected across waveform and coordinate code.
- numba-accelerated code must stay numba-compatible.
