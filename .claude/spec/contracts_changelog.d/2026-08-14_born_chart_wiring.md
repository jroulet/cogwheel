---
date: 2026-08-14
bump: minor
---

`born_residual_chart`: the false "attaches at construction time" claim is
replaced by the shipped truth — `BornResidualChart.load()` (new: schema
`born_residual_v1`, content hash, JSON provenance; the npz re-saved with
byte-identical numeric payload) auto-attaches at
`LensedRelativeBinningLikelihood` / `LensedMarginalizedExtrinsicLikelihood`
construction (opt-out: explicit `None` = pure engine), consulted by the
first-class Born intercept in `_amplification_coefficients` (gate:
kappa==0, beta==0, gamma != 0, rho > 2, `covers(gamma, rho, chart_w)`),
band-split against the certified ppGO map. Coverage narrowed to the
artifact's truth: astroid parity only (gamma grid 0.05-0.9); saddle Born
nodes are a training-campaign decision. Build `wire_serving_artifacts`.
