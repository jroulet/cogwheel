---
date: 2026-08-18
section: Backlog
---

- **Born far-field completion RESOLVED across two builds — floor
  band-split shipped (+3.43% revived), carrier-only measured dead at
  every order, residual-chart representation settled** `[→ spec]` —
  builds born_farfield_completion (3f3c57d) + born_farfield_go_carrier
  (b50c6d4, first fully clean in-pipeline commit of the hardened SDK).
  Closure #3 SHIPPED: three-tier trained-floor band-split in
  _born_residual_analytic (engine below trained floor, chart residual
  on [trained_floor, w_trust], diffractive bottom, ppGO above) —
  in-build 10k census measured 3.430% of prior revived to
  born_analytic. Closure #1 MEASURED OUT, twice: the lead/corrected
  Born carriers are dead at any order (beat floor sqrt(|mu2|/|mu1|)
  29-137x over bar, corrected_carrier_remainder_probe.md), and the
  two-image GO carrier's admissible slice (<=2-3%, w_hi>15) is ALREADY
  served by the live c3 rung while >=75% of the far field (w_hi<15)
  fails both honest certificates — provably chart territory; no rung
  built, no dead code. Closure #2 SETTLED as the representation
  contract: annulus/far-field residual charts store against the
  two-image GO carrier (one definition, rung+chart DRY; lobe-gauge
  cusp-adapted coordinates on the saddle) — training and tiles belong
  to [[lensing_training_campaign]]. covers() is no longer a blanket
  refusal anywhere (certificate or band-split decides). Honest ledger
  after both builds: engine_residual ~41% (44.62 - 3.43), all of it
  named chart demand or the 2b corner.
