# Test Dev Short-Term Observations

PPGO CUSP FAST RUNG TESTS (2026-08-08): added 5 classes / 13 tests to test_lensing_airy_fold.py exercising the ppGO fast rung inside ``cusp_amplification`` (WP1). Fixtures: gamma=0.5, source at 0.20*(cos(pi/4), sin(pi/4)) (astroid) for positive-parity; gamma=1.2, source=[-0.5, 0.5] (saddle/deltoid). Key measurements: ppGO rung fires at w>=500 for astroid (radius=98>r_ppgo_min=54), w>=200 for saddle (radius=29 at w=100). ppGO rung delegates to ``fold_ppgo_correction`` (fold-corrected, not cusp-corrected) -- Pearson golden reference comparison is infeasible (12-36% difference at w where both paths serve). Constants ``_R_PPGO_ERROR_CONST=2.0`` is a known-provisional placeholder (post-build driver measurement to tighten). Tests cover: structural rung firing, finite/deterministic output, small-R refusal, w-floor gate, finiteness guard (NaN/Inf), saddle parity, self-falsification.

REMOVED golden-agreement comparison from Test 1 and Test 5 -- the fold_ppgo_correction applies a fold correction formula in a cusp context, so the ppGO rung value is NOT the asymptotic limit of the Pearcey uniform form. This is a design gap: the ppGO rung should use a cusp-corrected ppGO function, not fold_ppgo_correction. The current code serves ``_R_PPGO_ERROR_CONST=2.0`` as a provisional placeholder -- at the measured threshold the fold correction introduces O(36%) error relative to Pearcey. Documented in test class docstrings.
