Flipped FieldExposureTestCase.test_exterior_polar_uses_caustic_fixed_axes: assertNotIn->assertIn for theta_to_u on ExteriorPolarChart (now an optional nn.Array | None field from cusp-adapted coordinate WP). Docstring updated. 37/37 pass, 0 regressions. INS-3-003/004 fixes (2026-08-08): (1) test_lensing_farfield_envelope.py `_train_tile`/`_train_exterior_chart` hardcoded origin='low' -> new shared `_exterior_cusp_axis_map(theta_c_grid, gamma_band, n_gamma)` mirrors production `_build_farfield_chart` origin (waist at median log-spaced rep_gamma; falls back to (None,None) raw-theta when theta_c range outside [0,pi/2] or degenerate, so domain guard never fires from a fixture). STRADDLING stays low (0.7698<0.7766), EXTERIOR/OVERSIZED now high (0.7854>0.7766). (2) RESTORED missing `class DefinitionTagLoaderRefusalTestCase` (dropped in 0a31fcf polar re-chart): its 8 loader tests were silently absorbed into `_legacy_single_box_arrays` dead code; retired `test_legacy_single_box_known_tag_loads` (production now hard-refuses EVERY legacy single-box unconditionally at surrogate.py:3849). (3) INS-3-004 `_synthetic_exterior_polar_chart` sentinel block: added 3rd branch raising ValueError when exactly one of theta_to_u/u_grid real and the other sentinel (previously leaked _SENTINEL -> confusing TypeError); added SentinelAxisContractTestCase (6) + SentinelAxisContractSelfFalsification (2). PRE-EXISTING drift, not mine: WholeInteriorSacrc/InteriorWnpd/SelfFalsification reds (worktree un-skipped them but production `KNOWN_FARFIELD_DEFINITIONS` dropped `interior_sacr_c_envelope`). Added 6 test classes / 22 tests for ExteriorPolarChart cusp-adapted theta_to_u in surrogate training (test_lensing_surrogate_training.py): (1) BuildFarfieldPositiveParityCuspAdaptedTestCase (5 tests: theta_to_u shape, monotonicity, endpoint exactness); (2) BuildFarfieldHighSideCuspAdaptedTestCase (4 tests: high-side origin, monotonic, zero-start, endpoints); (3) BuildFarfieldCuspOriginSelfFalsificationTestCase (4 tests: correct/wrong theta_to_u differ, coefficients differ); (4) BuildFarfieldSaddleExteriorUnchangedTestCase (2 tests: parity=-1 theta_to_u=None); (5) SubdividedChildrenCuspAdaptedTestCase (2 tests: parity pass-through children); (6) BuildFarfieldCuspAdaptedSelfFalsification (5 tests: domain raises, non-uniform spacing, identity not byte-identical). 22/22 fast-tier + engine-backed pass all green.
Flipped FieldExposureTestCase.test_exterior_polar_uses_caustic_fixed_axes: assertNotIn->assertIn for theta_to_u on ExteriorPolarChart (now an optional nn.Array | None field from cusp-adapted coordinate WP). Docstring updated. 37/37 pass, 0 regressions.