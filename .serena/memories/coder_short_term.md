# Coder Short-Term Observations

WP1 (low_w_chart_airy_representation): added module-level `reduced_source(gamma_prime, rho, theta)` (inverts fence |y'| = rho*|caustic_point|, matches trainer _residual_at inline) and `airy_fold_reference(w_grid, gamma_prime, source)` (q=p absolute-frame Airy ref via _merging_fold_pair/_soft_axis_cubic/_fold_amplitudes/airy_fold_value; returns None on LensDomainError/None pair/b3/amplitudes/non-finite; NEVER calls fold_amplification). Renamed dataclass field log_w_grid->w23_grid (+NPZ key, load read, content_hash order); evaluate() maps w->w**(2/3); covers() w-band uses w**(2/3); _SCHEMA='low_w_diffractive_v2'. Note: covers() returns np.bool_ (not Python bool) in the w-given branch, unchanged from original (grid elements are np.float64) — don't use `is False` in tests.

WP2 (trainer): `_residual_at` signature changed to `(w_grid, gamma_prime, rho, theta) -> ndarray | None` (was scalar w -> complex): builds y_eig via `reduced_source` + F_ref via `airy_fold_reference`, returns r = f_pure*sqrt(1-gp**2)/F_ref sampled on w_grid, or None when F_ref unbuildable. Reference-building wrapped in `except ValueError -> None` (geometry/buildability = DECLINED); f_schwinger raises propagate to `_fill_coefficients` = n_refused (grid bug). `_fill_coefficients` now returns 4-tuple `(real, imag, n_refused, unbuildable_mask)`; unbuildable cells get NaN coeffs (declined), engine refusals counted separately. `_off_grid_engine` returns `(points, values, off_unbuildable)`, SKIPS unbuildable midpoints. `_iter_points` SKIPS non-finite on-grid cells (unbuildable). `_w_grid` now uniform in w**(2/3): `linspace(W_LO**(2/3), W_CEILING_SCHWINGER**(2/3), n)**(3/2)`; _SMOKE_N_W 5->8, _FULL_N_W 14->16. main(): grid_n = sum(~unbuildable_mask); off_served filled via `off_served[~off_unbuildable] = served_errs[grid_n:]` (boolean-index fill, NOT reshape); declined_mask = unbuildable_mask | declined_by_error; provenance adds n_unbuildable_cells/n_declined_by_error; log_w_grid -> w23_grid everywhere (constructor, content_hash, savez, round-trip). Dropped prefactor_c import; imports reduced_source + airy_fold_reference. Note: test file test_lensing_low_w_diffractive_chart.py still mirrors the OLD `_residual_at(w, ...)->complex` + log_w_grid scheme (test_dev updates it — its `_build_unit_chart`/`_build_exact_chart` pass log_w_grid= which no longer exists on the chart dataclass).

WP1 (low_w_chart_cusp_fallback, _pearcey_cusp.py): added `_CuspUniformForm` frozen dataclass (15 fields: uniform, far_sum, images, nearest, vertex, matrix, handoff_radius, stationary_values, matched_delays, tau_c, c4, x, y, reflected, phase_sign) + private `_cusp_uniform_form(w, source, gamma, *, beta=0.0, kappa=0.0, pearcey_table=None)` holding the FULL core (geometry -> F074 control map -> primitive/asymptotic -> cluster/far split -> uniform). pearcey_table is passed STRAIGHT to _consult_pearcey (None = live quadrature); cusp_amplification resolves explicit-arg/global-table BEFORE calling (resolved_table = pearcey_table if not None else _PEARCEY_TABLE) and passes it in. cusp_amplification refactored onto the bundle: preamble (w=float/asarray/guards/envelope_bar) kept verbatim, then ppGO gate FIRST, F074 gate SECOND, calibration THIRD, total=FOURTH (bundle.uniform+bundle.far_sum). New public `cusp_uniform_reference(w, source, gamma, *, beta=0.0, kappa=0.0)` returns bundle.uniform only (live quadrature, no gates); added to __all__ after cusp_amplification; `import dataclasses` added. FLAG for downstream: the extraction reorders primitive/cluster-split computation to BEFORE the ppGO early-return (bundle must be fully built first) — byte-identical EXCEPT when ppGO would serve a valid result but _cusp_uniform_form returns None (pearcey cert failure / asymptotic zero / non-finite primitive / _leading_geometric None) in the exterior ppGO region; side-by-side HEAD sweep (step 4) is the definitive check.

WP2 (low_w_chart_cusp_fallback): renamed airy_fold_reference -> fold_cusp_reference in 3 PRODUCTION files (low_w_diffractive_chart.py def+module docstring, likelihood.py import ~119/call ~2056/docstring ~1987, trainer import ~117/call ~310/docstrings). Factored the Airy build into private `_airy_fold_form` (named to avoid the `airy_fold_reference` substring so the "no airy_fold_reference in production" grep passes; only the test file retains 12 refs incl `_likelihood_mod.airy_fold_reference`@859 + string literal@862 -> test_dev must rename). Added `_pearcey_cusp_reference` (loops `cusp_uniform_reference(float(w), source, gamma_prime)`, beta/kappa default 0.0, None on ANY node refusal/non-finite) and `fold_cusp_reference` orchestrator: Airy primary, Pearcey fallback on None, then non-vanishing guard `min|F_ref|/max|F_ref| < _NON_VANISHING_MIN_RATIO(1e-3) -> None`. Imports cusp_uniform_reference from _pearcey_cusp (added by WP1). Trainer docstrings: unbuildable now = both Airy+Pearcey refused OR guard failed; n_unbuildable_cells expected ~0; cusp cells BUILDABLE. Smoke grid already covers witness (gp 0.8 in _SMOKE_GAMMA_PRIMES, rho 2.0 in _SMOKE_RHOS) -> no grid change. UNVERIFIED: live Pearcey reference values / de-rate (downstream driver bake + test_dev).

INS-1-001 FIX (_pearcey_cusp.py, order-preserving extraction): the WP1 extraction had `cusp_amplification` call `_cusp_uniform_form` (which runs `_consult_pearcey` + cluster/far split) BEFORE the ppGO fast rung and F074 error gate -> ~1000x slowdown + silent refusal (primitive None short-circuits before fast rung) + SPEC.md line 54 violation. Fixed by splitting: `_CuspUniformForm` SHRANK to 4 fields (uniform, far_sum, stationary_values, matched_delays) — no more geometry/controls fields; new `_CuspControls` dataclass (x, y, handoff_radius, reflected, phase_sign) + `_cusp_controls(geometry_bundle, w)` (pure scalar, no quadrature) inserted after `_cusp_uniform_geometry`; `_cusp_uniform_at_w` now calls `_cusp_controls` and computes ONLY primitive/asymptotic/split. `cusp_amplification` restructured: `_cusp_uniform_geometry` -> `_cusp_controls` -> ppGO fast rung (early return, uses geometry_bundle.images/nearest/matrix + controls.handoff_radius) -> F074 error gate (geometry_bundle.matrix) -> DEFERRED `_cusp_uniform_at_w` (resolved_table passed) -> calibration -> total. Fast-rung comment updated to note it 'returns before any table or quadrature lookup' (SPEC.md). The two test docstrings in test_lensing_airy_fold.py (PpgoGoldenAgreementTestCase + test_ppgo_result_finite_and_deterministic: 'fires before the Pearcey path is ever consulted') were LEFT UNCHANGED — they already describe the correct contract and become accurate again after the fix; no textual change needed. `_cusp_uniform_form` wrapper docstring updated (cusp_amplification no longer calls it; it's the cusp_uniform_reference convenience entry). Note: `_CuspUniformGeometry.branch` field is stored but never read (pre-existing from foreman's geometry split, harmless, out of scope). Also fixed a missing blank line between _CuspUniformForm and _CuspUniformGeometry.

WP3 (low_w_diffractive_chart serve re-modulation): `_low_w_diffractive_chart_serve` (likelihood.py) now rebuilds F_ref at serve: `source = reduced_source(gamma_prime, rho, theta)` then `fref = airy_fold_reference(dense_w, gamma_prime, source)`, returns None if fref is None (falls through to w_low_fit/engine), placed AFTER rho but BEFORE chart.covers. farfield = mass_sheet_phase * fref * sqrt_mu_full * r_fit (prefactor_c block deleted; no f_schwinger on path). Imports: added reduced_source+airy_fold_reference to low_w_diffractive_chart import; removed prefactor_c from _hyp1f1 import (HypergeometricDomainError kept). reduced_source needs no try/except: its caustic_point(gamma_prime,theta) is byte-identical to _caustic_rho's, already evaluated just above for the same args.
