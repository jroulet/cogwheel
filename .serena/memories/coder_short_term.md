# Coder Short-Term Observations

- INS-w-001 fix: Added `InteriorWedgeChart` to the isinstance tuple in `LensAmplificationSurrogate.__init__` (line ~3228) and updated error message to list all four chart types. Without this, `from_wedge_engine` and `load()` would crash with ValueError when constructing a surrogate containing wedge charts.

- WP2 serve-path dispatch + from_wedge_engine: In `cogwheel/lensing/surrogate.py`: (1) Added `_wedge_serves(chart, gamma, log_w_min, log_w_max, eta, image_count, y1_eig, y2_eig)` gate function (after `_lobe_serves`, line ~2978) with gate order: (a) isfinite precondition, (b) gamma box, (c) log-w band, (d) _to_wedge_fixed fold + ValueError catch, (e) (r, theta_wedge) box containment, (f) exclusion ball, (g) image-count guard, (h) eta floor. (2) In `select_chart`: added fourth scan loop `for chart in charts: if isinstance(chart, InteriorWedgeChart) and _wedge_serves(...)` AFTER the lobe loop, before return None. Priority: tube > farfield > lobe > wedge. (3) In `_evaluate_chart`: added `elif isinstance(chart, InteriorWedgeChart)` branch that calls `_to_wedge_fixed(gamma, y1_eig, y2_eig, chart.wedge_map)`, optionally remaps theta_wedge→s via theta_to_s, then contracts spline with (gamma, r, v2). (4) `from_wedge_engine` classmethod on `LensAmplificationSurrogate` (after `from_lobe_engine`, line ~3693): builds gamma_grid via _log_reach_gamma_axis, r/theta_wedge grids via _uniform_axis, builds wedge_map with 101 dense theta nodes, runs nested training loop calling _from_wedge_fixed → ChangRefsdalChannels.evaluate, stores envelope/carrier, runs _assert_carrier_continuity, builds chart via InteriorWedgeChart.from_wedge_values. (5) `_build_wedge_provenance` staticmethod (after `_build_lobe_provenance`, line ~3952): stamps axis_schema=_WEDGE_AXIS_SCHEMA, r_range, theta_wedge_range. (6) Updated `_in_exclusion_ball` type annotation to include `InteriorWedgeChart`.


- WP1 InteriorWedgeChart: added to `cogwheel/lensing/surrogate.py`: (1) `_WEDGE_AXIS_SCHEMA` + `_KNOWN_WEDGE_AXIS_SCHEMAS` constants (line ~293); (2) `_WedgeCausticMap` frozen dataclass (after `_FarFieldArcMap`, ~line 517) with fields gamma_nodes, theta_nodes, r_table; (3) `_validate_wedge_caustic_map` validation function (after `_validate_farfield_arc_map`, ~line 1468) enforcing finite/positive/increasing/shape/endpoint constraints; (4) `_interp_r_caustic` bilinear interpolation helper (after `_to_lobe_fixed`, ~line 1235) — interps along gamma first then theta using np.interp; (5) `_to_wedge_fixed(gamma, y1_eig, y2_eig, wedge_map)` folds into canonical first quadrant via abs(), computes theta_wedge=atan2(|y2|,|y1|), normalises by interpolated r_caustic; (6) `_from_wedge_fixed(gamma, r, theta_wedge, wedge_map)` exact inverse for training; (7) `InteriorWedgeChart` frozen dataclass (after `LobeInteriorChart`, ~line 2388) with `from_wedge_values` classmethod (fits tensor spline, handles theta_to_s/s_grid pattern) and `_assemble` classmethod (no re-fit, for NPZ load); (8) `_chart_to_npz` new `elif isinstance(chart, InteriorWedgeChart)` branch stores kind='wedge', wedge_gamma_nodes/theta_nodes/r_table arrays, optional theta_to_s; (9) `_chart_from_npz` new `if meta['kind'] == 'wedge'` branch validates schema against `_KNOWN_WEDGE_AXIS_SCHEMAS`, rebuilds `_WedgeCausticMap`, calls `InteriorWedgeChart._assemble`. Round-trip verified: coordinate transforms exact to ~1e-16, NPZ save/load preserves coefficients bit-for-bit.

- INS-c8-001 fix: In `cogwheel/tests/test_lensing_ppgo_bandsplit.py`, added import of `_airy_fold as _airy_fold_module` from `cogwheel.lensing.chang_refsdal` and added `mock.patch.object(_airy_fold_module, 'fold_ppgo_correction', _stub_amplification)` to `TruncationOnRefusalTestCase.setUpClass` patchers list. This ensures the deferred import of `fold_ppgo_correction` inside `_measure_cell`'s `evaluate` closure picks up the stub (returns ones) instead of the real fold correction which calls real geometry functions.


- WP2 wire fold_ppgo_correction: (1) In `_measure_cell` (ppgo_map.py ~line 1103), replaced `geometric_amplification(w_prefix, source, gamma, beta=0.0, kappa=kappa)` with `fold_ppgo_correction(w_prefix, source, gamma, beta=0.0, kappa=kappa)` via a deferred import inside the `evaluate` closure. The demodulation `ppgo * exp(-1j * w_prefix * t_min)` unchanged (fold_ppgo_correction returns in the same absolute-delay frame). Kept the `geometric_amplification` import in the function preamble as per WP instruction. (2) In `born_carrier_from_partition` (channels.py ~line 1575), added an additive fold correction block in the above-split non-saddle else-branch, AFTER `carrier[above] = ppgo_plus_ghost[above]`. The correction computes (airy_values - pair_ppgo) * exp(-1j*w_above*t_min) using the same structural gates as fold_ppgo_correction (_merging_fold_pair, _soft_axis_cubic, _fold_amplitudes, _image_at_delay, nearest_caustic_point, airy_fold_value). On any structural refusal (pair not found, degenerate fold, LensDomainError, ValueError, ZeroDivisionError), the correction is silently skipped and carrier stays byte-identical to the uncorrected path. Non-finite airy values are guarded with np.where(finite_mask, ...).


- WP1 fold_ppgo_correction: added `fold_ppgo_correction(w, source, gamma, *, beta=0.0, kappa=0.0)` (line ~473) in `cogwheel/lensing/chang_refsdal/_airy_fold.py`. Structural-gates-only design (no error-estimate or ETA_MAX gate, per DO-NOTHING control). Falls back to raw `geometric_amplification` on any gate miss. Uses lazy import of `geometric_amplification` from `operator` to avoid circular import (`operator` imports `_airy_fold` at module level). Also relaxed `_uniform_error_estimate`'s guard from `if not (xi > 0.0): return None` to two lines: `if xi < 0.0: return None` + `if xi == 0.0: return 0.0` (Airy is exact on the fold). Added to `__all__` and to `__init__.py` import.

- INS-2-001 fix: `_subdivide_farfield_tile` (line ~3655) in `cogwheel/lensing/surrogate_training.py` had same stale ternary eff_w_nodes pattern as the main tiler. Applied identical 3-way if/elif/else: tile override -> interior/lobe_interior uses config.interior_w_nodes_per_decade -> else config.w_nodes_per_decade. Mirrors the pattern at line ~4327 in _train_band_charts.


- WP1 interior_w_nodes_per_decade: added `interior_w_nodes_per_decade: int = 15` field (line ~269) to `TrainingConfig` in `cogwheel/lensing/surrogate_training.py`. Modified `_train_band_charts` eff_w_nodes logic (line ~4324-4329) from a ternary to a 3-way if/elif/else: tile override -> interior/lobe_interior uses config.interior_w_nodes_per_decade -> else config.w_nodes_per_decade. Changed both `build_lobe` and `build_ff` closures' default-arg binding from `w_nodes=tile_w_nodes` to `w_nodes=eff_w_nodes` so the resolved integer (not None) is passed to the chart builders. Report `n_w_per_decade` was already reading `eff_w_nodes` — no change needed there.


- WP1 ppGO interior-cell extrapolation: added `_extrapolate_floor` (line ~875) and 5 `_EXTRAP_*` constants (line ~197) in `cogwheel/lensing/ppgo_map.py`. Modified `_measure_cell` to store `per_angle_data` list, run extrapolation fallback for interior cells (rho_center < 1.0), and relax the `floor > w_ceiling` guard for interior cells only. Exterior cells are byte-identical to HEAD.
