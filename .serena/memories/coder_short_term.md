# Coder Short-Term Observations

INS-2-002 FIX (declined_mask excluded from content hash): the earlier INS-1-001 fix deliberately left declined_mask OUT of _content_hash (7-arg) to avoid breaking the then-7-arg test helper, but the Test Dev since moved _save_chart_artifact to hash 8 fields INCLUDING chart.declined_mask, so production was the laggard (load() + train script hashed 7 fields). FIXED both production sites to hash the 8th field in IDENTICAL order (gamma_prime_grid, rho_grid, theta_grid, log_w_grid, real_coeffs, imag_coeffs, derate, declined_mask): low_w_diffractive_chart.py load() `actual = _content_hash(..., derate, declined_mask)` + scripts/train_low_w_diffractive_chart.py `content_hash = _content_hash(..., chart.derate, chart.declined_mask)`; updated _content_hash docstring (mask folded to float64 0.0/1.0 bytes; un-declining near-fold resonance cells is the correctness breach the hash must catch). VERIFIED: 8 LoadContract + LoadContractSelfFalsification tests pass (round-trip bit-identical, rehashed-tamper-loads-cleanly positive controls, tampered-declined-mask-hard-refuses now non-vacuous), and a direct smoke confirms valid round-trip loads + tampered all-False mask refuses with the train_low_w_diffractive_chart.py message. Lesson: don't leave a hashed-artifact field out of the hash "to avoid breaking tests" — the Test Dev's helper is the intended contract; match production to it in the same pass.


INS-2-001 FIX (rho rebind corrupting caustic_rho field): in serve_route_census.classify_draw's Rung-P chart-consult block, `rho = mods.caustic_rho(abs(gamma_prime), s, theta)` REBOUND the outer closure-captured SCALAR `rho` (`_caustic_rho_or_none(gamma, abs_y)` = ppgo_map.caustic_rho gauge), so every subsequent fall-through `_result(...)` for that draw recorded the DIRECTIONAL `_diffractive._caustic_rho` value in the `caustic_rho` field consumed by residual_demand's 3-way split (gauges differ ~1.45-6.2x -> silently corrupted buckets). FIX: fresh local `rho_dir = mods.caustic_rho(abs(gamma_prime), s, theta)` passed to covers()/declined(); outer scalar `rho` untouched. Re-derived the two coupled tests in test_lensing_low_w_diffractive_chart.py: added import `_caustic_rho` from _diffractive + helper `_witness_directional_rho()` (computes rho_dir = _caustic_rho(0.3, s, theta) directly from NEAR_FOLD_WITNESS_Y1/Y2); `test_near_fold_shell_witness_routes_to_chart` premise now asserts rho_dir in [RHO_LO, RHO_HI] (not res.caustic_rho); `test_census_consults_chart_covers` asserts spy rho == _witness_directional_rho() (not res.caustic_rho). NOTE the witness's scalar rho (1.2472) ≈ directional rho (1.2473) to 1e-4 here (theta≈90° so the two gauges nearly coincide) — the field-stays-scalar invariant is what matters, not the numeric coincidence. VERIFIED: py_compile clean; the 2 re-derived tests pass; all 4 CensusMirror tests pass; smoke confirms res.caustic_rho == scalar (1.247219...) and route still 'low_w_diffractive_chart'.


INS-1-001 FIX (amplitude-de-rate vs 1e-4 bar contradiction): removed `_HARD_DERATE_CEILING=0.85` from scripts/train_low_w_diffractive_chart.py (the 0.85 clamp made every serve 15% low even at perfect interpolation, ~1500x the 1e-4 bar). derate = min(1.0, 1/max_overshoot) — no hard ceiling, so a no-overprediction grid bakes derate=1.0 and served error == raw interp error. `_node_metrics` refactored onto shared `_iter_points` generator; added `_served_errors(..., derate)` computing two-sided |derate*r_interp - r_engine|/|r_engine| sup over w. `_margin_report` now takes (ratios, raw_errs, served_errs) and counts n_within_bar on SERVED error vs CERTIFICATION_BAR over the FULL grid (grid + off-grid combined `full_stats`, plus grid/off-grid split). NEW per-cell decline mask: LowWDiffractiveChart gained `declined_mask` (3-D bool (n_gp,n_rho,n_theta), default empty (0,0,0)) + `declined(gamma_prime,rho,theta)` method (theta D2-folded, searchsorted side='right' -1 clamped to node index); load() reads optional `declined_mask` key (absent -> all-False shaped (n_gp,n_rho,n_theta)); mask NOT hashed (kept `_content_hash` 7-arg to avoid breaking test helper `_save_chart_artifact` — flagged to Test Dev). Training main() bakes decline mask: off-grid served errors reshaped (n_gp,n_rho,n_theta-1), node i_theta declined if max(adjacent midpoint served err, uniform bias 1-derate) > CERTIFICATION_BAR. likelihood `_low_w_diffractive_chart_serve` adds `if chart.declined(gamma_prime,rho,theta): return None` after covers check (formula unchanged). serve_route_census classify_draw chart consult adds `and not mods.low_w_chart.declined(...)` (declined cell -> fall through to w_low_fit -> near-fold shell lands engine_residual). SMOKE-VERIFIED: chart round-trip + declined() True at flagged node, absent-mask all-False, census decline=True -> diffractive_analytic / decline=False -> low_w_diffractive_chart. NOTE: declined-cell census outcome is diffractive_analytic for a wall-band draw (w_low_fit admits), engine_residual only for the near-fold shell — faithful production mirror, not a blanket engine_residual.


WP1 (diffractive_wall_nearfold_chart) shipped LowWDiffractiveChart in cogwheel/lensing/low_w_diffractive_chart.py (new module). Frozen dataclass, 4-D residual chart over (gamma_prime, rho, theta, log_w); variadic _content_hash(*arrays) includes derate as bare float; RHO_LO/RHO_HI single-sourced from _diffractive fence constants (0.6 / 1.4); _WALL_GAMMA_PRIME=0.5; covers() = box AND union((RHO_LO<=rho<=RHO_HI) OR gamma_prime>_WALL_GAMMA_PRIME) AND w-band; evaluate() theta D2-folds (|theta| mod pi -> pi-theta if >pi/2) then lazy cubic RGI returning complex. NOTE: task text is inconsistent on RHO_LO/RHO_HI vs _RHO_LO/_RHO_HI — chose PUBLIC RHO_LO/RHO_HI (matches covers-predicate + How section); _WALL_GAMMA_PRIME private. RGI cubic needs >=4 pts/dim (synthetic smoke grids used 4+). derate read via data['derate'] default 1.0 if absent. No mpmath/f_schwinger import in the module (imports _diffractive for fence consts only — transitive _schwinger load is fine per no-call-not-no-import).

WP3 (diffractive_wall_nearfold_chart, likelihood serve wiring): wired LowWDiffractiveChart into LensedRelativeBinningLikelihood mirroring the born chart exactly — `_AUTO_LOW_W_CHART` sentinel + `low_w_diffractive_chart=_AUTO_LOW_W_CHART` kwarg; __init__ auto-loads `LowWDiffractiveChart.load()` refuse-to-None (OSError/ValueError/KeyError -> RuntimeWarning naming scripts/train_low_w_diffractive_chart.py); `_low_w_diffractive_chart_is_default` recorded; get_init_dict three-way round-trip (drop on default / emit None on explicit None / raise NotImplementedError on in-memory). Added `_low_w_diffractive_chart_serve(lens, dense_w, geom)`: chart None->None; `_reduced_shear` in try/except DiffractiveDomainError->None; `gamma_prime==0.0` guard (degenerate caustic, `_caustic_rho` would ZeroDivision) -> None; s=|y'|^2 with `not s>0` guard; theta=atan2(z_eig.imag, z_eig.real) via cmath.exp(-1j beta); rho=_caustic_rho(abs(gamma_prime), s, theta); covers(gamma_prime, rho, dense_w)->None on miss; r_fit=chart.evaluate(...)*chart.derate; F = mass_sheet_phase * prefactor_c(w) * sqrt_mu_full * r_fit where sqrt_mu_full=_born_factors(y1,y2,gamma,beta,kappa)[0], mass_sheet_phase=np.exp(0.5j*dense_w*(log(lam)-kappa*s)), prefactor_c vectorized via list-comp over dense_w (imported from _hyp1f1). Reconstruct tail byte-identical to _low_w_diffractive_serve (FARFIELD_DIFFRACTIVE). Wired at TOP of gamma<1.0 Rung-P branch (before w_low_fit split). NO f_schwinger call on serve path (grep: only comments). Re-modulation consistent with _engine_reference_kappa: f_pure/lam = prefactor_c*sqrt_mu_full*r_pure since sqrt_mu_full=1/lam*1/sqrt(1-gamma'^2). NOTE: artifact not yet shipped (WP2 training script absent) so load() refuses -> None at construction (RuntimeWarning) until WP2 lands.

WP4 (census 12th SERVE_ROUTE low_w_diffractive_chart, this build): added draw-level 'low_w_diffractive_chart' (12th entry, EMPTY node kinds, no ROUTE_KIND) to serve_route_census.py SERVE_ROUTES, before 'diffractive_analytic'. _ProductionModules gained low_w_chart (LowWDiffractiveChart.load() in try/except (OSError,ValueError,KeyError)->None, mirrors born_chart) + reduced_shear=_reduced_shear + caustic_rho=_caustic_rho (both from _diffractive, engine-free). classify_draw intercept 6 Rung-P (gamma<1.0, after 2-image + w_lo<w_floor guards) consults chart FIRST: gamma'=gamma (kappa=0 via mods.reduced_shear(gamma,0.0), wall DiffractiveDomainError caught -> fall through), guard gamma_prime!=0 and s>0 (mirrors _low_w_diffractive_chart_serve), theta=atan2(y2,y1), rho=mods.caustic_rho(abs(gamma_prime),s,theta) (DIRECTION-DEPENDENT, NOT _caustic_rho_or_none), then mods.low_w_chart.covers(gamma_prime, rho, w_grid) -> _result('low_w_diffractive_chart', ()); else falls through to w_low_fit->diffractive_analytic byte-for-byte. serve_routes_decision_order header updated to 'low_w_diffractive_chart|diffractive_analytic|diffractive_engine_hosted' at Rung-P position. residual_demand/aggregate_cells need NO change (key off SERVE_ROUTES / 'engine_residual' filter dynamically). SMOKE-VERIFIED: module imports, 12 routes, classify_draw with fake covers=True chart returns 'low_w_diffractive_chart' with rho matching _caustic_rho(gamma,s,theta) to 1e-15; chart None (artifact not shipped) falls through unchanged; gamma==0.0 no crash. Existing test file test_lensing_serve_route_census.py derives from src.SERVE_ROUTES dynamically (no hardcoded 11-count) -> no laggard. Test Dev still needs coverage for the new route.

WP2 (train_low_w_diffractive_chart script, this build): created scripts/train_low_w_diffractive_chart.py. Oracle r_pure = f_schwinger(w, y_eig, gp)*sqrt(1-gp^2)/prefactor_c(w); node source inverted from the fence discriminator (|y'| = rho*|caustic_point(gp,theta)|, y_eig=|y'|(cos theta, sin theta)) — NO _reduced_shear/_rot_minus_beta needed (chart is pure reduced frame, kappa=beta=0). Grid: gamma' [GAMMA_LO=0.05, 1-DELTA_GAMMA_P=0.995] full (smoke [0.3,0.5,0.8,0.9]); rho full = [min(RHO_LO, 0.8*lo), max(RHO_HI, 1.1*hi)] from MEASURED wall-band 2-image rho spread (measured [0.0775, 7.6115] over source prior box, seed 42) — never a literal; theta [0,pi/2] 8 smoke/16 full nodes (8 = harmonic Nyquist, _N_THETAS=32 lesson); log w [log 0.02, log 60]. De-rate = min(0.85, 1/max_overshoot), overshoot=|r_interp|/|r_engine| sup over grid + off-grid theta midpoints; derate stored + hashed (7th arg to _content_hash, as float). Bake np.savez keys mirror load(): schema/content_hash/gamma_prime_grid/rho_grid/theta_grid/log_w_grid/real_coeffs/imag_coeffs/derate/provenance; round-trip via LowWDiffractiveChart.load(path) verified. smoke->tempdir artifact, full->shipped path. FLAG for Inspector/Professor (NOT redesigned, binding WP spec): scalar MAGNITUDE de-rate is dominated by the single worst off-grid overshoot; smoke w-grid (5 nodes) under-resolves the w-oscillation so de-rate crashed to 0.05-0.15 (median ratio ~0.06-0.2) — full bake (14 w nodes) expected to drive it toward 0.85, but the (1-derate) systematic magnitude bias is in TENSION with the handoff's "agree <= 1e-4" acceptance; also w-oscillation at large s (rho~8, s~16) needs >>14 log-w nodes, so the far-exterior wall band may be interpolation-limited.
