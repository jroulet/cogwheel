Last build: INS-1-001 fix. In _subdivide_lobe_tile build_child, changed _build_lobe_chart(..., w_nodes_per_decade=w_nodes) to w_nodes_per_decade=eff_w_nodes — lobe-interior children now get config.interior_w_nodes_per_decade instead of config.w_nodes_per_decade, matching the wedge subdivider pattern.. Added _LOBE_CUSP_EXCLUSION_DISTANCE=0.1 constant (documented, sized downward from _CUSP_EXCLUSION_DISTANCE=0.2). Professor ruling recorded in _SaddleLobeAdmission.admits: existing eta_max tube-shell exclusion already handles near-cusp-vertex rejection because cusp vertices are in caustic_cloud. No explicit carve-out needed.. Added _lobe_child_boxes + _subdivide_lobe_tile wrapper over _subdivide_tile. Threaded lobe key in child tile dict. Wired into _train_band_charts lobe branch for subdivision instead of immediate ladder_served_gap. WP-4 ppGO above-ceiling rung. Added _ppgo_above_ceiling method + intercept in _amplification_coefficients. Imports W_CEILING_SCHWINGER_QD (150.0) from _schwinger and RHO_END (4.0) from operator. Gate: w_max > 150 AND min_delta_tau > 0 AND w_lo*min_delta_tau >= 4.0. whole-band serve via fold_ppgo_correction + reconstruct_farfield(FARFIELD_KERNEL_SUM). LensDomainError propagates unswallowed. Non-finite guarded. Fallthrough → SchwingerCertificationError unchanged.