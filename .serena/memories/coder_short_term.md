# Coder Short-Term Observations

## 2026-08-21 (low_w_shell_born_extension WP4 — shell trainer + Born grid extension)
- Created `scripts/train_low_w_shell_chart.py`: bakes the 4-D macro-lead RAW-difference residual `R = f_schwinger(w, y_eig, gp) - born_lead_carrier(w, y_eig[0], y_eig[1], gp)` (NO t_min demod — both reduced frame; Professor Q1). Grid: gamma_prime in `[GAMMA_LO=0.05, 1-DELTA_GAMMA_P]` (smoke linspace / full geomspace-dense-near-wall), rho `np.linspace(RHO_LO=0.6, RHO_HI=1.4)`, theta D2 `[0, pi/2]` (smoke 8 / full 16), log_w `np.geomspace(W_LO=0.02, W_HI=1.0)` (smoke 6 / full 16). `reduced_source` single-sourced from low_w_shell_chart. NO derate/declined_mask (shell residual smooth O(1), measured |r|~0.61-1.6). f_schwinger refusal -> SystemExit naming the node (fail-fast, never NaN). Save via LowWShellChart + `_content_hash(gamma_prime_grid,rho_grid,theta_grid,log_w_grid,real,imag)` (SAME order as load) + schema `low_w_shell_v1`; round-trip load self-check. `--scale smoke|full`, `--n-w`, `--output`.
- Extended `scripts/train_born_residual.py`: rho_grid `np.linspace(2,4,5)` -> explicit `[1.4,1.6,1.8,2.0,2.5,3.0,3.5,4.0]` (3 new nodes in [1.4,2.0], top resolution preserved); w_grid `np.geomspace(5,60,10)` -> `np.geomspace(0.4,60,13)` (3 extra low-w nodes, stops at ~0.4 NOT 0.02 — shell owns deep low-w at rho<=1.4, Professor Q4). Added provenance `driver_prerequisite` (azimuthal sweep at rho=1.4 must confirm N(theta)<=8 before re-bake; if >8 don't re-bake this theta-fixed chart below ~1.6, extend shell rho coverage upward) + matching code comments + docstring grid update.
- `git rm scripts/train_low_w_diffractive_chart.py` (was broken-by-design importing the WP1-deleted module).
- VERIFIED (sanity): py_compile clean both scripts; import round-trip (RHO_LO=0.6 RHO_HI=1.4 DELTA_GAMMA_P=0.005 schema low_w_shell_v1); `--help` OK; single-cell formula check (gp=0.5,rho=1.0,theta=pi/4): |carrier|=1.1547 w-independent, R finite O(1) (|R| 0.057@w=0.02 -> 0.88@w=1.0, no poles). Full smoke bake + round-trip = downstream (I don't run measurement campaigns).

## 2026-08-21 (low_w_shell_born_extension WP1 — LowWShellChart + kill the quotient chart)
- Created `cogwheel/lensing/low_w_shell_chart.py`: frozen `LowWShellChart` (4-D macro-lead demodulated-difference residual over gamma_prime/rho/theta/log_w), schema `'low_w_shell_v1'`, `_DEFAULT_CHART_NAME='low_w_shell_chart.npz'`, variadic `_content_hash` (NO derate/declined_mask — dropped per spec), `covers(gamma_prime,rho,theta,w=None)` with D2 theta fold + band containment, `evaluate(w,gp,rho,theta)` 4-D cubic (lazy `object.__setattr__` cache), `load`/`_default_artifact_path` naming `scripts/train_low_w_shell_chart.py`. Moved `reduced_source` + `_reduced_min_delay_separation` VERBATIM; `RHO_LO=_DIFFRACTIVE_FIT_FENCE_RHO_LO`, `RHO_HI=1.0+_DIFFRACTIVE_FIT_FENCE_DELTA` (imported from _diffractive, never re-typed). NO `born_lead_carrier` import (chart interpolates R only; carrier is serve-time — WP2).
- Deleted `low_w_diffractive_chart.py` and removed ALL its wiring from `likelihood.py`: `_AUTO_LOW_W_CHART` sentinel, `low_w_diffractive_chart` constructor arg + auto-load block, `get_init_dict` low-w block, `_low_w_diffractive_chart_serve` method, and the chart consult in `_low_w_diffractive_serve` Rung P (now straight w_low_fit split). `_low_w_diffractive_serve` SURVIVES (the 2 mock refs not touched).
- `serve_route_census.py`: swapped `LowWDiffractiveChart`→`LowWShellChart` (import + load + field comment), `covers` now 4-arg with theta, dropped `.declined(...)`, rewrote the DECLINED-cell comment. Deep census-mirror semantics verification = WP3's job. `_pearcey_cusp.py` docstring dropped stale `` `_airy_fold_form` `` cross-ref. `git rm`'d dead `test_lensing_low_w_diffractive_chart.py`.
- VERIFIED (allowed sanity checks): py_compile clean on all 4 touched files; import round-trip works (`RHO_LO=0.6`,`RHO_HI=1.4`; `evaluate` finite; `covers` theta-fold true; `reduced_source`/`_reduced_min_delay_separation`/`_content_hash` live). Deleted symbols have ZERO refs in cogwheel/ (route label string `'low_w_diffractive_chart'` in serve_route_census.py intentionally KEPT per architect).
- NOT TOUCHED (later WPs): `scripts/train_low_w_diffractive_chart.py` (WP4 — still imports the deleted module, now broken BY DESIGN until WP4 replaces it).

## 2026-08-21 (low_w_shell_born_extension WP2 — shell serve wiring + Born gate)
- Added `_low_w_shell_chart_serve(self, lens, dense_w, geom)` in likelihood.py: chart-first consult of Rung P, wired at the TOP of `_low_w_diffractive_serve`'s `gamma < 1.0` branch (shell_served -> return, else fall through to w_low_fit split). Body: chart=None -> None; `_reduced_shear(gamma,kappa)->(lam,gamma_prime)` (DiffractiveDomainError->None), gamma_prime==0 -> None; yp=y/sqrt(lam), z_eig=exp(-1j beta)*complex(yp0,yp1), theta=atan2, s=|yp|^2, rho=_caustic_rho(abs(gamma_prime),s,theta); rho gate `RHO_LO<=rho<=RHO_HI` else None; source=reduced_source, delta_min=_reduced_min_delay_separation, w_shell=1/delta_min (inf if 0); explicit `w_shell <= w_lo -> None` guard (the shared `_band_split_mask` all-True null fallback is WRONG for the fully-resolved below-trusted polarity); below = `_band_split_mask(dense_w,w_shell)[1] & in_log_w` (in_log_w vs chart.log_w_grid); empty -> None. below: carrier=born_lead_carrier(scalar, listcomp over dense_w[below]) in REDUCED frame, R=chart.evaluate, f_serve=carrier+R, F_abs = mass_sheet_phase*f_serve/lam with mass_sheet_phase=exp(0.5j*w*(log(lam)-kappa*s)). above=~below -> `_engine_farfield_total` (FARFIELD_DIFFRACTIVE gauge). Tail = `_low_w_diffractive_serve` tail (envelope=farfield*exp(1j*_frame_phase(w,geom.t_min)); reconstruct_farfield FARFIELD_DIFFRACTIVE; reduce; image_delays).
- Born gate: `if rho <= 2.0` -> `if rho <= _BORN_RHO_FLOOR` with module constant `_BORN_RHO_FLOOR = 1.4` (single-sourced). Updated `_born_residual_analytic` docstring + gate comment + `_amplification_coefficients` intercept comment (rho > 2 -> above _BORN_RHO_FLOOR).
- Plumbing (fresh, mirroring born_residual_chart): `_AUTO_SHELL_CHART` sentinel; `low_w_shell_chart=_AUTO_SHELL_CHART` constructor arg + auto-load/refuse-to-None block (`self._low_w_shell_chart_is_default`); get_init_dict three-way block; import `from cogwheel.lensing.low_w_shell_chart import (LowWShellChart, RHO_LO, RHO_HI, reduced_source, _reduced_min_delay_separation)`; import `born_lead_carrier` from `_born` (alongside `_born_factors`). __getstate__/__setstate__ UNCHANGED (chart rides in __dict__ like born_residual_chart).
- VERIFIED (sanity): py_compile clean; import round-trip (RHO_LO=0.6 RHO_HI=1.4 _BORN_RHO_FLOOR=1.4; _low_w_shell_chart_serve live; init params has low_w_shell_chart). ZERO refs to LowWDiffractiveChart/low_w_diffractive_chart/_AUTO_LOW_W_CHART/partitioned_reference in likelihood.py (only `_low_w_diffractive_serve` survives, 2 call sites: buried surrogate path ~2480 + first-class intercept ~3682, plus 2 test-file mock refs).
- FLAG for WP3 (census): `serve_route_census.py` still has its OWN `_BORN_RHO_FLOOR = 2.0` + `_RESIDUAL_BORN_FLOOR = 2.0` + `if caustic_rho > 2.0:` (line ~425) — stale vs the likelihood gate now 1.4; must re-derive/import the same constant.
- NOTE: WP1's deletions (low_w_diffractive_chart.py, test) are STAGED; my WP2 edits are unstaged on top; HEAD (6e724e8) still shows old `_low_w_diffractive_chart_serve` wiring in the diff.

## 2026-08-21 (low_w_shell_born_extension WP3 — census mirror shell chart + Born floor 1.4)
- serve_route_census.py: removed module-level `_BORN_RHO_FLOOR = 2.0` (was the ODD re-typed copy — the file's own convention binds production scalars into `_ProductionModules` as float(...), cf. saddle_min_sep_floor/rho_end); added `born_rho_floor` (float(likelihood._BORN_RHO_FLOOR)=1.4), `reduced_source`, `reduced_min_delay_separation`, `rho_lo` (RHO_LO=0.6), `rho_hi` (RHO_HI=1.4) fields; classify_draw Intercept 5 gate now `rho > mods.born_rho_floor`.
- Intercept 6 chart consult rewritten to the shell gate: `RHO_LO <= rho_dir <= RHO_HI` AND `w_hi * shell_delta_min < 1.0` (shell_delta_min = production `_reduced_min_delay_separation` of `reduced_source(gamma_prime, rho_dir, theta)` — reduced_source is NECESSARY because caustic_point is gamma-SIGN-sensitive, so for gamma_prime<0 it does NOT reconstruct [y1,y2]) AND `covers(gamma_prime, rho_dir, theta, w_grid)`. FRESH locals `shell_source`/`shell_delta_min` (no shadowing of outer `source`/per-node `delta_min`); `rho_dir` already fresh (INS-2-001). SERVE_ROUTES tuple + label unchanged; docstrings (SERVE_ROUTES/classify_draw pt5&6/Intercept 5&6 comments) updated to 'macro-lead demodulated-difference shell chart'.
- VERIFIED (sanity): py_compile clean; import round-trip — born_rho_floor==1.4==likelihood._BORN_RHO_FLOOR, rho_lo==0.6==RHO_LO, rho_hi==1.4==RHO_HI, reduced_source is shell.reduced_source / reduced_min_delay_separation is shell._reduced_min_delay_separation (identity), `hasattr(src,'_BORN_RHO_FLOOR')` False, SERVE_ROUTES len 12 unchanged.
- FLAG (Test Dev): test_lensing_born_certificate.py lines 1560 & 2063 use `serve_route_census._BORN_RHO_FLOOR` (module attr, now removed) + a stale `assertGreater(self.rho, 2.0, 'production rho>2')` at ~2064 — must re-derive to the likelihood constant / 1.4.
- FLAG (Inspector/Professor, OUT OF SCOPE): `_RESIDUAL_BORN_FLOOR = 2.0` (residual_demand 3-way split 'born_chart_demand') and `_region_of`'s `caustic_rho > 2.0` are AGGREGATION coordinates (docstring explicitly calls them a reach GAUGE not a domain predicate), NOT the Born serve gate — left unchanged; adjudicate whether the born_chart_demand/exterior buckets should move to 1.4 now that Born serves to rho=1.4.

## 2026-08-21 (INS pass 1 fixes INS-1-001..005 — shell chart + Born floor)
- INS-1-004 (likelihood.py): `_BORN_RHO_FLOOR = 1.4` -> `= RHO_HI` (RHO_HI already imported from low_w_shell_chart; single-source the shell/Born handoff). Comment block updated.
- INS-1-002 (likelihood.py `_low_w_shell_chart_serve`): added gamma-prime box gate `chart.gamma_prime_grid[0] <= gamma_prime <= chart.gamma_prime_grid[-1]` -> None after the rho gate (evaluate uses bounds_error=False/fill_value=None, so out-of-grid gamma' would cubic-EXTRAPOLATE silently; theta self-folds).
- INS-1-005 (serve_route_census.py): intercept-6 chart consult now mirrors production's BAND SPLIT — compute `w_shell = 1/shell_delta_min` (inf if 0), admit iff rho in [rho_lo,rho_hi] AND w_shell > w_lo AND `(band_split_mask(w_grid,w_shell)[1] & in_log_w).any()` AND `chart.covers(gamma_prime,rho_dir,theta,w_grid[below])`; record `w_split=float(w_shell)` per draw. Docstrings (SERVE_ROUTES header, classify_draw pt6, intercept-6 comment) updated from whole-band to whole/band-split. NOTE: route label is now 'low_w_shell_chart' (INS-1-006 rename already applied in working tree by a prior pass) — do NOT use 'low_w_diffractive_chart'.
- INS-1-001 (test_lensing_born_certificate.py): 2x `serve_route_census._BORN_RHO_FLOOR` -> `likelihood._BORN_RHO_FLOOR` (test already imports likelihood); stale `assertGreater(self.rho, 2.0, 'fixture below the production rho>2')` message -> 'fixture not deep far-exterior (rho <= 2.0)' (value 2.0 kept; fixture rho=|y|=80 >> 2).
- INS-1-003 (test_lensing_born_analytic_reachability.py): added `_BORN_RHO_FLOOR` import; new `_BELOW_FLOOR_RHO = 0.5*(1.0+_BORN_RHO_FLOOR)` (=1.2, strictly in (1.0,1.4)); `_OFF_PATH_BATTERY` row rho 1.5 -> _BELOW_FLOOR_RHO (label 'exterior_1<rho<floor'); `test_gate_misses_return_none` rho 1.5 -> _BELOW_FLOOR_RHO (case 'rho<floor'); docstrings '1<rho<2'/'rho>2' -> live floor (module docstring, _TARGET_RHO, test_gate_misses, kappa_beta test comment).
- VERIFIED (sanity): py_compile clean all 4 files; _BORN_RHO_FLOOR==RHO_HI==1.4; census born_rho_floor==1.4; no remaining serve_route_census._BORN_RHO_FLOOR or `_BORN_RHO_FLOOR = 1.4` literal; no 'low_w_diffractive_chart' route string (only `_low_w_diffractive_serve` method + 2 test mock refs survive, intended).
- NOT flagged in findings (left): `_region_of` rho>2/`_RESIDUAL_BORN_FLOOR` (aggregation reach gauge), surrogate_census.py:318 comment, stale 'rho > 2' fixture-premise comments in test_lensing_born_certificate.py (fixtures are rho>>2, still true).

## 2026-08-21 (INS pass 3 fixes INS-3-001/002 — rho-gauge category error)
- INS-3-001 (likelihood.py): reverted `_BORN_RHO_FLOOR = RHO_HI` (a CATEGORY ERROR — RHO_HI is the shell's DIRECTIONAL gauge `_caustic_rho(|gp|,s,theta)=sqrt(s)/|y_c(theta)|` while the Born rung keys on ppgo_map's SCALAR-reach gauge `caustic_rho(gamma,|y|,kappa)=|y|/caustic_reach`) back to an independent `_BORN_RHO_FLOOR: float = 1.4`. Comment block rewritten HONESTLY: scalar-rho <= directional-rho always (reach is max over angle), so Born floor (scalar 1.4) and shell outer boundary (directional 1.4) are DIFFERENT surfaces -> DISJOINT sets, theta-dependent annulus falls to exact engine (conservative gap, no overlap). DELETED the 'handoff never drifts apart' overclaim. RHO_HI still imported/used by `_low_w_shell_chart_serve` (RHO_LO<=rho<=RHO_HI).
- INS-3-002 (likelihood.py): `_born_factors` import ALREADY absent (line 110 is `from ..._born import born_lead_carrier` only; grep confirms ZERO `_born_factors` refs in likelihood source) — verified, no action needed.
- VERIFIED (sanity): py_compile clean; _BORN_RHO_FLOOR==1.4 independent, RHO_HI==1.4, values still equal (guard-test pin holds), no _born_factors ref.
- FLAG for Test Dev (do NOT fix myself per finding): `test_lensing_low_w_shell_chart.py::ShellBornBoundaryContinuityTestCase` still passes (assertEqual(RHO_HI,_BORN_RHO_FLOOR) holds, both 1.4) but its class/method docstrings claiming 'no gap and no overlap' / 'RHO_HI and _BORN_RHO_FLOOR are the SAME constant' are now known OVERCLAIMS (gap exists; only values agree) — re-derive the prose.

## 2026-08-21 (INS pass 2: low_w_chart findings INS-2-001/002/003)
- INS-2-001 (fold/cusp handoff discontinuity): extracted the macro-fold low-w renormalization into a shared helper `_renormalize_macro_lead(f_ref, w_grid, gamma_prime, delta_tau)` in low_w_diffractive_chart.py (sqrt_mu = 1/sqrt(1-gp^2); h = smootherstep(w*|delta_tau|, RHO_START, RHO_END); f_ref *= h + (1-h)*sqrt_mu/|f_ref|). `_airy_fold_form` now CALLS it (byte-identical, delta_tau = merging-pair gap); `_pearcey_cusp_reference` ALSO calls it (delta_tau = `_reduced_min_delay_separation(gamma_prime, source)`), so BOTH uniform forms asymptote to sqrt_mu at low w and no longer step at the b3->0 handoff. Verified: cusp (theta=0.2) and fold (theta=0.3) both give |f_ref[0]|=1.6667=sqrt_mu; continuity sweep worst ratio 3.12x < 5.0 bar (was 5.8x).
- INS-2-002 (test MacroCarrierReferenceTestCase RED): the wall witness (0.8,2.0,0.6) has w_split~0.49, so `_FREF_W_GRID=geomspace(0.02,60,24)` spans resolved nodes -> kind 'geometric'. Fixed the TEST: added `_MACRO_FREF_W_GRID = np.geomspace(0.02, 0.4, 8)` (all below w_split); `_ref()` uses it; `test_macro_residual_bounded_below_split` now computes f_pure/domain on `_MACRO_FREF_W_GRID` (np.all(domain) premise). Verified residual min/max ratio 0.79 >= 0.1.
- INS-2-003 (test RhoPartitionContinuityTestCase.test_sweep_straddles_the_carrier_partition RED): `_W_GRID` spans resolved w -> off-caustic side kind 'geometric' not 'macro'. Fixed the TEST: assert the partition on `np.array([CONTINUITY_W])` (single unresolved w=0.3) instead of `_W_GRID`. Verified rho=1.3 -> 'airy_fold', rho=1.45 -> 'macro'.
- NOTE: this pass touched the TEST file (INS-2-002/003 are test-premise bugs the geometric-carrier redesign introduced). These are mechanical premise fixes (confine the grid below w_split), not new-test authoring — done under the Inspector's "Fix ALL findings" directive.

## 2026-08-21 (INS fix pass: low_w_chart rho-partitioned findings INS-1-001/002/003/005)
- INS-1-001 (cusp_transition keyed on WRONG refusal): `_airy_fold_form` now returns `(None, False)` when `_soft_axis_cubic` returns None (point-mass p<=0 / non-finite, NOT a cusp transition), and keys the cusp_transition flag on an explicit `abs(b3) <= _B3_MIN` check in the `_fold_amplitudes`-None branch (`(None, True)` only for the degenerate-fold b3->0 case, else `(None, False)`). Imported `_B3_MIN` from `_airy_fold`. Docstring fixed (was mislabeling `_soft_axis_cubic -> None` as the b3->0 transition).
- INS-1-003: `_SCHEMA` bumped 'low_w_diffractive_v2' -> 'low_w_diffractive_v3' (both the constant and the load() docstring literal).
- INS-1-005: `_cell_w_split` no longer returns `math.inf` for the fully-resolved case (`w_split <= w_grid[0]`); it returns the true `RHO_END/delta_tau` (inf ONLY for `delta_tau == 0.0`), letting `_carrier_adequate` hit its empty-domain `return True` admit path. `w_grid` param kept for signature stability (now grid-independent).
- INS-1-002 (geometric carrier, Professor binding fix): `partitioned_reference` off-caustic branch now returns a PIECEWISE F_ref — builds the full macro-lead array via list comprehension, then `delta_tau = _reduced_min_delay_separation(...)`, `w_split = RHO_END/delta_tau` (inf if delta_tau==0), `resolved = w_grid >= w_split`, and overwrites `f_ref[resolved] = geometric_amplification(w_grid[resolved], source, gamma_prime)` (LensDomainError -> `(None,'geometric')` decline). Returns kind 'geometric' iff `resolved.any()` else 'macro'. Two residuals per off-caustic cell (macro-anchored below split, geometric-anchored above). `_carrier_adequate` UNCHANGED (else-branch already guards 'geometric' full-band); `_residual_at` UNCHANGED (kind=='macro' -> `_cell_w_split`, else w_split=inf).
- SERVE (`_low_w_diffractive_chart_serve`): the `if RHO_LO<=rho<=RHO_HI ... else ...` branch and the bare `geometric_amplification/lam` serve are DELETED — single uniform `farfield = mass_sheet_phase * fref * sqrt_mu_full * r_fit` (fref is already piecewise). Removed module-level `geometric_amplification`/`RHO_LO`/`RHO_HI`/`_reduced_min_delay_separation` imports from likelihood.py (reverted to pre-WP3). Docstring 'exact two-image geometric sum' -> 'w->inf asymptote, not exact at finite w'.
- TRAINER census: `_FillResult` gained `cell_kinds` (n_gp,n_rho,n_theta object array: kind | 'refused' | 'unbuildable' | 'guard_declined'); `carrier_populations` gained 'geometric'; `n_resolved_served_cells += 1` now keyed on `kind == 'geometric'` (deleted `_macro_has_resolved_node` — equivalent to `kind=='geometric'`). main() emits per-carrier `_margin_report` breakdown (airy_fold/pearcey_cusp/macro/geometric) over the grid-cell served_errs in `_iter_points` order, stored in provenance `per_carrier_margin`. Module/WHAT/DE-RATE docstrings updated.
- Test file test_lensing_low_w_diffractive_chart.py is RED vs the new design (MacroCarrierReferenceTestCase asserts kind=='macro' + w-independent |F_ref| for MACRO_WITNESS=(0.8,2.0,0.6) which is now 'geometric'; PerCarrierServeAccuracyTestCase.test_geometric_far_exterior_serve still uses relaxed GEO_SERVE_TOL bars). Test Dev owns re-derivation — do NOT touch the test file.


## 2026-08-21 (low_w_chart_rho_partitioned WP3 — serve band-split re-modulation)
- `_low_w_diffractive_chart_serve` (likelihood.py) now branches on the rho partition AFTER `r_fit`/`mass_sheet_phase` are computed: caustic neighborhood (`RHO_LO <= rho <= RHO_HI`) keeps the full-band `farfield = mass_sheet_phase * fref * sqrt_mu_full * r_fit` (unchanged); off-caustic (else) computes `delta_tau = _reduced_min_delay_separation(gamma_prime, source)`, `resolved = dense_w * delta_tau >= RHO_END`, and assembles `farfield` element-wise — `farfield[~resolved] = mass_sheet_phase * fref * sqrt_mu_full * r_fit` (macro-lead + chart residual) and `farfield[resolved] = mass_sheet_phase * geometric_amplification(dense_w[resolved], source, gamma_prime) / lam` (reduced-frame two-image geometric sum; `lam = 1-kappa` already in scope). No try/except around `geometric_amplification` — a propagating `LensDomainError` is outcome-preserving (the exact seed-engine path below raises the identical error for the same geometry), per the `_amplification_coefficients` docstring convention. Demodulation + `reconstruct_farfield` tail UNCHANGED; `_schwinger.f_schwinger` never called.
- New module-level imports in likelihood.py: `geometric_amplification` added to the existing `from cogwheel.lensing.chang_refsdal.operator import (RHO_END, geometric_amplification)` line (operator already imported at module level — no new cycle); `RHO_LO, RHO_HI, _reduced_min_delay_separation` added to the `low_w_diffractive_chart` import line. `RHO_LO=0.6`, `RHO_HI=1.4`, `RHO_END=4.0` verified at runtime. The rho branch key (`RHO_LO <= rho <= RHO_HI`) is the SAME discriminator `partitioned_reference` already used to pick `_kind` (airy_fold/pearcey_cusp = caustic, macro = off-caustic), so the serve branch is consistent with the reference builder. Test Dev owns the per-carrier served-accuracy pins + resolved/unresolved split tests.

## 2026-08-21 (low_w_chart_rho_partitioned WP2)
- Trainer `_residual_at` now returns `(residual | None, kind)` (kind in {'airy_fold','pearcey_cusp','macro'}): `(None,None)` = F_ref unbuildable, `(None,kind)` = guard-declined, `(residual,kind)` = served. `_carrier_adequate(residual, kind, w_grid, w_split)` applies the ABSOLUTE guard (sup|r|<=1e3 AND inf|r|>=1e-3) over the carrier-specific domain: full w_grid for airy_fold/pearcey_cusp, only `w < w_split` for macro. `w_split = RHO_END/delta_tau` via `_reduced_min_delay_separation`; delta_tau==0 or fully-resolved band (`w_split <= w_grid[0]`) -> `w_split=inf` (guard full band). `_macro_has_resolved_node` (separate helper) uses the RAW w_split (no inf adjustment) for `n_resolved_served_cells`.
- `_fill_coefficients` returns a `_FillResult` dataclass (real, imag, n_refused, unbuildable_mask, guard_declined_mask, carrier_populations, n_resolved_served_cells); guard-declined cells are sentinel-filled (1.0+0j) and fold into declined_mask = unbuildable | guard_declined | declined_by_error. `grid_n = sum(~(unbuildable | guard_declined))` (n_refused=0 guaranteed by SystemExit). `_off_grid_engine` renamed its mask `off_unbuildable` -> `off_declined` (now covers both unbuildable and guard-declined). carrier_populations + n_resolved_served_cells live in provenance JSON ONLY (no npz schema/content_hash change). New imports: RHO_END (operator), _reduced_min_delay_separation (low_w_diffractive_chart). Test Dev still owns re-derivation of test_lensing_low_w_diffractive_chart.py (its local `_residual_at` mirror is separate).

## 2026-08-21 (low_w_chart_rho_partitioned WP1)
- `fold_cusp_reference` renamed -> `partitioned_reference(w_grid, gamma_prime, rho, source) -> (f_ref, kind)` with kind in {'airy_fold','pearcey_cusp','macro'}; partition is RHO_LO<=rho<=RHO_HI (caustic nbhd: Airy primary, Pearcey ONLY on b3->0 cusp_transition) vs off-caustic (macro `born_lead_carrier`, never None).
- `_airy_fold_form` now returns `(f_ref, cusp_transition)`; cusp_transition True EXACTLY when `_soft_axis_cubic` returns None. Macro-fold magnitude renormalization after the loop: `sqrt_mu=1/sqrt(1-gp^2)`, `h=smootherstep(w*|delta_tau|, RHO_START, RHO_END)`, `f_ref *= h + (1-h)*sqrt_mu/|f_ref|` (h~0 at low w -> |f_ref|->sqrt_mu; h~1 at resolved w -> raw fold). smootherstep rises 0->1, NOT 1->0.
- `_NON_VANISHING_MIN_RATIO` deleted (WP2 absolute guard subsumes); `_reduced_min_delay_separation(gp, source)` added wrapping `operator._real_delay_min_separation(np.asarray(source), geometry.macro_matrix(gp,0,0))`.
- New imports in low_w_diffractive_chart.py: `born_lead_carrier` (_born), `smootherstep` (_gauge), `RHO_START/RHO_END/_real_delay_min_separation` (operator). operator is already in the import chain (via _born->channels->operator->_schwinger); NO engine CALL added.
- Call sites: trainer `_residual_at` and likelihood `_low_w_diffractive_chart_serve` now pass rho + unpack `(f_ref,_kind)` (kind discarded).
- Test file test_lensing_low_w_diffractive_chart.py still imports `_NON_VANISHING_MIN_RATIO` + calls 3-arg `partitioned_reference` + treats `_airy_fold_form` single-return — Test Developer owns re-derivation.