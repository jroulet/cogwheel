# Coder Short-Term Observations

(empty — last consolidated by Dreamer on 2026-08-19)

- diffractive_certificate_fit_corner WP1: shipped even-harmonic basis cos(2k theta) k=1..7 (was cos(4k) k=1..4, N_HARM 4->7) + parametric-caustic feature log(|y'|/|y_c(theta)|) as ONE trailing feature in `_fit_features`, new fitted `_DIFFRACTIVE_FIT_CAUSTIC_COEFF` (came out -0.727, negative as expected); `w_low_fit` slices poly[:n_poly]/harm[n_poly:n_poly+n_harm]/caustic[n_poly+n_harm] explicitly (the old `features[n_poly:]` zip would have silently dropped the caustic feature). New pure-python `geometry.caustic_point(gamma, theta, *, beta=0, kappa=0, branch=1.0) -> (float,float)` mirroring `_caustic_source` byte-identically (verified equal across beta/kappa/branch), no numba/ndarray, NO brentq/Newton. Fit script: `_fit_model` design n_poly+n_harm+1; main slices harmonic coeffs [n_poly:n_poly+n_harm] + caustic coeff; emission block prints `_DIFFRACTIVE_FIT_CAUSTIC_COEFF`; smoke grid now corner-inclusive ((0.41,0.5)x(0.55) @32 thetas). Smoke bake (`--scale smoke --seed 42 --n-w 16`, SHA 3827f48) ran 754.2s — EXCEEDS ~10-min soft budget (but < 1200s SDK ceiling). Corner raw over-prediction gamma=0.41 r=0.55 theta=2.454369 = 1.986x (fit 6.866 vs true 3.456) — NOT < 1.5x, flat vs old 2.06x; de-rate crushed to 0.5034. DIAGNOSIS: caustic feature IS load-bearing (without it corner raw ~2.46x) but smoke grid's gamma gap (0.3->0.41) under-constrains the degree-2 poly (smoke POLY coeffs huge/unstable). ESCALATED to driver per WP >=1.5x fallback; provisional coefficients committed with '# PROVISIONAL' markers. Corner-witness diagnostic matches witness_theta = 3pi/4 + pi/N_THETAS (=2.454369, NOT rounded 2.454 — 1e-6 tol missed it first try).

- diffractive_certificate_fit_corner INSPECTOR-FIX (INS-1-001/002, 2026-08-20): ROOT CAUSE of the corner over-prediction (1.986x) is NOT the representation — it's narrow MARGINAL resonances in the order-16 series near the fold: rel(w) barely exceeds CERTIFICATION_BAR (1.1-1.2e-4 vs 1e-4) in ~0.1-wide w-windows at the outside-caustic DIAGONAL directions (theta=pi/4, 3pi/4,...), so the honest ceiling oscillates between ~3.5 resonance floor and ~6.9 smooth level. The n_w=16 coarse scan samples these INCONSISTENTLY: same source gives w_low_true 3.456 (n_w=16), 6.54 (n_w=24/40/64); on-grid diagonal 3pi/4 gives 7.122 (misses resonance) while off-grid midpoint 3pi/4+pi/32 gives 3.456 (catches it) — a spurious 5.6°-wide step no smooth O(1) fit (harmonics min scale ~13° at cos14θ) can follow. Robust dense scan (256 pts) confirms resonance floor ~3.5 at diagonals, ~5.9-6.9 elsewhere. Degree-3 / region-split address gamma/s (already smooth power law) NOT the angular resonances — so they CANNOT fix the corner. Robust measurement needs ~250 log-w points/row (spacing <<0.014) = cost-prohibitive (~5-17 min for just 2 corner cells). RESOLUTION (Inspector's explicit fallback): (1) INS-1-001 documented in _diffractive.py provenance comment + _measure_w_low_true docstring — driver's de-rate target LOWERED to resonance-limited ~0.5 (NOT >=0.70); measurement-robustness is a SEPARATE future WP. (2) INS-1-002: gated test_raw_fit_over_prediction_below_150_percent behind COGWHEEL_DIFFRACTIVE_FULL_BAKE=1 (was un-gated RED 1.986 vs <1.5 pin); rewrote its docstring from "flips green after full bake" to "NOT expected to flip green (resonance-limited)". Test file now 33 passed / 3 skipped / 0 failed. Self-falsification test (test_dropping_caustic_feature... >1.5) stays un-gated/green. This session's changes are DOCUMENTATION+GATING only (no functional code).

- diffractive_w_low WP1: extracted closure -> module `_honest_tail_ratio(w,u0,v0,s,gamma_prime,order)` (returns inf on domain/overflow/ValueError AND when abs(total_trunc) not >0 OR total_trunc/tail_sum non-finite — hardened so doubling can't compute inf/inf=nan); added `_rootfind_w_high(relerr,lower,target,w_lo,w_hi,max_iter=100)` up-search mirror of _rootfind_w_low. `diffractive_w_low` gained keyword-only w_lo/w_hi=None; candidate-clears branch now returns _rootfind_w_high(..., CERTIFICATION_BAR) instead of candidate; deep-optimistic None + _rootfind_w_low(bar_inner) down-search branches semantically unchanged (relerr now delegates to _honest_tail_ratio). Production callers likelihood.py:2013/:2750 + serve_route_census.py:924 call positionally — unaffected. Candidate-clears return is now >= old candidate, so existing test_lensing_diffractive pinned exact-w_low expectations may need re-deriving (downstream Test Dev/Inspector).

- diffractive_certificate_reach WP2: `_diffractive_bottom_ceiling(self, lens, w_lo=None, w_hi=None)` now forwards w_lo/w_hi to `diffractive_w_low` (band-aware honest ceiling; docstring notes `w_hi` whole-band return). Rung-P `_low_w_diffractive_serve` call adds `w_lo=w_lo, w_hi=w_hi`; `_saddle_farfield_analytic` + `_born_residual_analytic` call `_diffractive_bottom_ceiling(lens, float(dense_w.min()), float(dense_w.max()))`; nested-low-split comments reworded to `[w_lo, honest_ceiling)`.
- diffractive_certificate_fit WP1: added engine-free `w_low_fit(y, gamma, beta, kappa, *, w_lo=None, w_hi=None) -> float|None` to `_diffractive.py` — an O(1) parametric surface fitted to the ENGINE-HONEST ceiling (largest w with |F_order16 - f_schwinger|/|f_schwinger| <= CERTIFICATION_BAR), replacing the per-proposal `diffractive_w_low` SCAN in the hot path (WP2 wires it). Reduction: `_reduced_shear` (wall raises DiffractiveDomainError, left in place), gamma_prime==0 -> 0.0, `sqrt_mu = _born_factors(...)[0]` (None if non-finite), eigenframe `yp=y/sqrt(lam)`, `s=|yp|^2`, `z_eig=exp(-1j*beta)*yp`, `theta=atan2(imag,real)`. Fit: `log w_low = P(deg2 poly in log gamma', log s, log(1-gamma')) + sum_{k=1..16} a_k cos(4k theta) + (1/17)*log(lam*sqrt_mu)`; coefficients baked as module constants (tuples, NOT npz): `_DIFFRACTIVE_FIT_DEGREE=2`, `_DIFFRACTIVE_FIT_POLY_COEFFS` (10), `_DIFFRACTIVE_FIT_HARMONIC_COEFFS` (16), `_DIFFRACTIVE_FIT_DERATE=0.85`, `_DIFFRACTIVE_FIT_LIP=1/17`, `_DIFFRACTIVE_FIT_N_HARM=16`, `_DIFFRACTIVE_FIT_CEILING=W_CEILING_SCHWINGER` (imported, not re-typed). NO deep-optimistic None branch. w_hi caps via min; w_lo accepted-but-unused (signature parity for WP2). Shared feature basis lives in `_fit_features`/`_fit_poly_exponents`/`_fit_poly_features`; the calibration script imports them (single source of truth) rather than re-deriving. New `scripts/fit_diffractive_certificate.py`: measures w_low_true per grid point (coarse log-w scan + geometric bisection, `_W_MIN=0.2`, saturates at ceiling), fits poly+harmonics by lstsq, computes derate=min(1,0.85,1/max_overpred), reports conservative/tight margin + emits Python-literal coefficients + git SHA. --scale smoke (51 pts, ~100s) vs --scale full (post-build driver step). Smoke margin: 49/49 conservative, 49/49 tight, worst 0.9857, median 0.85. Key gotcha: the margin loop must use the SCRIPT's fresh fit (derate*_evaluate_fit), NOT the module's `w_low_fit`, or it measures the PREVIOUSLY-baked (placeholder) coefficients (recurring: paste-then-measure circularity). oracle frame: `y_eig = _rot_minus_beta(beta) @ (y/sqrt(lam))` where `_rot_minus_beta = [[cosB,sinB],[-sinB,cosB]]` == complex `exp(-1j beta)` (verified equal); `f_schwinger(w, y_eig, gamma_prime)` is the exact oracle (gamma_prime>0 both parities valid per _validate_inputs). `_fit_poly_exponents` counts 10 monomials at deg2; n features = n_poly + _DIFFRACTIVE_FIT_N_HARM.
- diffractive_certificate_reach WP3 (census mirror, serve_route_census.py): Born intercept `w_low = mods.diffractive_bottom_ceiling(lens)` -> `(lens, w_lo, w_hi)`; Rung-P admission `mods.diffractive_w_low((y1,y2), gamma, 0.0, 0.0)` -> added `w_lo=w_lo, w_hi=w_hi` (band now threads to the bound production predicates; whole-band-certified -> w_hi, floor-fail -> None falls through to node pass). No gate re-typed. NOTE: depends on WP2 landing `_diffractive_bottom_ceiling(self, lens, w_lo=None, w_hi=None)` in likelihood.py — until then the Born-intercept call is 3-arg into a 1-arg method (parallel batch, lands together).
- diffractive_certificate_fit WP2 (retire the per-proposal scan): deleted `diffractive_w_low`+`_honest_tail_ratio`+`_rootfind_w_high`+`_rootfind_w_low`+`_DIFFRACTIVE_CERT_SAFETY`+`_CERT_REFERENCE_W` from `_diffractive.py`; module docstring now describes `w_low_fit`. likelihood.py: `_low_w_diffractive_serve` certificate call -> `w_low_fit(y,gamma,beta,kappa,w_lo=,w_hi=)` AND its except switched `HypergeometricDomainError`->`DiffractiveDomainError` (fit only raises the wall; serve loop keeps its own `except HypergeometricDomainError`); `_diffractive_bottom_ceiling` wrapper swapped internally (outer `except DiffractiveDomainError` stays); all `diffractive_w_low` docstring refs -> `w_low_fit`. serve_route_census.py: `_ProductionModules.diffractive_w_low` field+import+binding+call -> `w_low_fit`; `diffractive_refusal_errors` shrunk `(HypergeometricDomainError, DiffractiveDomainError)` -> `(DiffractiveDomainError,)`; `_hyp1f1.HypergeometricDomainError` import dropped (now unused). Verified mirror binds identical predicate (`mods.w_low_fit is _diffractive.w_low_fit`). Rung S untouched. OUT-OF-SCOPE stale prose left: `w_low_fit` docstring + `scripts/fit_diffractive_certificate.py:3` still name the retired `diffractive_w_low` historically (Librarian/Inspector may reword).
- diffractive_certificate_fit INSPECTOR-FIX (INS-1-001/002/003, 2026-08-19): NOTE a LATER work package (not mine) DROPPED the `w_lo` kwarg from `w_low_fit` — current signature is `w_low_fit(y, gamma, beta=0.0, kappa=0.0, *, w_hi=None)`; `_diffractive_bottom_ceiling(self, lens, *, w_lo=None, w_hi=None)` now forwards ONLY `w_hi` to `w_low_fit`, keeps `w_lo` solely for the call-site `w_low <= w_lo` null-split. (1) INS-1-001: migrated `test_lensing_diffractive.py` off the retired API — import `w_low_fit`, dropped `diffractive_w_low/_honest_tail_ratio/_CERT_REFERENCE_W/_DIFFRACTIVE_CERT_SAFETY/_reduced_shear/_born_factors/_DEFAULT_MAX_ORDER`; deleted helpers `_honest_tail_ratio_at`+`_closed_form_candidate` and the scan-internal classes `CertificateOptimismWitnessTestCase`/`WLowMonotonicityWitnessTestCase`/`CeilingTightnessTestCase`/`CeilingMonotonicityTestCase`/`BandFloorAndWholeBandAdmissionTestCase`/`WrapperFidelityBandRoutingTestCase` + their constants (`NONMONOTONE_DRAW/OPTIMISTIC_GAMMAS/LOW_HEADROOM_GAMMA/TIGHT_*`); KEPT the engine-oracle classes (LowWAnchor/TruncationCertifiedBand/KappaEngineOracle/WallRefusal/ShearFreePointMassPhase/NestedNullSplit/RungSQuadrature) with `diffractive_w_low`->`w_low_fit` swap; removed 2 self-falsification methods (`test_optimism_witness_has_teeth`/`test_ceiling_tightness_has_teeth`); rewrote module docstring. Collects 31 tests clean (import OK). (2) INS-1-002: `scripts/fit_diffractive_certificate.py` oracle now compares `diffractive_amplification` against `_engine_full(w,y_eig,lam,gamma_prime,kappa,s_reduced)` = mass-sheet-reconstructed `f_schwinger` (`exp(0.5j*w*(log lam - kappa*s))/lam * f_pure`), well-defined at kappa!=0 (verified: kappa=0 collapse rel 0.0, kappa=0.3 diff-vs-full rel ~8e-15 at w=0.5). (3) INS-1-003: `derate = min(_HARD_DERATE_CEILING, 1.0/max_overpred)` with `_HARD_DERATE_CEILING=0.85` (dead `min(1.0,...)` clamp removed; behaviorally identical); comment rewritten. `test_lensing_part0_mechanical.py` is the Test-Dev-authored mechanical `w_low_fit` suite (engine-free) — complementary to the migrated engine-oracle suite, NOT redundant.
- diffractive_certificate_fit INSPECTOR-FIX pass2 (INS-2-001/002, 2026-08-19): (A) INS-2-002 DONE: dropped dead `w_lo` kwarg from `_diffractive_bottom_ceiling` (now `(self, lens, *, w_hi=None)`); removed `w_lo=` from the 3 call sites (likelihood.py:2889/:3088 -> `w_hi=float(dense_w.max())`, serve_route_census.py:856 -> `w_hi=w_hi`); reworded wrapper docstring + `w_low_fit` Notes to say the null-split is owned by call sites via `_band_split_mask(dense_w,w_low)` + `w_low>=dense_w.max()` branch; dropped the vestigial "(The `>=` also absorbs the down-search branch...)" parentheticals; mechanical test-compat: `NestedNullSplitByteIdentityTestCase._compose` + 4 stub lambdas in test_lensing_born_certificate.py now `lambda lens, *, w_hi=None`. (B) INS-2-001 BLOCKED (CRITICAL, for Professor/Architect): the suggested fix (paste the `--scale full` re-bake) produces a DEGENERATE harmonic fit. Root cause: `_grid_points` samples only 8 thetas (multiples of pi/4), which ALIASES `cos(4k theta)` for k=1..16 (`_DIFFRACTIVE_FIT_N_HARM=_DEFAULT_MAX_ORDER=16`): at 8 samples even-k harmonics all collapse to the all-+1 column, odd-k to the alternating column, so the lstsq spreads the angular signal into 2 effective patterns. The shipped re-bake (fit_full2.log, derate 0.745168) has odd-k harmonics ~ -0.016671 and even-k ~ 1.193 (all near-identical) = smoking gun. MEASURED consequence: `w_low_fit` oscillates catastrophically off-grid — at gamma=0.2,r=0.9: theta=0 -> 11.70, pi/4 -> 15.27, pi/8 -> 0.00096; and at Y_REF=(0.8,0.4) (theta=0.4636, off-grid) w_low_fit collapses to ~0.0014 (was ~67 at gamma=0.05 pre-drift). So pasting the re-bake KILLS the analytic rung (under-serves by ~1e4 off-grid) even though it fixes the on-grid over-serve. I pasted then REVERTED to the shipped 0.85 (net-zero on _diffractive.py coefficients; only the Notes docstring reword remains). FIX NEEDED (design, not Coder scope): either raise theta sampling to resolve 16 harmonics (>=32 thetas -> huge re-bake) OR reduce `_DIFFRACTIVE_FIT_N_HARM` to what 8 thetas can resolve (~k=1) OR a hybrid; then re-bake. The `_DIFFRACTIVE_FIT_N_HARM = _DEFAULT_MAX_ORDER` coupling is a latent trap (already noted by Inspector as "trivial/carry": harmonic count tied to truncation order). Extending the engine-oracle suite (INS-2-001 part 2) is ALSO blocked on this: a sweep over off-grid thetas would go red on the degenerate fit.
- diffractive_certificate_fit INSPECTOR-FIX pass3 (INS-3-001, 2026-08-19) — RESOLVED, pass2's "blocked" was WRONG: the degenerate fit (fit_full2.log 16:52, 224/224) was a symptom of the kappa-oracle bug (INS-1-002): with the 12 kappa rows skipped, the fit saw only the 8 aliased thetas. Once INS-1-002 landed (kappa rows now measurable), the Inspector's LIVE `--scale full` re-bake (fit_full.log 18:32, 236/236 measurable) produces NON-degenerate, varied harmonic coefficients (-2.195, -0.692, -0.739, 0.103, ...) — the 12 random-theta kappa rows break the 8-theta aliasing. Pasted that emission block verbatim into `_diffractive.py`: POLY[0]=9.6050, HARM varied, DERATE=0.745168, provenance SHA 7eeedee (440.3 s), margin 236/236 conservative+tight, worst 1.0000, median 0.7506, p90 0.8852. VERIFIED off-grid collapse gone: gamma=0.25,y=(0.3,0) -> 15.31 (was 52.83 vs true ~21.6, now conservative); theta sweep gamma=0.2,r=0.9 -> [11.70, 60.0, 15.27] (saturates at ceiling, no ~0.001 collapse). SHA-SKEW (noted in report): provenance stamps 7eeedee = committed HEAD, but the baking script (`scripts/fit_diffractive_certificate.py`) is UNTRACKED and carries the INS-1-002/003 fixes, so the stamped SHA does NOT identify the script state. Updated the `# post-build` -> `# in-build` Usage comment in the script. Residual concern (carry to Professor): the angular fit at 8 grid thetas + 12 random kappa thetas is still under-resolved for 16 harmonics (w_low_fit quantizes to ~3 theta values: 11.7 / 15.3 / 60-saturated), so off-grid it under-serves (conservative but wasteful) — a future theta-refinement of the calibration grid would tighten it; `_DIFFRACTIVE_FIT_N_HARM=_DEFAULT_MAX_ORDER` coupling remains a latent trap.
