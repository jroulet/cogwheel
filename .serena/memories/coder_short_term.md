# Coder Short-Term Observations

WP1 saddle-exterior-cusp-coordinate: added _deltoid_cusp_axis_map to surrogate.py (generalized cusp-adapted u=d**(2/3) map for saddle deltoid cusps, with straddle fallback to None). Wired into _build_farfield_chart parity==-1 branch in surrogate_training.py: detects deltoid cusp rays via _deltoid_cusp_source_angles, picks nearest to tile centre, builds cusp-adapted map when tile is entirely on one side (raw-theta fallback when straddle/no cusp). _subdivide_farfield_tile children inherit the same logic automatically via _build_farfield_chart. Fixed stale 'Positive-parity only' docstrings in _exclude_ghost_dominated and _needs_fold_carrier. Created scripts/measure_saddle_cusp_arm_coverage.py (mirrors measure_cusp_arm_actual_boundary.py for saddle cusps). All imports pass, no diagnostics. WP2 parity-gated cusp-window: added _SADDLE_CUSP_ARM_COVERAGE=0.0 after _CUSP_ARM_COVERAGE (line 307), parity-gated _tube_serves cusp-window shrink (chart.parity==-1 uses 0.0, else 0.07). Positive-parity path is byte-identical to HEAD. on 2026-08-10)