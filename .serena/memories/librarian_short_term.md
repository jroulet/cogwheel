# Librarian Short-Term Observations

## 2026-08-10 — Post-commit sync: re-scope rho-phase-carrier build (0d9a1b8)

### Scope
Commit 0d9a1b83 (2026-08-10). Changed files:
- `.claude/handoff/brief_exterior_rho_phase_carrier.md` — build brief (agent-internal, not a doc surface)
- `.claude/spec/TODO.md` — generated canonical (already regenerated by pre-commit hook)
- `.claude/spec/todo.d/lensing_exterior_rho_phase_carrier.md` — TODO fragment (already rendered)

### Outcome
Pure no-op sync. No cogwheel/*.py changes, no public API changes, no Sphinx doc surfaces stale.

### sync_derived_docs.py result
Ran clean. Only warnings are the KNOWN recurring test-consumer warnings for
`lens_amplification_surrogate` (4 test-only callers in test_lensing_surrogate.py).
Escalation fragment `todo.d/surrogate_contract_test_consumer_warning.md` confirmed still
open — no duplicate created. "Some issues auto-fixed" is the known no-op internal state
flush (confirmed via `git diff --stat` showing only stray diffs in `agent_state/librarian.json`,
reverted per institutional memory).

### Brief working-tree surprise
During this session, `.claude/handoff/brief_exterior_rho_phase_carrier.md` acquired an
additional +15-line working-tree change (driver-validated "ANALYTIC GHOST-CARRIER
DEMODULATION" section, dated 2026-08-10) that was NOT in the initial session git status.
This was written by another process/agent concurrently. Left untouched per
"don't touch concurrent in-flight uncommitted changes" rule.

### Fragile cross-references to watch
(unchanged from previous run)
- `_EXTERIOR_POLAR_AXIS_SCHEMA_V3` cited in BOTH SPEC.md and DATA_CONTRACTS.yaml
- If `carrier_rate` or `rho_log_axis` field names are renamed in code, BOTH doc surfaces need updating
