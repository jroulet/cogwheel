# Architect Short-Term Observations

Build 8h-b (ppGO cell ceilings + ghost v3 label + caustic-fixed interiors), planned 2026-07-23:
- Professor rulings that became test tolerances: ghost G is the discarded complex-conjugate quartic-root pair in geometry._generic_candidates (~L662 filter); needs a DEDICATED _ghost_kernel — complex-log delay ½·clog(x_c·x_c) phase-tracked from the real fold, amplitude 1/sqrt(det H_c) on an unwrapped-arg branch seeded at the real saddle, C1/C2 continued verbatim, NEVER route through image_kernel/delay/morse_index. Gate currency is w·Im τ_c radians (mirrors SACR-C RHO_END=4): apply G at w_min·Im τ_c≥2, keep v2 at <1, plus cusp-window mask; on-axis Im τ_c=0 → refuse. Helpful reachable-red: γ∈{0.2,0.4}, off-cusp ρ∈[1.2,1.8], w∈[3,40] → eps_v3≤eps_v2/3. Harmful: γ=0.4 on-cusp ρ≈1.05-1.15 → gate refuses AND forced-G grows residual.
- Node reprovision = reuse LOO _heldout_eps (F-normalized 1e-3) reducing w-nodes; y1/y2 tiling held (fold turnover unrescued). Probe decides, don't hardcode 2×.
- Interior: ρ=|y|/r_caustic(γ,θ_y) admit (exact), nearest-caustic dist for tube-shell, tile edges ON the 4 astroid cusp rays. Saddle per-lobe: origin at source-plane deltoid centroid, 3 cusp rays/lobe, exclude inter-lobe equidistance corridor. Crown (γ0.87-0.99, eps3.4 catastrophe = ill-conditioned far-field-style interior label): switch to SACR-C τ_c-demodulated interior envelope (partition.envelope, switch ON) — falsifiable bar: synthetic mid-γ=0.40 tile that fails 6e-2 under far-field label PASSES 1e-3 under SACR-C label; crown milestone eps3.4→≤1e-1, Pearcey cusp-uniform deferred post-build.
- Simplifier: WP1 bisect on prefix-endpoint INDEX (monotone-in-w refusal ⇒ accepted set is a prefix), O(log n_w) engine calls/angle. WP2 no NEW sentinel — w_ceiling accessor parallels w_cert/w_trust. WP5 campaign-disguise risk: scope it as a deterministic adaptive MECHANISM + synthetic-tile test, scale reprovisioning is post-build driver. WP8 design IS pinned by Professor (not open) — phrase as SACR-C envelope label, not vague 'TubeChart-style'. scratchpad/complex_saddle_probe.py NOT in worktree — P1 facts taken as given.