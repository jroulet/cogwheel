# Architect Short-Term Observations

## 2026-08-21 (low_w_shell_born_extension plan — supersedes rho_partitioned)
- KILLS the quotient low-w diffractive chart (partitioned_reference / 4-carrier F_ref / LowWDiffractiveChart / derate / declined_mask) — 5800x poles. Replaced by: (1) LOW-W SHELL chart rho in [RHO_LO=0.6, RHO_HI=1.4], w*delta_min<1 — MACRO-LEAD demodulated-DIFFERENCE chart; (2) Born extension rho 2.0->1.4 + re-train grid.
- Professor: residual = RAW difference R=f_pure-carrier (reduced frame, NO t_min demod — t_min demod is a far-exterior/phi_geo≈t_min thing); serve F=mass_sheet_phase*(carrier+R)/lam then _frame_phase into FARFIELD_DIFFRACTIVE; carrier=born_lead_carrier(reduced) imported. Serve kappa!=0/beta!=0 via reduced_shear (NOT Born's kappa==0 gate — shell is a reduced-frame object). Gate: delta_min=_reduced_min_delay_separation (min pairwise real-image gap, reduced), BAND-SPLIT at w_shell=1/delta_min (chart below, fold/tube/engine above) — not whole-band decline. w-continuity at w*delta_min=1 needs NO new gate; the "no step" acceptance is at rho=1.4 (shell vs Born, both 1e-4). Born theta=pi/4 fixed adequacy at rho=1.4 is UNMEASURED — driver must azimuthal-sweep at rho=1.4 BEFORE re-train; if N(theta)>8, extend shell upward instead of bolting theta onto Born. Node budget: 2-3 rho nodes [1.4,2.0], log_w down to ~0.3-0.5 not 0.02. Tolerances: node-exact 1e-10, round-trip 1e-13, off-grid 1e-4.
- Simplifier: NEW FILE low_w_shell_chart.py (delete old; only 3 test files ref old name, 2 mock the SURVIVING _low_w_diffractive_serve=zero churn, 1 is the dead quotient suite). Keep 4 WPs (don't split WP2 Born-gate+shell-serve, don't split WP4). DROP derate+declined_mask (pole-free needs neither). KEEP route label 'low_w_diffractive_chart' (no rename churn). Shell serve must copy _low_w_diffractive_serve's DIFFRACTIVE tail NOT _born_reconstruct's KERNEL_SUM. Band-split via _band_split_mask idiom. rho grid = RHO_LO..RHO_HI (no decimal literals).
- 4 Coder WPs: WP1 shell chart class -> WP2 likelihood serving (shell serve + Born gate 2.0->1.4) -> WP3 census mirror; WP4 training scripts (parallel after WP1).

## 2026-08-21 (low_w_shell_chart — INS-3-001 SUPERSEDES INS-1-004): triage ruled coder_fix on the rho-gauge single-source. Verified in code: Born gate rho = ppgo_map.caustic_rho(gamma,|y|,kappa) SCALAR-reach gauge (|y|/caustic_reach, reach=max over angle); shell serve rho = _diffractive._caustic_rho(|gp|,s,theta) DIRECTIONAL gauge (sqrt(s)/|y_c(theta)|); scalar<=directional always (reach/r_caustic in [1.45,6.2]) so the two covered sets are DISJOINT (no overlap ever) with a theta-dependent gap the exact engine serves. `_BORN_RHO_FLOOR = RHO_HI` therefore crosses gauges — decouple: `_BORN_RHO_FLOOR: float = 1.4` scalar-reach constant, honest comment (node-budget boundary lowered 2.0->1.4, NOT a physics law; NO 'can never drift apart' claim). Value equality stays pinned only by test assertEqual(RHO_HI, _BORN_RHO_FLOOR). Census reads float(_BORN_RHO_FLOOR) so auto-follows; no census change. INS-3-002 coder_fix: _born_factors import line 110 unused (only line in file). Test-prose flag: ShellBornBoundaryContinuityTestCase docstrings overclaim 'no gap and no overlap'/'SAME constant' — assertion passes, prose re-derivation routed to Test Dev.
- INS-1-004 (older ruling, superseded) -> coder_fix: `_BORN_RHO_FLOOR = RHO_HI` single-source — SEE INS-3-001 above.
- INS-1-004 -> coder_fix: `_BORN_RHO_FLOOR = 1.4` in likelihood.py should be
  `_BORN_RHO_FLOOR = RHO_HI` (RHO_HI already imported from low_w_shell_chart).
  The shell/Born handoff boundary is ONE concept; the census reads
  float(_BORN_RHO_FLOOR) so it auto-follows. Update the module comment.
- INS-1-005 -> coder_fix: census intercept-6 admits low_w_diffractive_chart
  only when the WHOLE band is smooth (w_hi*shell_delta_min<1), but production
  _low_w_shell_chart_serve band-splits at w_shell=1/delta_min (chart below,
  engine above). The census's OWN header docstring claims "served whole /
  band-split" — implementation deviates. Mirror via w_split detail (saddle_c3
  convention), band_split_mask + covers on the below-split sub-band. Both
  INS-1-004 and INS-1-005 ruled coder_fix (2026-08-21 triage).

## 2026-08-21 (low_w_chart_rho_partitioned plan — SUPERSEDED)
- rho-partitioned carrier hierarchy for the low-w diffractive chart planned.
  Professor rulings: (1) reuse geometric_amplification(w,y_eig,gp)+born_lead_carrier(w,y_eig,gp) in the REDUCED frame; residual stays r=f_pure*sqrt(1-g'^2)/F_ref; serve identity F=mass_sheet_phase*f_pure/lam is F_ref-independent. (2) rho_split=RHO_HI=1.4 (no new literal); deep interior (rho<RHO_LO, 4 img) uses the SAME macro/geometric ladder, NOT fold/cusp. (3) design (b): chart bakes ONLY the macro-carrier residual over full w_grid; serve splits the off-caustic band at w*Delta_tau>=RHO_END=4.0 (resolved->geometric analytic, unresolved->macro+chart). (4) Delta_tau=operator._real_delay_min_separation (min-pairwise real-image gap, ABSOLUTE frame). (5) absolute guard sup|r|<=1e3 AND inf|r|>=1e-3, trainer-only, carrier-specific domain (macro: w<w_split); deletes _NON_VANISHING_MIN_RATIO. (6) Airy macro-fold: F_ref=F_airy*(h+(1-h)*sqrt(mu_macro_pure)/|F_airy|), h=smootherstep(w*|delta_tau|,RHO_START,RHO_END); r->sqrt(1-g'^2) at low w NOT 1. (7) bars 1e-4 off-grid / 1e-10 node-exact. Simplifier: 3 sequential Coder WPs; rename fold_cusp_reference->partitioned_reference (Lean); (f_ref,kind) tuple return (Lean); named guard helper (Lean); bind smootherstep window to operator.RHO_START/RHO_END (no literals); _reduced_min_delay_separation wrapper (Lean); deep-interior serving free/keep; per-cell kind is provenance-ONLY, NO npz field (Trim). WP1 must include mechanical call-site updates (pass rho, unpack tuple).

## 2026-08-21 (low_w_shell_chart build — INS-2-001 DESIGN triage)
- INS-2-001 -> coder_fix (executing agent: TEST DEVELOPER, named explicitly in coder_instructions — Coders never author tests; not the "fully-prescriptive cheap fixture edit" exception since it needs 2 NEW test classes + an end-to-end harness). Verified in code: new test_lensing_low_w_shell_chart.py never calls production _low_w_shell_chart_serve (only chart.evaluate + local _reconstruct_f) and has NO load()/hash-tamper tests; deleted suite had ServeEngineNodeExactTestCase (production serve via reconstruct_farfield intercept + _engine_reference_kappa incl. kappa!=0) + LoadContractTestCase. Professor: t_min=0 intercept only (real-tail 1e-10 NOT guaranteed — INS-4-003 catastrophic-cancellation floor ~1.7e-11); stub _engine_farfield_total (above-split host) or the engine-vs-engine comparison is F002-vacuous; folded theta must be a grid node; serve never calls chart.covers (inline gates — covers() tests don't pin serve gating); hash covers the 6 physics arrays (4 axes + real/imag coeffs), provenance EXCLUDED (add a negative pin: provenance tamper still loads); scope "no f_schwinger" claim to the below-split band only. Simplifier fences: port ONLY the serve-node-exact harness + load contract (do NOT port FrefNonVanishing/Conservativeness/derate/CensusMirror/Cusp/CarrierAdequacyGuard/CoverageUnion — ~15 old classes pin the dead quotient); ONE tamper mutation family (single canonical pin per predicate); keep the ONE positive control (rehashed tamper loads cleanly, pins saver/loader hash-field agreement); straddle premise needs a MEASURED delta_min + anti-vacuity below.any()/above.any() assertions (fallback: below.all() premise if no fixture straddles); ~+200 lines max.