# Architect Short-Term Observations

2026-08-20 TRIAGE low_w_nearfold_chart INS-1-001 -> coder_fix. Amplitude de-rate with _HARD_DERATE_CEILING=0.85 is arithmetically incompatible with the two-sided |F_serve-F_engine|/|F_engine|<=1e-4 bar (0.85 serve = 15% low at exact-interp cells, 1500x bar); the 0.85 clamp is an inapplicable transplant from the w_low_fit CEILING de-rate (there de-rate repositions a served boundary, values stay unbiased). Ruling: derate = min(1.0, 1/max_overpred) over the FULL calibration grid (cap removed), sole role = never-over-serve + center worst overpred, so served two-sided error ~ raw interp error; _margin_report must measure the SERVED value |derate*r_interp - r_engine|/|r_engine| sup over full grid (stale-green oracle rule); un-certifiable (resonance-limited) cells get a baked per-cell DECLINE mask -> serve returns None -> engine (never amplitude scale); census mirror reads the mask engine-free, declined covered draws stay engine_residual. Driver: post-build bake must meet 1e-4 raw on full grid; declined cells remain engine-hosted in honest demand map. Test Dev must re-target the derate tests to two-sided served-value agreement.

2026-08-20 2026-08-20 TRIAGE INS-2-002 -> coder_fix. Content hash in low_w_diffractive_chart.py omits declined_mask at BOTH sites (train script ~L601, load recompute ~L365) though the mask is stored in the npz and is the INS-1-001 sole route-level protection for resonance-limited cells (all-False mask loads silently -> serve returns >CERTIFICATION_BAR amplitudes). Fix is a one-line variadic arg addition at both sites; must land BEFORE driver bake so shipped artifact hash covers the mask (no shipped artifact yet -> no backward-compat concern; in-build smoke bakes are transient). bool->float64 hashing is byte-deterministic. LoadContractTestCase tamper case -> Test Developer, not Coder.
\nlow_w_nearfold_chart plan: 4 Coder WPs (chart class -> training script -> likelihood serve wiring -> serve_route_census mirror). Professor rulings: reduced-frame residual r_pure = f_schwinger(w,y_eig,gamma')/[sqrt(1-gamma'^2)*prefactor_c(w)], serve re-modulates F_serve = mass_sheet_phase*prefactor_c*sqrt_mu_full(*born_factors[0])*r_fit; coverage = union {rho in [0.6,1.4]} OR {gamma'>0.5}; scalar de-rate; theta D2-fold [0,pi/2]; w in [w_lo,60]. Simplifier: separate class (no N-D base yet), share covers() predicate serve+census, no new ROUTE_KIND (draw-level route w/ empty kinds), don't write 3rd _content_hash.
