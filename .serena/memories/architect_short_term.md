# Architect Short-Term Observations

Build brief_ppgo_interior_certification: (previous build, retained for context).

Build brief_interior_sacrc: Interior SACR-C charts for positive-parity astroid. The infrastructure (from_engine + INTERIOR_SACR_C tag + serve path in _surrogate_coefficients + reconstruct_from_envelope) is already fully wired. The bottleneck is w-node density: interior tiles currently fall back to config.w_nodes_per_decade (default 4 smoke, no per-tile override since _reprovision_w_nodes only runs on exterior tiles). At WNPD=6 in tests, eps is 0.023 (gamma=0.40, already passes 0.05) and 0.072 (gamma=0.65, fails). Professor confirms: cubic spline h^4 scaling means 15-22 wnpd passes comfortably — no complex analytic-pair decomposition needed at mid-gamma. Simplifier: add interior_w_nodes_per_decade to TrainingConfig (parallel to interior_eps_max); the critical trap is that build_ff captures tile_w_nodes=None → must override tile_w_nodes or pass eff_w_nodes explicitly to the builder, not just display it in reports.