Build: saddle_forensics. Items 1+4 = driver post-build measurements. Item 5 = already probed (inter-lobe corridor, 0.0% overlap, closed). Item 6 = Professor ruling: ghost exp(-0.5j*pi) correct for both parities — no code change. Two Coder WPs: (WP-1) _subdivide_lobe_tile wrapper over shared _subdivide_tile + wire into _train_band_charts; (WP-2) lobe cusp carve-out in _SaddleLobeAdmission.admts (~0.1-0.15 y-units). Professor: lobe normalized-radius disease PATHOLOGICAL (r_deltoid->0 at cusps by |dtheta|^(1/3)), subdivision alone insufficient, cusp-adapted u=d^(2/3) coordinate is the clean fix but left for follow-on. Simplifier: WPs independent, keep separate; WP-2 placement in admts (not tiler).. Items 1-2 ALREADY SHIPPED (exterior FARFIELD_KERNEL_SUM no ghost; cusp exclude 0.2 in tiler). Item 3 = driver post-build measurement. Item 4 = ONE Coder WP: engine-intercept ppGO above-ceiling rung in _amplification_coefficients (before engine eval); gate w_max>150 AND w_lo*min_delta_tau>=RHO_END (4.0); whole-band serve via fold_ppgo_correction + reconstruct_farfield(FARFIELD_KERNEL_SUM); NO band-split, NO census mirror, NO kappa/beta guards. Simplifier: engine intercept (not surrogate path — unreachable), band-split trimmed, engine-intercept cleaner. Professor: 1e-2 at 150, 1e-3 at 500, decreasing trend; RHO_END=4.0; all-image serve; boundary-continuity primary gate.