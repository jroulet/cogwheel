# Architect Short-Term Observations

Build brief_ppgo_interior_certification: (previous build, retained for context).

Build brief_fold_corrected_ppgo: (previous build, retained for context).

Build brief_interior_wedge_chart: Replace positive-parity interior chart architecture (FarFieldChart in far-field-smooth (s,d) coords, w_max capped by DD product) with InteriorWedgeChart in caustic-relative (r, theta_wedge) coordinates. Key decisions: (1) Symmetry is D₂ (Klein four-group, two axis reflections), NOT Z₄ — quadrant fold via (|y₁|, |y₂|). (2) New separate dataclass (not retrofit into FarFieldChart — spatial axes semantically different). (3) r = |y|/r_caustic(gamma, theta), same as existing _to_caustic_fixed interior arm. (4) Flat rectangular w-band (no r-dependent w_max — that's step 9 optimization). (5) theta_to_s arc-length remap for angular axis (replaces manual cusp knots), same pattern as LobeInteriorChart. (6) _WedgeCausticMap precomputed table for serve-time r_caustic interpolation (simpler than _FarFieldArcMap, no cumulative integration). (7) _assert_carrier_continuity wired into from_wedge_engine at training time. Professor corrections: D₂ not Z₄/D₄; caustic is elongated along y₂ axis (cusp distances differ at y₁ vs y₂ axes). Simplifier confirms: separate class justified, from_wedge_engine in scope (step 9 exclusion targets production tiling/LOO/census, not the basic engine loop).