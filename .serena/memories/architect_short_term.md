# Architect Short-Term Observations

- low_w_chart_airy_representation (2026-08-20): residual keeps sqrt(1-γ′²) — F_ref (q=p, ABSOLUTE frame, airy_fold_value) replaces prefactor_c ONLY; serve F = mass_sheet_phase·F_ref·sqrt_mu_full·(derate·r) = mass_sheet_phase·f_pure/lam. Global w^{2/3} axis (smoke 8 / full 16 over [0.0737,15.33]); delta_tau only inside F_ref. Per-cell F_ref-unbuildable decline folded into declined_mask (census mirrors covers()+declined() -> auto-correct). Schema bump v1->v2; rename log_w_grid->w23_grid. Builder lives in low_w_diffractive_chart.py (consumer placement), imports geometry, never calls fold_amplification. F075 does NOT apply (F_ref is a demod carrier, not a serve value). Tolerances: node-exact 1e-10 (pre-derate), residual min/max >=1e-1 (shell >=3e-1), F_ref min/max >=1e-1, w->0 anchor ±1e-2. Brief's literal "r=f_pure/F_ref" is WRONG (drops sqrt(1-γ′²)).

## 2026-08-21 build (low_w_chart_cusp_fallback)
- Scope widening (Professor ruling): "do not touch _pearcey_cusp internals" is UNREACHABLE — the pure cusp `uniform` form is private (F074 controls + matched-asymptotic A/sigma_c inline in cusp_amplification); calling cusp_amplification as the reference is UNSOUND (F074 gate 0.35/sqrt(w) needs envelope_bar >= ~2.5 at low w; the raised bar opens the ppGO rung which returns fold_ppgo_correction/raw-ppGO for w in [8,60] = hard F_ref discontinuity; calibration cert len-mismatches at low w). Fix = add ONE additive public accessor `cusp_uniform_reference` to _pearcey_cusp returning CLUSTER-ONLY `uniform = cluster_sum*(P/P_asymp)` with the 3 serving gates bypassed.
- Reference = `uniform` (cluster-only, NO far_sum) — mirrors Airy F_ref's merging-pair-only convention; total=uniform+far_sum adds destructive zero-crossings and far_sum is a poor carrier at low w.
- Simplifier: Option B (extract shared _cusp_uniform_form helper, refactor cusp_amplification onto it byte-identically, add accessor) NOT Option A (duplicate ~50 lines of the F074 control map — the one piece that silently broke pre-F074). Rename airy_fold_reference -> fold_cusp_reference (3 production files + 12 test refs).
- Non-vanishing guard: min|F_ref|/max|F_ref| < 1e-3 -> decline (P can vanish on interior cusp fold lines; exterior cusp cells P != 0).
- Low-w note: F_cusp ~ A w^{1/2} P -> 0 at w->0, so residual r ~ w^{-1/2} (bounded, ~55x dynamic range), unlike Airy Wronskian (r -> w^{1/6} -> 0).
- SPEC/DATA_CONTRACTS stale from PRIOR build: still say schema v1 + log_w_grid + prefactor_c; code is v2 + w23_grid + F_ref. Librarian fixes BOTH the staleness AND adds fold/cusp reference.
- Serve re-modulates via the SAME fold_cusp_reference (single-source) -> serve needs NO logic change, only the rename; census mirror unchanged (reads declined_mask engine-free, never rebuilds F_ref).
