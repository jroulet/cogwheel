# Architect Short-Term Observations

Build exterior_rho_axis_conditioning (2026-08-10): ADDING `rho_log_axis: bool` to ExteriorPolarChart. Mirrors theta_to_u but simpler — analytic `ur = log(rho-1)` vs persisted (2,N) map. Simplifier: trim map → analytic bool + REQUIRED (always built, no parity split). Professor: log(rho-1) correct for power-law envelope decay; no phase carrier needed on rho; 4-node sufficiency gated by A/B test. Old tag `exterior_polar_carrier_demod_v2` → new tag `exterior_polar_rho_log_v3`, old tag removed from KNOWN set. Single Coder WP. reconstruct_farfield + census unaffected.

Build exterior_ghost_gate_exclusion (2026-08-10): architect design phase complete. Plan: single Coder WP adding `_exclude_ghost_dominated()` to surrogate_training.py, wired into `_farfield_exterior_tiles` (positive-parity only). The check calls `geometry.ghost_kernel` at tile corners+center; tiles where ghost EXISTS and Im(tau_c) < 0.4 are excluded. Ghost-non-existence → retain (no contaminating ghost). Multi-gamma band-edge sampling. Simplifier: lean; Professor: correct physics, gate distinction critical. Test Dev gets 8 domain_test_descriptions. (2026-08-10): DISCOVERY — the pure power-law decay |E|~w^(-0.60) is exactly representable by a cubic B-spline in log_w (it's exponential in log_w, which is beyond cubic's exact representability), but the Simplifier's Phase-Is-The-Culprit diagnostic is definitive: sinces the spline fits |E| alone perfectly and the measured ~0.05 abs error comes from phase rotation of the complex E. The real/imag parts oscillate in w, which a cubic in log_w cannot track above ~3 nodes per cycle. Professor: |E| power-law alone would be spline-able if fitted directly, but the oscillatory phase is the error driver. Design: ln|E| + carrier-demodulated phase ordinate (option c). Per-chart constant k_chart = median of per-node unwrapped-phase slopes; |δk| < 0.01 tolerance. One Coder WP touching surrogate.py only; reconstruct_farfield untouched.

Build cusp_exclusion_cut (2026-08-09): exterior tile-count ~500 → ~70 by fixing cusp exclusion. Root causes: (1) `_CUSP_EXCLUSION_DISTANCE=0.2` too tight — tiles at 0.206 from cusps fail (median eps 0.0039); (2) `_exclude_near_cusp` only checks astroid cusps; (3) saddle exterior path has no cusp exclusion. Professor: use 1e-4 threshold for turn-on measurement, check at band edges (not just gamma_mid), same A3 physics for both astroid + deltoid cusps. Simplifier: keep single constant; wire saddle via `_farfield_tiles` kwargs, not by switching to `_farfield_exterior_tiles`. (2026-08-09): exterior tile-count ~500 → ~70 by fixing cusp exclusion. Root causes: (1) `_CUSP_EXCLUSION_DISTANCE=0.2` too tight — tiles at 0.206 from cusps fail (median eps 0.0039); (2) `_exclude_near_cusp` only checks astroid cusps; (3) saddle exterior path has no cusp exclusion. Professor: use 1e-4 threshold for turn-on measurement, check at band edges (not just gamma_mid), same A3 physics for both astroid + deltoid cusps. Simplifier: keep single constant; wire saddle via `_farfield_tiles` kwargs, not by switching to `_farfield_exterior_tiles`.
