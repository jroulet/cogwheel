# Architect Short-Term Observations

(2026-08-19 diffractive_certificate_reach plan) diffractive_w_low only searched DOWNWARD; fix brackets UP to the honest ceiling (N/2N tail ratio). Professor narrow ruling: up-bracket ONLY on honest_error<=CERTIFICATION_BAR path; keep deep-optimistic None + down-search(bar_inner) branches. Up-search targets OUTER 1e-4 bar; thread kw-only w_lo/w_hi (w_hi=cap, floor-fail->None). N/2N is estimator not bound -> re-target engine sweep to [w_lo,0.9*w_low]; tightness via _honest_tail_ratio at w* and 1.5w* engine-free. THIRD consumer _low_w_diffractive_serve (likelihood:2014) must be threaded too. Order scan = post-build driver measurement; default stays 8. Hardening: _honest_tail_ratio must return inf on non-finite total/tail (nan>bar is False -> doubling overshoots).

(2026-08-19 diffractive_certificate_fit plan) Replace the diffractive_w_low per-proposal scan with fitted w_low_fit (order-16 engine-honest ceiling vs f_schwinger). Professor: recon prefactor cancels identically; measure truth as full recon*total vs f_schwinger at kappa=0 (total is phase-stripped, converges to f_schwinger*e^{-0.5j w s}); cap fit at DD ceiling 60 (saturate >=60, skip mpmath-QD); representation = log-log poly in (log gamma', log s, log(1-gamma')) + 4-fold harmonics cos(4k theta) (even-about-axes, period pi/2); keep (lam*sqrt_mu)^(1/(M+1)) as explicit multiplier OUTSIDE the fit (kappa path); de-rate D~0.7-0.85 from worst residual; DELETE deep-optimistic None semantics (no non-convergent interior; near-wall w_low_true is SMALL not None); refusal = closed-form gate only (wall via _reduced_shear DiffractiveDomainError, degenerate sqrt_mu -> None). Simplifier: TWO WPs additive->subtractive (WP1 w_low_fit+fit script+coefficients, diffractive_w_low untouched; WP2 delete scan+helpers+swap 3 consumers+census mirror+shrink diffractive_refusal_errors to (DiffractiveDomainError,)); delete scan not retain (fit-vs-scan = fit-vs-wrong-estimator); baked floats not npz (no new data product); calibration minutes is post-build driver step (smoke-scale in-build). ~30 test refs split by fate (scan-internal witness classes deleted; served-semantics classes rewritten against w_low_fit).
(2026-08-19 triage INS-1-003) fit_diffractive_certificate de-rate `min(1.0, min(0.85, 1/max_overpred))`: outer clamp DEAD (always <=0.85), comment claims unreachable 'no de-rating when conservative'. Professor ruling: KEEP hard 0.85 floor (grid-sparsity safety margin against over-serve, the bug class under repair; max_overpred is optimistically low on finite grid); the only fixes are remove dead clamp + rewrite comment (semantics a). Inspector's 'drop the clamp' option is OFF the table; conditional D=1.0 forbidden.
(2026-08-19 triage INS-2-002) `_diffractive_bottom_ceiling(self, lens, *, w_lo=None, w_hi=None)` (likelihood.py:2718): `w_lo` DEAD — body forwards only `w_hi` to `w_low_fit` (whose `w_lo` was already dropped by INS-1-004), and the docstring's "`w_low <= w_lo` null-split identity applied at the call sites" is FALSE — verified all 3 production/census call sites handle the null-split via `_band_split_mask(dense_w, w_low)` (band_split=False when w_low <= dense_w.min()) + explicit `w_low >= dense_w.max()` whole-band branch; `w_lo` never consulted. Verdict coder_fix (incomplete INS-1-004 cleanup): drop `w_lo` from wrapper + 3 call sites (likelihood.py:2889/3088, serve_route_census.py:856) + test `_compose` (test_lensing_diffractive.py:730); reword wrapper docstring AND the identical stale null-split claim in `w_low_fit`'s Notes; drop stale "(down-search branch, not capped at w_hi)" parentheticals at likelihood.py:2895/3094 (scan retired). Behavior-preserving (null-split fully mask-owned); test stubs in test_lensing_born_certificate.py:744/808/1524/1726 remain functional but may drop `w_lo` for parity.
(2026-08-19 plan diffractive_certificate_fit_fix_aliasing) GATE-FAILED build: w_low_fit over-serves off-grid ~5x (16 harmonics cos(4kθ) k=1..16 fitted on 8 thetas -> angular DOFs underdetermined/aliased). Fix: (a) _DIFFRACTIVE_FIT_N_HARM decoupled from _DEFAULT_MAX_ORDER -> 4; (b) _grid_points theta -> 32 (Professor: 16 STILL aliases k=3->k=1, k=4->constant; Nyquist for cos(4kθ) is N>=8K=32); (c) fold OFF-GRID theta-midpoint overpred into max_overpred (data-driven de-rate, Simplifier) + off-grid margin report; (d) re-bake --scale full in-build (~35min: 960 grid + 240 offgrid + 12 random x 1.75s), paste verbatim w/ provenance SHA (commit script edit FIRST so SHA identifies script state). ONE Coder WP. Tests (Test Dev): NEW OffGridOverServeOracleTestCase (end-to-end rel-err at w=w_low_fit over off-grid theta midpoints, gamma{0.2,0.3} x r{0.5,0.9,1.1} ~192pts ~25s) = permanent pin; RE-SCOPE FullGridCertificateOracleTestCase (32-theta grid 4x cost ~140s -> subsample thetas, keep gamma-r corner coverage, stay <60s). Ratio<=1.0 + tightness 90%/0.5 + median>=0.7 = ACCEPTANCE RECORD (fit script margin report), NOT permanent tests (Simplifier TRIM; Professor's exact-ratio _measure_w_low_true suite dropped). Zero-tolerance = w_low_fit<=w_low_true*(1+1e-5) (_measure_w_low_true returns always-honest lo, ~3.4e-7 width). 0.85 hard de-rate floor KEPT.
(2026-08-19 triage INS-3-001/002, diffractive_certificate_fit) Inspector findings VERIFIED against code: smoke-baked `_DIFFRACTIVE_FIT_DERATE=0.85` + coefficients over-serve up to 4.7x (likelihood 17.8x) because smoke grid samples only gammas (0.1,0.2,0.3) x radii (0.5,0.9) — out-of-sample r~0.3, r~1.1-1.3, gamma 0.4-0.5 unconstrained, 0.85 ceiling cannot absorb. Oracle suite sweeps ONLY CLEAN_GAMMAS at Y_REF=(0.8,0.4) (r~0.894) = conservative region -> stale bake ships GREEN. TestWLlowFitSMonotonicity sweeps s<=0.4 and its docstring ADMITS the positive log(s)^2 up-turn for s~0.5-0.6 "FLAGGED rather than pinned"; TestWLlowFitDerateTeeth proves the de-rate MECHANISM only. Verdicts: BOTH coder_fix — my plan's post-build-calibration phasing was a cost hedge overturned by the Inspector's measured ~1-2 min full-scale run (well inside fast-tier budget); baked coefficients ARE the deliverable, so smoke bake fails the build's own acceptance. INS-3-001 = Coder re-bakes in-build with `fit_diffractive_certificate.py --scale full` (seed 42, n_w=16, degree=2), pastes VERIFIED emission verbatim (expected derate 0.745168, provenance SHA from working-tree HEAD at run time — commit fit script first, then run+commit emission for a correct SHA), margin report to change report for the completion record; script docstring line 45 'post-build' -> 'in-build final bake' so the driver doesn't redundantly re-bake. INS-3-002 = TEST-side (Test Developer, NOT Coder): oracle sweep must cover the full calibration grid via the fit script's `_grid_points('full', seed=42)` (reuse to prevent drift), pin small-r/large-r/gamma 0.4-0.5; TestWLlowFitSMonotonicity re-scope or extend so the documented artifact is pinned. Cost ~500 engine evals ~15-30s. Driver post-build step becomes verify-only re-run.
