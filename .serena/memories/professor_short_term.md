# Professor Short-Term Observations

- **2026-08-XX CarrierDiscontinuityError grace test review (PASS)**: Verified `test_reprovision_catches_carrier_discontinuity_error` in `ReprovisionNodeCountTestCase`. The test correctly mocks `_build_farfield_chart` to raise `CarrierDiscontinuityError`, confirming that the `_eps_for` helper catches it and returns `None`; the outer `_reprovision_w_nodes` then returns the conservative full density (`n_start`) with `status='engine_refused'`. The trace carries the `carrier_discontinuity` entry with the detail string. All 5 assertions match spec. Full suite: 77 passed, 1 xfailed (the literal-bar xfail is a known tolerance tracking item). Physics reasoning: when the tile straddles a carrier-basin flip, the reprovision probe cannot meaningfully measure interpolation error — the correct conservative default is to keep the full node density (never guess a reduction), which is what the code does.
