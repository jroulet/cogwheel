2026-08-01 prof_review (lobe sqrt-edge coordinate tests): ALL 54 tests in test_lensing_surrogate_lobe.py PASS (51.16s). Verified: (1) COORDINATE ROUND-TRIP: theta_to_s formula s = sqrt(span) - sqrt(theta_max - theta) matches production code in surrogate.py:3001-3002 exactly; round-trip error at machine epsilon (~3e-17), well within 1e-4 budget; strict monotonicity confirmed; shape (2,2001) with _LOBE_ARC_MAP_SIZE=2001; s[0]=0.0 exact. (2) NPZ PERSISTENCE: theta_to_s bit-for-bit through save/load; schema tag _LOBE_AXIS_SCHEMA round-trips. (3) BOUND-SHIFT MARGIN: swing < 0.01 bar (measured ~0.003); shifted charts' theta_to_s all match own closed-form oracle; sqrt-edge not worse than uniform (at this smoke tile both ~0.138). F042 knife-edge is a PRODUCTION phenomenon at cusp-adjacent tiles; test honestly encodes the measured reality rather than falsely claiming dramatic swing sensitivity at this scale. (4) V1 IDENTITY PATH: theta_to_s=None chart serves finite values, round-trips byte-identical through npz, schema tag = _LOBE_AXIS_SCHEMA_V1, no theta_to_s key in npz, mutation check confirms TypeError on None indexing. Self-falsification (wrong-orientation oracle, perturbed inverse map) both have proper teeth. Heavy full-sampling validation deferred to operator.