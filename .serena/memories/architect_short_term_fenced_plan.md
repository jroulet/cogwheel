Fenced w_low_fit build plan (diffractive_certificate_fit_fenced): ONE Coder WP (production `_diffractive.py` fence + fit-script fencing + smoke re-bake) + Test Dev. Professor ruled two-sided fence (deep interior served at ceiling, not declined) with delta 0.35-0.5, discriminator rho_code miscalibrated-but-monotone (kept, O(1) constraint). Simplifier: lean one WP, fence in grid generators, trim prior-mass fraction to row fraction, delete dead corner-witness block, smoke bake ~750s run early.