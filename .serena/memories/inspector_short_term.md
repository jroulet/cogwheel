2026-07-31 seventh/final read-only far-field review: PASS. The last carrier-continuity terminology correction is complete: _assert_carrier_continuity and _assert_farfield_carrier_continuity now use/document (n_gamma,n_s,n_d) and adjacent axes (gamma,s,d). The only remaining n_rho/n_theta search hits are in the separate lobe-local API/provenance, where they are correct. Prior fixes remain coherent: DATA_CONTRACTS requires axis_schema='farfield_arclength_s_perp_d_framewinv'; _validate_farfield_axis_schema describes the gamma-resolved (s,d) coordinate; _farfield_serves rejects parity != 1 and maps source through the chart arc map; _evaluate_chart remains threaded with y1_eig/y2_eig. Final clean check: branch claude-dev, git diff HEAD --check clean, py_compile surrogate.py/surrogate_census.py passed, manual+loaded macro-saddle fall-through test passed (1 passed, 4.57s). Earlier targeted schema/degenerate/straddling checks remained green; no files modified.