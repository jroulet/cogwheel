# Foreman Short-Term Observations (2026-08-08, INS-3-004)

- SENTINEL THREE-CASE CONTRACT FIX (test_lensing_exterior_windows.py, `_synthetic_exterior_polar_chart`): the finding flagged that `theta_to_u=None` without `u_grid` leaked the `_SENTINEL` default through to `from_values`, raising ValueError. Fix = rewrite the sentinel block as `if both _SENTINEL: build identity-like map / elif either is None: force BOTH to None / else pass through`. Note: `ExteriorPolarChart` stores ONLY `theta_to_u` (u_grid is consumed by from_values to build knots) — verify via `c.theta_to_u is None`, not a `u_grid` attribute. From_values validation traps for smoke fixtures: map row 0 must start at `theta_c_grid[0]` (NOT an arbitrary value), and `u_grid` length must equal `theta_c_grid` length. n=3 violates the production >=4 nodes/axis constraint — use n=4 in smoke tests.
- Pre-existing unrelated failure `SelfFalsificationTestCase::test_equal_labels_lose_the_representational_contrast` (ValueError: Unknown far-field envelope-definition tag 'interior_sacr_c_envelope') — the parallel build's uncommitted production channels.py `KNOWN_FARFIELD_DEFINITIONS` rejects the new `INTERIOR_SACR_C` tag that the new `_interior_chart`/`_interior_heldout_eps` path passes to `from_engine`. PROVEN pre-existing: reverting ONLY my sentinel edit (targeted file-surgery, not git stash which also reverts the parallel build's production edits) reproduces the identical failure; it never enters `_synthetic_exterior_polar_chart`. Out of INS-3-004 scope — note for the parallel build owner.
- DIFF TRAP re-confirmed: the entire sentinel block + `_synthetic_exterior_polar_chart` are part of the parallel build's uncommitted WP1 block, so `git diff` shows everything as `+`; my edit's `elif` lines can't be isolated via diff — verify via targeted source string asserts instead.
- TARGETED-REVERT PROOF PATTERN: to prove a failure is independent of a specific edit when the tree has unrelated uncommitted changes, revert ONLY the edit in-memory (read file, remove the exact new lines, write, run test, restore from backup copy) — git stash is unusable because it also reverts the parallel build's production changes and changes test skip-vs-fail behavior.