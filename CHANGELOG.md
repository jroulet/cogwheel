# Changelog

> When something breaks after a `git pull`, look here first.

This file is **generated** from fragments in `changelog.d/` — do not edit it
directly. Add an entry by creating `changelog.d/<date>_<slug>.md` with frontmatter
`date: YYYY-MM-DD` and running `python scripts/render_fragments.py`.

## 2026-06-05

### TejaForce pipeline installed on existing codebase

Installed the TejaForce 10-agent development pipeline (SDK orchestrator, crew
prompts, hooks, data-infrastructure modules, and mind-meld memory sharing) on the
`claude-dev` worktree branch. No changes to `cogwheel/` source.
