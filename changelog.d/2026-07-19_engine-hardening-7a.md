---
date: 2026-07-19
---
### The engine hardens at its boundaries

Three certify-or-refuse gaps closed in the Chang-Refsdal engine.  A
runtime index-theorem census check now guards the image solver's one
producer, converting a silent near-axial image-pair drop into a named
refusal on both parities.  Strong-shear positive-parity configurations
that the legacy wave path refused near ``gamma ~ 0.5`` are now served
by the exact Schwinger evaluator (certified against an independent
oracle at 1e-10, with every previously-certified output byte-frozen) —
in practice the wave branch's strong-shear refusal onset moves to the
Schwinger ceiling, clearing the way to widen the sampled shear range.
And fold-degenerate images — which crashed a production sampling run
with a raw linear-algebra error — now raise the engine's named domain
refusal from the stationary-phase kernel (scoped to exactly the crash
class: the channel layer's bounded gauge keeps consuming near-singular
metrics by design).  The census guard understands degenerate Morse
censuses (on-fold merged pairs and on-cusp triple merges pass; the
dropped-pair defect class still refuses), and the sampler-facing
posterior maps the full named-refusal vocabulary — now including
Schwinger certification failures and bin-resolution refusals — to
``lnL = -inf``.

