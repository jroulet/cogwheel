---
date: 2026-07-18
---
### Microlensed likelihood: SACR-C beat-free channel construction

The Chang-Refsdal channels are rebuilt as the switched-analytic +
single-envelope decomposition: persistent resolved images are carried
by the analytic saddle kernel under smootherstep weights keyed on each
image's delay distance from the parked critical carrier, and ONE
beat-free transition envelope (demodulated phase bounded at 4 rad by
construction) is the only interpolated object — evaluated on a
leave-one-out-adaptive coarse engine grid whose node count is
config-independent (ceiling 48, typically ~21-44) instead of the former
~100 fixed nodes, then reconstructed in closed form. The reconstruction
identity holds at machine precision against the untouched exact oracle;
every accuracy gate (RB-vs-brute, near-cusp pin, zero-noise floors,
macro-magnification limit, fold/cusp crossing boundedness) passes at
original tolerances. Warm single-thread lnlike measures ~29 ms with the
exact 1F1 derivative ladder now ~89% of the remaining cost — the target
of the planned candidate/fiducial ratio layer toward the few-ms goal.
